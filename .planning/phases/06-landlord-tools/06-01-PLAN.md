---
phase: 06-landlord-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - britv3.0/supabase/migrations/20260307_epic7_property_management.sql
  - britv3.0/src/types/landlord.ts
  - britv3.0/src/lib/file-validation.ts
  - britv3.0/src/lib/image-compression.ts
  - britv3.0/src/lib/rent-period.ts
  - britv3.0/package.json
  - britv3.0/src/__tests__/landlord/file-validation.test.ts
  - britv3.0/src/__tests__/landlord/rent-period.test.ts
autonomous: true
requirements: [LL-01, LL-02, LL-03, LL-05, LL-06, LL-08]

must_haves:
  truths:
    - "Landlord tables (tenancies, maintenance_requests, financial_entries, property_documents) exist with RLS"
    - "TypeScript types match database schema exactly"
    - "File validation correctly identifies PDF, JPG, PNG via magic bytes"
    - "Rent period calculation handles weekly and monthly frequencies correctly"
  artifacts:
    - path: "britv3.0/supabase/migrations/20260307_epic7_property_management.sql"
      provides: "4 tables, enums, indexes, RLS policies, RPC functions, storage buckets"
      contains: "CREATE TABLE tenancies"
    - path: "britv3.0/src/types/landlord.ts"
      provides: "All landlord domain types and Zod schemas"
      exports: ["Tenancy", "MaintenanceRequest", "FinancialEntry", "PropertyDocument", "tenancySchema", "maintenanceRequestSchema", "financialEntrySchema", "documentUploadSchema"]
    - path: "britv3.0/src/lib/file-validation.ts"
      provides: "Magic byte file type validation"
      exports: ["validateFileType"]
    - path: "britv3.0/src/lib/rent-period.ts"
      provides: "Rent period calculation and status derivation"
      exports: ["getRentStatus", "calculateCurrentPeriodStart"]
  key_links:
    - from: "britv3.0/src/types/landlord.ts"
      to: "supabase migration"
      via: "Type definitions match table columns"
      pattern: "tenancy_status|maintenance_status|financial_entry_type"
---

<objective>
Set up the database schema, TypeScript types, Zod validation schemas, and shared utilities for the entire landlord tools phase.

Purpose: Every subsequent plan depends on these types, schemas, and utilities. Installing dependencies and creating the migration first eliminates blockers.
Output: Migration SQL, type definitions, Zod schemas, file validation utility, image compression wrapper, rent period calculator.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-landlord-tools/06-RESEARCH.md
@britv3.0/docs/epic7final.md
@britv3.0/src/types/auth.ts
@britv3.0/src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create migration, and define types</name>
  <files>britv3.0/package.json, britv3.0/supabase/migrations/20260307_epic7_property_management.sql, britv3.0/src/types/landlord.ts</files>
  <action>
1. Install new dependencies from britv3.0/:
   ```bash
   pnpm add jspdf browser-image-compression
   ```

2. Create the migration SQL file at `britv3.0/supabase/migrations/20260307_epic7_property_management.sql` using the EXACT schema from Epic 7 spec section 4 (Database Schema). This includes:
   - 5 enums: tenancy_status, maintenance_status, maintenance_priority, financial_entry_type, document_category
   - 4 tables: tenancies, maintenance_requests, financial_entries, property_documents
   - All indexes (12 total)
   - Triggers: updated_at for tenancies and maintenance_requests, calculate_reminder_date for property_documents
   - RLS policies: enable on all 4 tables, create policies per spec
   - RPC functions: get_property_financial_summary, get_documents_due_for_reminder
   - Storage buckets: maintenance-photos (1MB), expense-receipts (2MB), property-documents (2MB) with RLS policies
   - Copy the SQL from the epic spec verbatim -- it is production-ready.

3. Create `britv3.0/src/types/landlord.ts` with:
   - TypeScript type aliases matching each table (using Readonly<{}> wrapper per project convention)
   - Enums as const arrays: TENANCY_STATUSES, MAINTENANCE_STATUSES, MAINTENANCE_PRIORITIES, FINANCIAL_ENTRY_TYPES, DOCUMENT_CATEGORIES
   - Zod schemas for form validation:
     - tenancySchema: tenant_name required, lease_start_date required, rent_amount positive, optional email/phone/lease_end_date/deposit
     - maintenanceRequestSchema: title (max 200), description (max 2000), priority enum
     - financialEntrySchema: type enum, category string, amount positive, entry_date, optional description/rent_period_start/rent_period_end
     - documentUploadSchema: name required, category enum, optional expiry_date
   - Inferred form types from Zod schemas
   - Income categories: 'rent', 'deposit', 'other_income'
   - Expense categories: 'maintenance', 'insurance', 'service_charge', 'ground_rent', 'mortgage', 'management_fee', 'other_expense'
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Migration SQL file exists with 4 tables, RLS, RPC functions, and storage buckets. TypeScript types and Zod schemas exported from landlord.ts. jspdf and browser-image-compression in package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared utilities with tests</name>
  <files>britv3.0/src/lib/file-validation.ts, britv3.0/src/lib/image-compression.ts, britv3.0/src/lib/rent-period.ts, britv3.0/src/__tests__/landlord/file-validation.test.ts, britv3.0/src/__tests__/landlord/rent-period.test.ts</files>
  <behavior>
    - validateFileType returns {valid: true, mimeType: "application/pdf"} for a file starting with %PDF bytes
    - validateFileType returns {valid: true, mimeType: "image/jpeg"} for JPEG magic bytes (0xFF 0xD8 0xFF)
    - validateFileType returns {valid: true, mimeType: "image/png"} for PNG magic bytes (0x89 0x50 0x4E 0x47)
    - validateFileType returns {valid: false, mimeType: null} for a .exe file or random bytes
    - getRentStatus returns "paid" when payment >= rent_amount in current period
    - getRentStatus returns "partial" when 0 < payment < rent_amount
    - getRentStatus returns "overdue" when no payment and period has started
    - getRentStatus returns "not_due" when current date is before period start
    - calculateCurrentPeriodStart handles monthly frequency correctly (adds N months from lease start)
    - calculateCurrentPeriodStart handles weekly frequency correctly (adds N*7 days)
    - Edge cases: Feb 29 leap year, lease starting Jan 31 with monthly frequency
  </behavior>
  <action>
1. Create `britv3.0/src/lib/file-validation.ts`:
   - Export `validateFileType(file: File): Promise<{valid: boolean; mimeType: string | null}>` using inline magic byte checks for PDF (0x25 0x50 0x44 0x46), JPEG (0xFF 0xD8 0xFF), PNG (0x89 0x50 0x4E 0x47).
   - Read first 8 bytes via `file.slice(0, 8).arrayBuffer()`, compare against signatures.
   - Also export `MAX_FILE_SIZES` constant: { 'maintenance-photos': 1048576, 'expense-receipts': 2097152, 'property-documents': 2097152 }.

2. Create `britv3.0/src/lib/image-compression.ts`:
   - Export `compressImage(file: File, options?: {maxSizeMB?: number; maxWidthOrHeight?: number}): Promise<File>` wrapping browser-image-compression.
   - Default options: maxSizeMB=0.5, maxWidthOrHeight=1920, useWebWorker=true.
   - Export `compressReceipt(file: File): Promise<File>` with maxSizeMB=0.5 for receipt images (skip if PDF).

3. Create `britv3.0/src/lib/rent-period.ts`:
   - Export `calculateCurrentPeriodStart(leaseStartDate: string, frequency: 'weekly' | 'monthly'): Date`
     - For monthly: count months from lease start to now, return the most recent period start date.
     - For weekly: count 7-day intervals from lease start.
   - Export `getRentStatus(tenancy: {lease_start_date: string; rent_amount: number; rent_frequency: string}, payments: {category: string; entry_date: string; amount: number}[]): 'paid' | 'partial' | 'overdue' | 'not_due'`
     - Filter payments by category='rent' and entry_date >= currentPeriodStart.
     - Sum amounts. Compare to tenancy.rent_amount.

4. Write tests FIRST (RED), then implement (GREEN):
   - `britv3.0/src/__tests__/landlord/file-validation.test.ts`: Test all magic byte scenarios + rejection.
   - `britv3.0/src/__tests__/landlord/rent-period.test.ts`: Test all rent status scenarios + edge cases.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test:run src/__tests__/landlord/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>File validation correctly identifies PDF/JPG/PNG via magic bytes and rejects other types. Rent period calculation handles weekly/monthly with edge cases. All tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes from britv3.0/
- All landlord tests pass: `pnpm test:run src/__tests__/landlord/ --reporter=verbose`
- Migration SQL file has 4 CREATE TABLE statements, RLS ENABLE on all 4, storage bucket inserts
- TypeScript types export all entity types and Zod schemas
</verification>

<success_criteria>
- Migration SQL ready for Supabase (4 tables, RLS, RPC functions, storage buckets)
- All TypeScript types and Zod schemas exported from landlord.ts
- File validation utility tested (magic bytes for PDF, JPG, PNG)
- Rent period calculator tested (weekly, monthly, edge cases)
- Image compression wrapper created
- jspdf and browser-image-compression installed
</success_criteria>

<output>
After completion, create `.planning/phases/06-landlord-tools/06-01-SUMMARY.md`
</output>

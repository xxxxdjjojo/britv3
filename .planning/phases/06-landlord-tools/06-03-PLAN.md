---
phase: 06-landlord-tools
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - britv3.0/src/services/landlord/maintenance-service.ts
  - britv3.0/src/app/api/properties/[id]/maintenance/route.ts
  - britv3.0/src/app/api/maintenance/[id]/route.ts
  - britv3.0/src/components/landlord/MaintenanceList.tsx
  - britv3.0/src/components/landlord/MaintenanceForm.tsx
  - britv3.0/src/components/landlord/MaintenanceStatusBadge.tsx
  - britv3.0/src/components/landlord/ProviderAssignment.tsx
  - britv3.0/src/app/(protected)/dashboard/landlord/properties/[id]/maintenance/page.tsx
  - britv3.0/src/app/(protected)/dashboard/landlord/properties/[id]/maintenance/new/page.tsx
  - britv3.0/src/app/(protected)/dashboard/landlord/properties/[id]/maintenance/[requestId]/page.tsx
  - britv3.0/src/__tests__/landlord/maintenance.test.ts
  - britv3.0/src/__tests__/landlord/provider-assignment.test.ts
autonomous: true
requirements: [LL-03, LL-04]

must_haves:
  truths:
    - "Landlord can create a maintenance request with title, description, priority, and up to 3 photos"
    - "Maintenance requests display in a filterable list by status and priority"
    - "Landlord can update maintenance status through the state machine (new -> acknowledged -> assigned -> in_progress -> resolved -> closed)"
    - "Landlord can assign a contractor via marketplace link and it sets assigned_provider_id on the request"
    - "Photos are compressed client-side before upload to Supabase Storage"
  artifacts:
    - path: "britv3.0/src/services/landlord/maintenance-service.ts"
      provides: "Maintenance CRUD + photo upload"
      exports: ["getMaintenanceRequests", "createMaintenanceRequest", "updateMaintenanceRequest", "uploadMaintenancePhoto"]
    - path: "britv3.0/src/components/landlord/MaintenanceForm.tsx"
      provides: "Maintenance creation form with photo upload"
      min_lines: 50
    - path: "britv3.0/src/components/landlord/ProviderAssignment.tsx"
      provides: "Contractor assignment via marketplace link"
      min_lines: 20
  key_links:
    - from: "britv3.0/src/components/landlord/MaintenanceForm.tsx"
      to: "/api/properties/[id]/maintenance"
      via: "Form submission + photo upload"
      pattern: "fetch.*maintenance"
    - from: "britv3.0/src/components/landlord/ProviderAssignment.tsx"
      to: "/marketplace/search"
      via: "URL link with category filter"
      pattern: "marketplace/search"
---

<objective>
Build maintenance request management and contractor assignment for landlords.

Purpose: Landlords need to log, track, and resolve property maintenance issues, optionally assigning contractors from the marketplace.
Output: Maintenance CRUD with photo uploads, status tracking, and simple marketplace-linked contractor assignment.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-landlord-tools/06-RESEARCH.md
@.planning/phases/06-landlord-tools/06-01-SUMMARY.md
@britv3.0/docs/epic7final.md
@britv3.0/src/types/landlord.ts
@britv3.0/src/lib/file-validation.ts
@britv3.0/src/lib/image-compression.ts
@britv3.0/src/lib/supabase/server.ts
@britv3.0/src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maintenance service and API routes</name>
  <files>britv3.0/src/services/landlord/maintenance-service.ts, britv3.0/src/app/api/properties/[id]/maintenance/route.ts, britv3.0/src/app/api/maintenance/[id]/route.ts</files>
  <action>
1. Create `britv3.0/src/services/landlord/maintenance-service.ts`:
   - `getMaintenanceRequests(supabase, propertyId, filters?: {status?, priority?})`: Query maintenance_requests for property, with optional status/priority filters. Order by created_at DESC.
   - `getMaintenanceRequest(supabase, requestId)`: Get single request with all fields.
   - `createMaintenanceRequest(supabase, propertyId, data, photoFiles?: File[])`: Validate with maintenanceRequestSchema. Insert record. If photos provided (max 3), compress each via compressImage() from lib/image-compression, validate via validateFileType(), upload to Supabase Storage bucket 'maintenance-photos' at path `{propertyId}/{requestId}/{filename}`. Update photo_urls array on the record.
   - `updateMaintenanceRequest(supabase, requestId, data)`: Handle status transitions. Validate state machine: new->acknowledged->assigned->in_progress->resolved->closed. When status='resolved', require resolution_notes and set resolved_at. When assigning provider, set assigned_provider_id, assigned_provider_name, and status='assigned'.
   - `uploadMaintenancePhoto(supabase, propertyId, requestId, file: File)`: Compress, validate type, upload to storage, append URL to photo_urls array.

   State machine validation -- define allowed transitions:
   ```
   new -> acknowledged
   acknowledged -> assigned, in_progress
   assigned -> in_progress
   in_progress -> resolved
   resolved -> closed
   ```
   Reject invalid transitions with descriptive error.

2. Create API routes:
   - `GET /api/properties/[id]/maintenance` -- query params: status, priority. Returns array.
   - `POST /api/properties/[id]/maintenance` -- validates body with maintenanceRequestSchema, creates request. Photos handled separately via client-side upload to storage.
   - `PATCH /api/maintenance/[id]` -- handles status updates, provider assignment, resolution. Validates state transitions.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Maintenance service handles CRUD with state machine validation. API routes validate input and enforce status transitions. Photo upload compresses and validates file types.</done>
</task>

<task type="auto">
  <name>Task 2: Maintenance UI pages and components</name>
  <files>britv3.0/src/components/landlord/MaintenanceList.tsx, britv3.0/src/components/landlord/MaintenanceForm.tsx, britv3.0/src/components/landlord/MaintenanceStatusBadge.tsx, britv3.0/src/components/landlord/ProviderAssignment.tsx, britv3.0/src/app/(protected)/dashboard/landlord/properties/[id]/maintenance/page.tsx, britv3.0/src/app/(protected)/dashboard/landlord/properties/[id]/maintenance/new/page.tsx, britv3.0/src/app/(protected)/dashboard/landlord/properties/[id]/maintenance/[requestId]/page.tsx, britv3.0/src/__tests__/landlord/maintenance.test.ts, britv3.0/src/__tests__/landlord/provider-assignment.test.ts</files>
  <action>
1. Create Server Components:
   - `MaintenanceList.tsx`: Receives requests array. Renders list with title, priority badge, status badge, created date. Filter controls for status (dropdown) and priority (dropdown). Empty state: "No maintenance requests".
   - `MaintenanceStatusBadge.tsx`: Colored badge per status -- new=blue, acknowledged=purple, assigned=orange, in_progress=yellow, resolved=green, closed=gray.

2. Create Client Components:
   - `MaintenanceForm.tsx` ("use client"): React Hook Form + Zod. Fields: title (text, max 200 chars with counter), description (textarea, max 2000 chars with counter), priority (select: low/medium/high/emergency). Photo upload section: up to 3 photos, each max 1MB. Show file preview thumbnails. On file select: validate type via validateFileType(), compress via compressImage(), show compressed preview. On submit: POST to /api/properties/[id]/maintenance, then upload photos to Supabase Storage via client SDK, then PATCH request to add photo_urls. Use sonner toast for success/error.

   - `ProviderAssignment.tsx` ("use client"): Shows current assignment (provider name if assigned) or "Unassigned". "Find Provider" button that opens `/marketplace/search?category={relevantCategory}` in new tab (per spec: simple URL link, no auto-RFQ). A separate "Assign Manually" form with provider name input and optional provider user ID lookup. On assign: PATCH /api/maintenance/[id] with assigned_provider_id and assigned_provider_name, which also sets status to 'assigned'. Per spec: "No RFQ is auto-created. No marketplace job record is created. No status sync between systems."

3. Create pages:
   - `maintenance/page.tsx`: Server Component. Fetch maintenance requests for property. Render MaintenanceList with filter controls. "New Request" button links to /maintenance/new.
   - `maintenance/new/page.tsx`: Render MaintenanceForm in create mode.
   - `maintenance/[requestId]/page.tsx`: Server Component. Fetch single request. Show all details, photos (as image gallery), status history. Status update buttons (only valid transitions shown). ProviderAssignment component. Resolution form (when transitioning to resolved: textarea for resolution_notes).

4. Write tests:
   - `maintenance.test.ts`: Test MaintenanceForm validation, status badge rendering, state machine transitions (valid/invalid).
   - `provider-assignment.test.ts`: Test ProviderAssignment renders marketplace link, handles assignment update.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test:run src/__tests__/landlord/maintenance.test.ts src/__tests__/landlord/provider-assignment.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Maintenance list page with filters works. Create form validates and handles photo uploads. Status transitions enforced. Provider assignment links to marketplace. Tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm test:run src/__tests__/landlord/ --reporter=verbose` -- maintenance and provider tests pass
- Maintenance form validates title (max 200), description (max 2000), requires priority
- Status transitions follow state machine (invalid transitions rejected)
- Photos compressed client-side before upload
- Provider assignment is a simple marketplace link + manual input (no auto-RFQ)
</verification>

<success_criteria>
- Maintenance CRUD works with photo uploads (compressed, validated)
- Status state machine enforced (new->acknowledged->assigned->in_progress->resolved->closed)
- Contractor assignment via marketplace link and manual input
- No auto-RFQ, no dual-state management with marketplace
- Filterable maintenance list by status and priority
</success_criteria>

<output>
After completion, create `.planning/phases/06-landlord-tools/06-03-SUMMARY.md`
</output>

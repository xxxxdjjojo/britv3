---
phase: 17-service-provider-public-profiles
plan: "06"
type: execute
wave: 4
depends_on: ["17-01"]
files_modified:
  - britv3.0/src/app/(main)/mortgage-brokers/[slug]/page.tsx
  - britv3.0/src/app/(main)/conveyancers/[slug]/page.tsx
  - britv3.0/src/app/(main)/surveyors/[slug]/page.tsx
  - britv3.0/src/components/providers/SpecialistHero.tsx
  - britv3.0/src/components/providers/SpecialistCredentials.tsx
  - britv3.0/src/components/providers/SpecialistSidebar.tsx
autonomous: true
requirements:
  - PROF-12
  - PROF-13
  - PROF-14

must_haves:
  truths:
    - "Mortgage broker profile at /mortgage-brokers/[slug] shows FCA registration number badge, whole-of-market indicator, fee structure, and specialisms"
    - "Conveyancer profile at /conveyancers/[slug] shows SRA/CLC registration badge, quoted fees for standard purchase/sale, and no-sale-no-fee badge"
    - "Surveyor profile at /surveyors/[slug] shows RICS membership badge, survey types offered (RICS Level 1/2/3), turnaround, and coverage area"
    - "All three profiles reuse SpecialistHero (shared hero component) with role-specific credential section via SpecialistCredentials"
    - "All three routes use generateMetadata with provider name and specialist type in title"
    - "Regulatory registration numbers are sourced from accreditations[] JSONB array with type prefix: 'FCA:FRN123456', 'RICS:member', 'SRA:123456'"
  artifacts:
    - path: "britv3.0/src/components/providers/SpecialistHero.tsx"
      provides: "Shared hero for mortgage broker, conveyancer, and surveyor profiles"
      exports: ["SpecialistHero"]
    - path: "britv3.0/src/components/providers/SpecialistCredentials.tsx"
      provides: "Role-specific credential section component"
      exports: ["MortgageBrokerCredentials", "ConveyancerCredentials", "SurveyorCredentials"]
    - path: "britv3.0/src/app/(main)/mortgage-brokers/[slug]/page.tsx"
      provides: "Mortgage broker SSR profile page"
    - path: "britv3.0/src/app/(main)/conveyancers/[slug]/page.tsx"
      provides: "Conveyancer SSR profile page"
    - path: "britv3.0/src/app/(main)/surveyors/[slug]/page.tsx"
      provides: "Surveyor SSR profile page"
  key_links:
    - from: "britv3.0/src/components/providers/SpecialistCredentials.tsx"
      to: "britv3.0/src/types/providers.ts"
      via: "ServiceProviderPublicProfile.accreditations[]"
      pattern: "accreditations"
---

<objective>
Build the three specialist professional profiles: Mortgage Broker (13.12), Conveyancer/Solicitor (13.13), and Surveyor (13.14). All three share a SpecialistHero component and use a single SpecialistCredentials component that renders role-specific sections. The pages are at /mortgage-brokers/[slug], /conveyancers/[slug], and /surveyors/[slug]. Data comes from the same service_provider_details table using category filtering.

Purpose: These three routes serve FCA-regulated and RICS-accredited professionals whose profiles must prominently display regulatory registration to build trust.

Output: 3 page files + 3 shared component files.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-service-provider-public-profiles/17-CONTEXT.md
@.planning/phases/17-service-provider-public-profiles/17-RESEARCH.md
@.planning/phases/17-service-provider-public-profiles/17-01-SUMMARY.md

<specialist_designs>
From 17-CONTEXT.md:

Mortgage Broker (13.12):
- FCA registration number + badge (navy blue BadgeCheck icon)
- Whole-of-market or tied indicator
- Lender panel count (e.g., "57 lenders")
- Specialisms: First-Time Buyer | Remortgage | BTL | Commercial
- Fee structure: "Fee-free" (green badge) or "Fixed fee: £XXX" (blue badge)
- Reviews section
- "Get Free Advice" CTA

Conveyancer/Solicitor (13.13):
- SRA/CLC registration + badge (blue Scale icon)
- Quoted fees for standard purchase, sale, remortgage
- Turnaround time (e.g., "8-12 weeks")
- No-sale-no-fee badge (green)
- Reviews
- "Get a Quote" CTA

Surveyor (13.14):
- RICS membership badge (forest green Building2 icon)
- Survey types: RICS Level 1 / Level 2 (HomeBuyer) / Level 3 (Full Building)
- Turnaround: "5-10 working days"
- Coverage area postcode list
- Reviews
- "Book Survey" CTA

Accreditations parsing convention:
- "FCA:FRN123456" → FCA regulation, FRN123456
- "RICS:member" → RICS membership
- "SRA:123456" → SRA regulation
- "CLC:789" → CLC regulation
- "gas_safe:558291" → Gas Safe, reg 558291
</specialist_designs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SpecialistHero + SpecialistCredentials shared components</name>
  <files>
    britv3.0/src/components/providers/SpecialistHero.tsx,
    britv3.0/src/components/providers/SpecialistCredentials.tsx,
    britv3.0/src/components/providers/SpecialistSidebar.tsx
  </files>
  <action>
    **britv3.0/src/components/providers/SpecialistHero.tsx** — Server Component. Props: `Readonly<{ provider: ServiceProviderPublicProfile; specialistType: "mortgage_broker" | "conveyancer" | "surveyor" }>`.

    Layout (simpler than tradesperson — no cover photo, more professional/corporate feel):
    - Outer: `bg-white dark:bg-slate-900 rounded-xl border border-slate-200 dark:border-slate-800 p-8 mb-8`
    - Flex row: Avatar (80px, rounded-full, with ShieldCheck overlay) | Identity block
    - Avatar: rounded-full with role-specific ring color: mortgage_broker → bg-blue-800/10 ring-blue-800, conveyancer → bg-blue-700/10 ring-blue-700, surveyor → bg-[#1B4D3E]/10 ring-[#1B4D3E]
    - Identity: H1 business_name (text-3xl font-bold) | p professional category label (text-lg text-slate-600) | location pill | rating display | years_in_business badge
    - Primary regulatory badge: rendered inline in identity block via parseRegBadge(accreditations, specialistType) helper:
      - mortgage_broker: look for "FCA:" prefix → `<span className="inline-flex items-center gap-1.5 px-3 py-1 bg-blue-800 text-white text-xs font-bold rounded-full"><BadgeCheck className="w-3 h-3" /> FCA Authorised · FRN {number}</span>`
      - conveyancer: look for "SRA:" or "CLC:" → similar badge in blue-700
      - surveyor: look for "RICS:" → badge in [#1B4D3E]
    - TrustBadges row below identity (reuse TrustBadges component)
    - CTA buttons row: "Get Free Advice" / "Get a Quote" / "Book Survey" (bg-[#2563EB]) + "Call" (outline) + "Message" (ghost)

    Create helper function in file: `parseRegBadge(accreditations: { type: string; number?: string }[], specialistType: string): { regType: string; number: string } | null` — looks for the matching prefix in accreditations.type field.

    **britv3.0/src/components/providers/SpecialistCredentials.tsx** — Server Component. Three named exports:

    `MortgageBrokerCredentials({ provider: ServiceProviderPublicProfile })`:
    - Section heading "Professional Credentials"
    - 2-col credential grid:
      - FCA authorisation status card: `bg-blue-50 dark:bg-blue-900/20 border-blue-200 p-4 rounded-xl` — "FCA Authorised" badge + FRN number from accreditations
      - Whole-of-market card: "Whole of Market" (green) or "Tied Adviser" (amber) based on a field in the pricing JSONB or service description — parse provider.services for "whole_of_market" keyword, else show "Panel"
      - Fee structure card: parse pricing JSONB for fee_type: "fee_free" → `<Badge className="bg-green-50 text-green-700">Fee-Free</Badge>` | "fixed_fee" with amount → `<Badge className="bg-blue-50 text-blue-700">Fixed Fee: £{amount}</Badge>`
      - Lender count: if lender_count in pricing JSONB: "{N} lenders on panel"
    - Specialisms chips row: map provider.qualifications array to pill badges

    `ConveyancerCredentials({ provider: ServiceProviderPublicProfile })`:
    - SRA/CLC registration card (blue Scale icon, reg number from accreditations)
    - Fee table: standard 3-row table — "Standard Purchase", "Standard Sale", "Remortgage" — prices from pricing JSONB keys purchase_fee, sale_fee, remortgage_fee (show "POA" if not set)
    - Turnaround time badge: from pricing JSONB turnaround_weeks key — "{N}-{M} weeks typical"
    - No-sale-no-fee badge: `bg-green-50 text-green-700 border-green-200` — if pricing.no_sale_no_fee === true

    `SurveyorCredentials({ provider: ServiceProviderPublicProfile })`:
    - RICS membership badge (green Building2 icon, membership number from accreditations)
    - Survey types offered grid: map provider.services array looking for "rics_level_1", "rics_level_2", "rics_level_3", "homebuyer_report", "full_building_survey" — each shown as a card:
      - RICS Level 1 ("Condition Report") — most basic
      - RICS Level 2 ("HomeBuyer Report") — most popular
      - RICS Level 3 ("Full Building Survey") — most detailed
      - Include price range from pricing JSONB if available
    - Turnaround: `<Clock className="w-4 h-4" /> {pricing.turnaround_days ?? "5–10"} working days`
    - Coverage postcodes: `flex-wrap gap-1` of `bg-slate-100 text-slate-600 text-xs rounded px-2 py-1` pills for provider.service_postcodes.slice(0, 8) + "+{N} more" if longer

    **britv3.0/src/components/providers/SpecialistSidebar.tsx** — Server Component. Props: `Readonly<{ provider: ServiceProviderPublicProfile; ctaLabel: string; ctaHref: string }>`.

    Simpler sidebar than tradesperson:
    - CTA card: `bg-[#1B4D3E] text-white rounded-xl p-6` — heading "Ready to get started?", body text "{provider.business_name} typically responds within {response_time_hours ?? 24}h", CTA button (`<a href={ctaHref}` or open QuoteModal)
    - Contact info card: phone, email from profiles join
    - Britestate protection blurb (same as tradesperson sidebar)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^Error|error TS" | grep -v node_modules | head -10</automated>
  </verify>
  <done>3 component files created. No TypeScript errors. pnpm build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Three specialist profile pages (mortgage-brokers, conveyancers, surveyors)</name>
  <files>
    britv3.0/src/app/(main)/mortgage-brokers/[slug]/page.tsx,
    britv3.0/src/app/(main)/conveyancers/[slug]/page.tsx,
    britv3.0/src/app/(main)/surveyors/[slug]/page.tsx
  </files>
  <action>
    All three pages follow the same pattern — Server Component with generateMetadata, SSR data fetch, specialist hero + credentials + reviews.

    **britv3.0/src/app/(main)/mortgage-brokers/[slug]/page.tsx**:
    - Fetch: `fetchProviderBySlug(slug)` — category filter: provider.services includes "mortgage_broker"
    - If not found or wrong category: notFound()
    - Parallel fetch: `[provider, reviews, services]` via Promise.all
    - generateMetadata: `"${business_name} | Mortgage Broker | Britestate"`
    - JSON-LD: use buildProviderJsonLd(provider, "mortgage-brokers") with @type "FinancialService"
    - Layout:
      ```tsx
      <div className="min-h-screen bg-slate-50 dark:bg-slate-950">
        <main className="max-w-7xl mx-auto px-6 py-8">
          <SpecialistHero provider={provider} specialistType="mortgage_broker" />
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div className="lg:col-span-2 space-y-8">
              <MortgageBrokerCredentials provider={provider} />
              <ReviewsTab reviews={reviews.reviews} total={reviews.total} providerName={provider.business_name} />
            </div>
            <aside>
              <SpecialistSidebar provider={provider} ctaLabel="Get Free Advice" ctaHref={`mailto:${provider.profiles.email}`} />
            </aside>
          </div>
        </main>
      </div>
      ```

    **britv3.0/src/app/(main)/conveyancers/[slug]/page.tsx**:
    - Same structure. Category filter: services includes "conveyancing"
    - generateMetadata: `"${business_name} | Conveyancer | Britestate"`
    - JSON-LD @type: "LegalService"
    - Replace MortgageBrokerCredentials with ConveyancerCredentials
    - ctaLabel: "Get a Quote"

    **britv3.0/src/app/(main)/surveyors/[slug]/page.tsx**:
    - Same structure. Category filter: services includes "surveying"
    - generateMetadata: `"${business_name} | Surveyor | Britestate"`
    - JSON-LD @type: "ProfessionalService"
    - Replace with SurveyorCredentials
    - ctaLabel: "Book Survey"

    Note on category validation: `fetchProviderBySlug` returns a provider regardless of category. Each specialist page should validate the provider's services array includes the correct category key. If not, call `notFound()`. This prevents a tradesperson from accidentally loading on a mortgage-broker route.

    Run `pnpm test --run` to confirm Wave 0 stubs still pass.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test --run && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>3 page files created. All 3 routes appear in build output. pnpm test --run exits 0. pnpm build exits 0.</done>
</task>

</tasks>

<verification>
1. /mortgage-brokers/[slug] renders FCA Authorised badge with FRN number
2. /conveyancers/[slug] renders SRA/CLC badge and fee table
3. /surveyors/[slug] renders RICS badge and survey types grid
4. All 3 pages have generateMetadata with correct specialist type in title
5. SpecialistHero renders for all 3 types with appropriate ring/badge colors
6. Category validation: wrong-category slug returns 404
7. pnpm test --run exits 0
8. pnpm build exits 0
</verification>

<success_criteria>
- 6 files created (3 pages + 3 components)
- All 3 specialist routes work with real Supabase data
- Regulatory badges render from accreditations[] array
- pnpm test --run + pnpm build both exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-service-provider-public-profiles/17-06-SUMMARY.md`
</output>

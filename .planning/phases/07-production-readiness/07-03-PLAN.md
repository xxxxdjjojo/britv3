---
phase: 07-production-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - britv3.0/supabase/migrations/010_admin.sql
  - britv3.0/src/app/(admin)/layout.tsx
  - britv3.0/src/app/(admin)/admin/page.tsx
  - britv3.0/src/components/admin/AdminSidebar.tsx
  - britv3.0/src/components/admin/CountCard.tsx
  - britv3.0/src/services/admin-service.ts
  - britv3.0/src/middleware.ts
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-10]

must_haves:
  truths:
    - "Admin route group has its own layout with sidebar navigation"
    - "Non-admin users are redirected away from /admin routes"
    - "Admin dashboard shows 5 count cards (users, listings, providers, reviews, reports)"
    - "Admin sidebar includes external links to Stripe, Sentry, PostHog, Supabase dashboards"
  artifacts:
    - path: "britv3.0/supabase/migrations/010_admin.sql"
      provides: "is_admin column on profiles, content_reports table, push_subscriptions table"
    - path: "britv3.0/src/app/(admin)/layout.tsx"
      provides: "Admin layout with is_admin guard and sidebar"
    - path: "britv3.0/src/app/(admin)/admin/page.tsx"
      provides: "Admin dashboard with count cards"
    - path: "britv3.0/src/services/admin-service.ts"
      provides: "Admin query functions for counts and management"
      exports: ["getAdminCounts"]
  key_links:
    - from: "britv3.0/src/middleware.ts"
      to: "britv3.0/supabase/migrations/010_admin.sql"
      via: "is_admin check on profile for /admin routes"
      pattern: "is_admin"
    - from: "britv3.0/src/app/(admin)/admin/page.tsx"
      to: "britv3.0/src/services/admin-service.ts"
      via: "Server component calling getAdminCounts"
      pattern: "getAdminCounts"
---

<objective>
Create the admin panel foundation: database schema (is_admin, content_reports, push_subscriptions), admin route group with sidebar layout, middleware guard, and admin dashboard with count cards.

Purpose: Establish admin infrastructure that subsequent plans build upon for user management and moderation.
Output: Protected admin panel with dashboard overview and sidebar navigation.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-production-readiness/07-RESEARCH.md

@britv3.0/src/middleware.ts
@britv3.0/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin database migration and middleware guard</name>
  <files>britv3.0/supabase/migrations/010_admin.sql, britv3.0/src/middleware.ts</files>
  <action>
1. Create `supabase/migrations/010_admin.sql`:
   - ALTER TABLE profiles ADD COLUMN is_admin BOOLEAN DEFAULT FALSE
   - Create `push_subscriptions` table: id UUID PK, user_id UUID FK to auth.users ON DELETE CASCADE, endpoint TEXT NOT NULL, keys JSONB NOT NULL (p256dh, auth), user_agent TEXT, created_at TIMESTAMPTZ DEFAULT NOW(), UNIQUE(user_id, endpoint). Enable RLS with policy "Users manage own subscriptions" FOR ALL TO authenticated USING (user_id = auth.uid())
   - Create `content_reports` table: id UUID PK, reporter_id UUID FK to auth.users ON DELETE CASCADE, entity_type TEXT NOT NULL CHECK IN ('listing', 'review'), entity_id UUID NOT NULL, reason TEXT NOT NULL CHECK LENGTH <= 500, status TEXT DEFAULT 'open' CHECK IN ('open', 'resolved', 'dismissed'), resolved_by UUID FK to auth.users, resolution_note TEXT, created_at TIMESTAMPTZ DEFAULT NOW(), resolved_at TIMESTAMPTZ, UNIQUE(reporter_id, entity_type, entity_id). Create index on (status, entity_type) WHERE status = 'open'. Enable RLS with policies: authenticated users can INSERT own reports, admins (via service_role) can SELECT/UPDATE all
   - Create `listing_moderation` table: id UUID PK, listing_id UUID NOT NULL, flags JSONB DEFAULT '[]', status TEXT DEFAULT 'pending' CHECK IN ('pending', 'approved', 'rejected'), reviewed_by UUID FK, reviewed_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW(). Enable RLS -- admin access via service_role only

2. Update `src/middleware.ts`:
   - After existing auth checks, add admin route detection: `const isAdminRoute = pathname.startsWith("/admin")`
   - If admin route and user is authenticated, query profiles table for is_admin flag using the supabase client already created in middleware
   - If `!profile?.is_admin`, redirect to "/" with NextResponse.redirect
   - Keep all existing middleware logic intact (CSP, auth routes, protected routes)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Migration file exists with is_admin column and support tables, middleware blocks non-admin users from /admin routes</done>
</task>

<task type="auto">
  <name>Task 2: Admin layout, sidebar, dashboard page with count cards</name>
  <files>britv3.0/src/app/(admin)/layout.tsx, britv3.0/src/app/(admin)/admin/page.tsx, britv3.0/src/components/admin/AdminSidebar.tsx, britv3.0/src/components/admin/CountCard.tsx, britv3.0/src/services/admin-service.ts</files>
  <action>
1. Create `src/services/admin-service.ts`:
   - Export `getAdminCounts(supabase)` that returns counts for: total users (profiles), active listings (properties where status='active'), pending verifications (profiles where verification_status='pending'), pending reports (content_reports where status='open'), total reviews (provider_reviews)
   - Use `.select('id', { count: 'exact', head: true })` for efficient counting
   - Wrap each count query in try/catch returning 0 if table doesn't exist yet (safe query pattern from Phase 3)

2. Create `src/components/admin/CountCard.tsx`:
   - Props: title (string), count (number), href (string), icon (Lucide icon name string)
   - Render a card with Lucide icon, count number (large), title, and Link wrapping the card
   - Style with Tailwind: bg-white rounded-lg shadow-sm border p-6, hover:shadow-md transition

3. Create `src/components/admin/AdminSidebar.tsx`:
   - Navigation items: Dashboard (/admin), Users (/admin/users), Moderation (/admin/moderation), Verifications (/admin/verifications), Reviews (/admin/reviews)
   - External links section (ADM-10): Supabase Dashboard, Sentry Dashboard, PostHog Dashboard, Stripe Dashboard -- each opens in new tab with ExternalLink icon
   - Use pathname highlighting for active item via `usePathname()`
   - Style: w-64 bg-white border-r min-h-screen, fixed sidebar

4. Create `src/app/(admin)/layout.tsx`:
   - Server component that calls `getUser()` to verify authentication (redundant with middleware but defense-in-depth)
   - Render AdminSidebar on left + main content area on right with padding
   - Add metadata: title "Admin - Britestate"

5. Create `src/app/(admin)/admin/page.tsx`:
   - Server component that creates supabase client and calls `getAdminCounts()`
   - Render 5 CountCards in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5)
   - Cards: Total Users -> /admin/users, Active Listings -> /admin/moderation, Pending Verifications -> /admin/verifications, Open Reports -> /admin/moderation, Total Reviews -> /admin/reviews
   - Page title "Admin Dashboard" with description text
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Admin panel accessible at /admin with sidebar navigation, 5 count cards linking to management sections, external dashboard links present</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- Admin layout renders sidebar and content area
- CountCard component renders count, title, and icon
- Middleware blocks non-admin users (manual test with different profiles)
</verification>

<success_criteria>
- Admin route group `(admin)/` exists with sidebar layout
- is_admin column added to profiles via migration
- Admin dashboard shows 5 count cards with links
- Non-admin users redirected from /admin routes
- External dashboard links in sidebar for Stripe, Sentry, PostHog, Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness/07-03-SUMMARY.md`
</output>

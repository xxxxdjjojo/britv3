---
phase: 07-production-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - britv3.0/src/instrumentation.ts
  - britv3.0/src/instrumentation-client.ts
  - britv3.0/sentry.server.config.ts
  - britv3.0/sentry.edge.config.ts
  - britv3.0/src/lib/posthog.ts
  - britv3.0/src/components/providers/PostHogProvider.tsx
  - britv3.0/src/lib/logger.ts
  - britv3.0/src/lib/logger.test.ts
  - britv3.0/src/lib/features.ts
  - britv3.0/src/lib/features.test.ts
  - britv3.0/.github/workflows/migrate.yml
  - britv3.0/src/middleware.ts
  - britv3.0/src/app/layout.tsx
autonomous: true
requirements: [LCH-01, LCH-02, LCH-03, LCH-04, LCH-05]

must_haves:
  truths:
    - "Client and server errors are captured by Sentry"
    - "Page views and custom events are tracked by PostHog"
    - "Structured JSON logs output to stdout for Vercel"
    - "Feature flags toggle features via environment variables"
    - "Supabase migrations auto-deploy on push to main"
  artifacts:
    - path: "britv3.0/src/lib/logger.ts"
      provides: "Structured JSON logger with info/warn/error levels"
      exports: ["log"]
    - path: "britv3.0/src/lib/features.ts"
      provides: "Feature flag reader from NEXT_PUBLIC_ENABLE_* env vars"
      exports: ["isFeatureEnabled", "features"]
    - path: "britv3.0/.github/workflows/migrate.yml"
      provides: "GitHub Action for automated Supabase migration"
    - path: "britv3.0/src/lib/posthog.ts"
      provides: "PostHog initialization utility"
      exports: ["initPostHog"]
  key_links:
    - from: "britv3.0/src/app/layout.tsx"
      to: "britv3.0/src/components/providers/PostHogProvider.tsx"
      via: "Provider wrapping app children"
      pattern: "PostHogProvider"
    - from: "britv3.0/src/middleware.ts"
      to: "Sentry/PostHog hosts"
      via: "CSP connect-src additions"
      pattern: "sentry\\.io|posthog"
---

<objective>
Integrate Sentry error tracking, PostHog analytics, structured logging, feature flags, and CI/CD migration pipeline.

Purpose: Production observability and deployment automation on free tiers.
Output: Working monitoring, logging, feature flags, and migration CI.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-production-readiness/07-RESEARCH.md

@britv3.0/src/middleware.ts
@britv3.0/src/app/layout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structured logger, feature flags, and GitHub Actions migration CI</name>
  <files>britv3.0/src/lib/logger.ts, britv3.0/src/lib/logger.test.ts, britv3.0/src/lib/features.ts, britv3.0/src/lib/features.test.ts, britv3.0/.github/workflows/migrate.yml</files>
  <behavior>
    - log("info", "test", { key: "val" }) outputs valid JSON to stdout with level, message, timestamp, key
    - log("error", "oops") outputs to stderr
    - log entries always include ISO timestamp
    - isFeatureEnabled("ai_descriptions") returns true when NEXT_PUBLIC_ENABLE_AI_DESCRIPTIONS=true
    - isFeatureEnabled("nonexistent") returns false
    - features() returns object of all feature flags with boolean values
  </behavior>
  <action>
1. Create `src/lib/logger.ts`:
   - Export `log(level: "info" | "warn" | "error", message: string, context?: Record<string, unknown>)` function
   - Output JSON with: level, message, timestamp (ISO), spread context
   - Use `console.error` for "error" level, `console.log` for others

2. Create `src/lib/logger.test.ts`:
   - Mock console.log and console.error
   - Test each level routes to correct console method
   - Test output is valid JSON with required fields
   - Test context fields are included

3. Create `src/lib/features.ts`:
   - Export `isFeatureEnabled(name: string): boolean` -- reads `NEXT_PUBLIC_ENABLE_{NAME}` env var (uppercase), returns true if "true"
   - Export `features(): Record<string, boolean>` -- returns known feature flags: ai_descriptions, push_notifications, offline_mode
   - Document upgrade path to PostHog feature flags in JSDoc comment

4. Create `src/lib/features.test.ts`:
   - Test isFeatureEnabled with env var set to "true" returns true
   - Test isFeatureEnabled with env var unset returns false
   - Test isFeatureEnabled with env var set to "false" returns false
   - Test features() returns object with known keys

5. Create `.github/workflows/migrate.yml`:
   - Trigger on push to main when `supabase/migrations/**` files change
   - Also support workflow_dispatch
   - Job: checkout, setup supabase CLI via supabase/setup-cli@v1, run `supabase db push --project-ref $SUPABASE_PROJECT_REF`
   - Secrets: SUPABASE_ACCESS_TOKEN, SUPABASE_PROJECT_REF
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test:run -- src/lib/logger.test.ts src/lib/features.test.ts</automated>
  </verify>
  <done>Logger outputs structured JSON, feature flags read env vars, migration workflow file exists</done>
</task>

<task type="auto">
  <name>Task 2: Sentry and PostHog integration with CSP updates</name>
  <files>britv3.0/src/instrumentation.ts, britv3.0/src/instrumentation-client.ts, britv3.0/sentry.server.config.ts, britv3.0/sentry.edge.config.ts, britv3.0/src/lib/posthog.ts, britv3.0/src/components/providers/PostHogProvider.tsx, britv3.0/src/middleware.ts, britv3.0/src/app/layout.tsx</files>
  <action>
1. Install dependencies: `cd britv3.0 && pnpm add @sentry/nextjs posthog-js @posthog/next`

2. Create `sentry.server.config.ts`:
   - Import `@sentry/nextjs` and call `Sentry.init()` with dsn from `process.env.SENTRY_DSN`
   - Set tracesSampleRate to 0.1 (10% sampling for free tier)
   - Guard: early return if no SENTRY_DSN (graceful degradation)

3. Create `sentry.edge.config.ts`:
   - Same as server config but for edge runtime

4. Create/update `src/instrumentation.ts`:
   - Export async `register()` function
   - If `process.env.NEXT_RUNTIME === "nodejs"`, dynamically import sentry.server.config
   - If `process.env.NEXT_RUNTIME === "edge"`, dynamically import sentry.edge.config

5. Create `src/instrumentation-client.ts`:
   - Import and init Sentry for client: `Sentry.init({ dsn, tracesSampleRate: 0.1, replaysSessionSampleRate: 0, replaysOnErrorSampleRate: 1.0 })`
   - Guard: early return if no `process.env.NEXT_PUBLIC_SENTRY_DSN`

6. Create `src/lib/posthog.ts`:
   - Export `initPostHog()` function
   - Guard: return early if `typeof window === "undefined"` or no `NEXT_PUBLIC_POSTHOG_KEY`
   - Call `posthog.init(key, { api_host, person_profiles: "identified_only", capture_pageview: false })`

7. Create `src/components/providers/PostHogProvider.tsx` ("use client"):
   - Use `@posthog/next` PostHogProvider or create custom provider
   - Init PostHog on mount via useEffect calling initPostHog()
   - Track pageviews on pathname change using `usePathname()` + `posthog.capture('$pageview')`
   - Wrap children in the provider

8. Update `src/app/layout.tsx`:
   - Import PostHogProvider with dynamic import
   - Wrap children with PostHogProvider

9. Update `src/middleware.ts` `buildCsp` function:
   - Add `*.ingest.sentry.io` to connect-src
   - Add PostHog host (us.i.posthog.com or configurable) to connect-src and script-src
   - Add `worker-src 'self' blob:` for Sentry source map processing
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Sentry captures errors (when DSN configured), PostHog tracks pageviews (when key configured), CSP allows both services, build succeeds</done>
</task>

</tasks>

<verification>
- `pnpm test:run` passes for logger and features tests
- `pnpm build` succeeds with Sentry and PostHog integration
- `.github/workflows/migrate.yml` is valid YAML
- CSP middleware includes Sentry and PostHog hosts
</verification>

<success_criteria>
- Structured logger outputs JSON with level, message, timestamp
- Feature flags read NEXT_PUBLIC_ENABLE_* env vars correctly
- Sentry initializes on both client and server (gracefully skips if no DSN)
- PostHog tracks pageviews on route changes (gracefully skips if no key)
- GitHub Action deploys migrations on push to main
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness/07-02-SUMMARY.md`
</output>

---
phase: 07-production-readiness
plan: 06
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - britv3.0/src/app/(main)/help/page.tsx
  - britv3.0/src/app/(main)/contact/page.tsx
  - britv3.0/src/app/api/contact/route.ts
  - britv3.0/src/app/api/contact/route.test.ts
  - britv3.0/src/content/help-faq.ts
  - britv3.0/src/components/mobile/BottomTabBar.tsx
  - britv3.0/src/app/(protected)/layout.tsx
autonomous: true
requirements: [ADM-08, ADM-09, MOB-10]

must_haves:
  truths:
    - "Help page displays FAQ sections with expandable answers"
    - "Contact form validates input and sends email via Resend with rate limiting"
    - "Role-specific bottom tab bar shows on mobile (<768px) in protected routes"
  artifacts:
    - path: "britv3.0/src/app/(main)/help/page.tsx"
      provides: "Static help page with FAQ accordion"
    - path: "britv3.0/src/app/api/contact/route.ts"
      provides: "Contact form POST endpoint with rate limiting and Resend email"
      exports: ["POST"]
    - path: "britv3.0/src/content/help-faq.ts"
      provides: "FAQ data as TypeScript arrays (not MDX)"
      exports: ["FAQ_SECTIONS"]
    - path: "britv3.0/src/components/mobile/BottomTabBar.tsx"
      provides: "Role-specific bottom navigation for mobile"
  key_links:
    - from: "britv3.0/src/app/(main)/contact/page.tsx"
      to: "britv3.0/src/app/api/contact/route.ts"
      via: "form submit to /api/contact"
      pattern: "api/contact"
    - from: "britv3.0/src/app/(protected)/layout.tsx"
      to: "britv3.0/src/components/mobile/BottomTabBar.tsx"
      via: "rendered at bottom of protected layout on mobile"
      pattern: "BottomTabBar"
---

<objective>
Create help page with FAQ, contact form with rate-limited email delivery, and role-specific mobile bottom tab bar.

Purpose: User support pages and mobile navigation polish.
Output: Help page, contact form, and mobile bottom nav bar.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-production-readiness/07-RESEARCH.md

@britv3.0/src/app/(protected)/layout.tsx
@britv3.0/src/app/(main)/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Help page with FAQ and contact form with rate-limited email</name>
  <files>britv3.0/src/content/help-faq.ts, britv3.0/src/app/(main)/help/page.tsx, britv3.0/src/app/(main)/contact/page.tsx, britv3.0/src/app/api/contact/route.ts, britv3.0/src/app/api/contact/route.test.ts</files>
  <behavior>
    - POST /api/contact with valid {name, email, subject, message} returns 200
    - POST /api/contact with missing fields returns 400 with validation errors
    - POST /api/contact with invalid email returns 400
    - POST /api/contact rate limited to 3 requests per hour per IP
  </behavior>
  <action>
1. Create `src/content/help-faq.ts` (per research recommendation: TypeScript arrays, not MDX):
   - Export `FAQ_SECTIONS: { title: string; items: { question: string; answer: string }[] }[]`
   - Sections: Account & Registration, Property Search, Service Providers, Messaging, Landlord Tools, Payments & Billing
   - 3-5 Q&A pairs per section covering common user questions

2. Create `src/app/(main)/help/page.tsx`:
   - Server component with static metadata (title: "Help Center")
   - Import FAQ_SECTIONS and render each section with Shadcn Accordion component
   - Each section has a heading, each Q&A is an AccordionItem with question as trigger, answer as content
   - Link to contact page at bottom: "Can't find what you're looking for? Contact us"
   - Style: max-w-3xl mx-auto, clean spacing

3. Create `src/app/api/contact/route.ts`:
   - POST handler: validate body with Zod schema { name: string min 2, email: string.email, subject: string min 5, message: string min 20 max 2000 }
   - Rate limit: use Upstash Ratelimit (already in stack) with 3 requests per hour per IP (`request.headers.get("x-forwarded-for")` or "unknown")
   - On rate limit exceeded, return 429 with { error: "Too many requests" }
   - Send email via Resend (already in stack) to `process.env.SUPPORT_EMAIL` with formatted subject and body
   - Graceful degradation: if Resend env vars missing, log the contact submission and return 200
   - Return 200 with { success: true }

4. Create `src/app/api/contact/route.test.ts`:
   - Mock Resend and Upstash Ratelimit
   - Test valid submission returns 200
   - Test missing fields returns 400
   - Test invalid email returns 400
   - Test rate limiting returns 429

5. Create `src/app/(main)/contact/page.tsx` ("use client" for form interaction):
   - Form with fields: Name, Email, Subject, Message (textarea)
   - Client-side validation matching the Zod schema
   - Submit to /api/contact via fetch
   - Show success message on 200, error message on 4xx
   - Rate limit message on 429
   - Style: max-w-lg mx-auto, Shadcn Input/Textarea/Button components
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test:run -- src/app/api/contact/route.test.ts</automated>
  </verify>
  <done>Help page shows FAQ with accordion, contact form validates and sends email with rate limiting</done>
</task>

<task type="auto">
  <name>Task 2: Role-specific mobile bottom tab bar</name>
  <files>britv3.0/src/components/mobile/BottomTabBar.tsx, britv3.0/src/app/(protected)/layout.tsx</files>
  <action>
1. Create `src/components/mobile/BottomTabBar.tsx` ("use client"):
   - Import useAuth (or useRole) to get active user role
   - Import Lucide icons dynamically by name (matching existing ROLES constant pattern)
   - Define `TAB_CONFIG: Record<UserRole, { label: string; href: string; icon: string }[]>` with 5 tabs per role:
     - homebuyer: Search, Saved, Viewings, Messages, Profile
     - renter: Search, Saved, Applications, Messages, Profile
     - seller: Listings, Viewings, Offers, Messages, Profile
     - landlord: Portfolio, Tenants, Maintenance, Messages, Profile
     - agent: Listings, Leads, Viewings, Messages, Profile
     - provider: Jobs, Quotes, Calendar, Messages, Profile
   - Render a fixed bottom bar (md:hidden to hide on desktop):
     - Style: fixed bottom-0 left-0 right-0, h-16, bg-white border-t, flex justify-around items-center, z-40
     - Each tab: icon (20px) + label (text-xs), active state with brand color
     - Use `usePathname()` for active tab highlighting
   - Ensure safe area for iOS notch: add `pb-safe` (safe-area-inset-bottom via Tailwind)

2. Update `src/app/(protected)/layout.tsx`:
   - Import BottomTabBar with dynamic import (ssr: false)
   - Render BottomTabBar at the bottom of the layout, after children
   - Add bottom padding to main content area (pb-16 on mobile) so content isn't hidden behind tab bar
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Bottom tab bar shows on mobile with role-appropriate navigation items, hidden on desktop, active tab highlighted</done>
</task>

</tasks>

<verification>
- Contact form test passes
- Build succeeds
- Help page renders FAQ sections
- Bottom tab bar renders on mobile viewport
</verification>

<success_criteria>
- Help page shows expandable FAQ sections grouped by category
- Contact form validates input and sends email via Resend
- Contact form rate limited to 3/hour per IP
- Bottom tab bar shows 5 role-specific tabs on mobile (<768px)
- Bottom tab bar hidden on desktop (md:hidden)
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness/07-06-SUMMARY.md`
</output>

---
phase: 07-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - britv3.0/src/app/manifest.ts
  - britv3.0/src/app/sw.ts
  - britv3.0/next.config.ts
  - britv3.0/public/icons/icon-192.png
  - britv3.0/public/icons/icon-512.png
  - britv3.0/public/icons/icon-maskable.png
  - britv3.0/public/badge.png
  - britv3.0/src/components/pwa/InstallPrompt.tsx
  - britv3.0/src/components/pwa/OfflineIndicator.tsx
  - britv3.0/src/app/manifest.test.ts
autonomous: true
requirements: [MOB-01, MOB-02, MOB-07]

must_haves:
  truths:
    - "App is installable as PWA with standalone display mode"
    - "Deferred install prompt appears after 2nd visit"
    - "Static pages are cached with stale-while-revalidate strategy"
  artifacts:
    - path: "britv3.0/src/app/manifest.ts"
      provides: "Web App Manifest with icons, name, theme color"
      exports: ["default"]
    - path: "britv3.0/src/app/sw.ts"
      provides: "Service worker with Serwist precaching and runtime caching"
    - path: "britv3.0/src/components/pwa/InstallPrompt.tsx"
      provides: "Deferred install prompt component with visit counter"
    - path: "britv3.0/src/components/pwa/OfflineIndicator.tsx"
      provides: "Visual indicator when user is offline"
  key_links:
    - from: "britv3.0/next.config.ts"
      to: "britv3.0/src/app/sw.ts"
      via: "@serwist/next withSerwist wrapper"
      pattern: "withSerwistInit"
    - from: "britv3.0/src/app/layout.tsx"
      to: "britv3.0/src/components/pwa/InstallPrompt.tsx"
      via: "component rendered in root layout"
      pattern: "InstallPrompt"
---

<objective>
Set up PWA infrastructure: web app manifest, Serwist service worker with caching strategies, installable standalone mode, deferred install prompt, and offline indicator.

Purpose: Make the app installable on mobile devices with offline-capable static page caching.
Output: Working PWA with manifest, service worker, install prompt, and offline indicator.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-production-readiness/07-RESEARCH.md

@britv3.0/next.config.ts
@britv3.0/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PWA manifest, icons, service worker, and Serwist config</name>
  <files>britv3.0/src/app/manifest.ts, britv3.0/src/app/sw.ts, britv3.0/next.config.ts, britv3.0/public/icons/icon-192.png, britv3.0/public/icons/icon-512.png, britv3.0/public/icons/icon-maskable.png, britv3.0/public/badge.png, britv3.0/src/app/manifest.test.ts</files>
  <action>
1. Install dependencies: `cd britv3.0 && pnpm add serwist @serwist/next @serwist/window`

2. Create `src/app/manifest.ts` using Next.js MetadataRoute.Manifest:
   - name: "Britestate - UK Property Portal", short_name: "Britestate"
   - description: "Find, compare, and transact on UK properties"
   - start_url: "/", display: "standalone"
   - background_color: "#ffffff", theme_color: "#005F73" (from brand)
   - Icons: 192x192, 512x512 (any purpose), 512x512 (maskable purpose)

3. Generate placeholder PWA icons in `public/icons/` (192x192, 512x512, maskable 512x512) and `public/badge.png`. Use simple solid-color PNGs with the brand color via a small Node script or canvas. These are placeholders -- real icons come from design later.

4. Create `src/app/sw.ts` service worker:
   - Import `defaultCache` from `@serwist/next/worker` and `Serwist` from `serwist`
   - Declare global `__SW_MANIFEST` type
   - Create Serwist instance with: precacheEntries from `self.__SW_MANIFEST`, skipWaiting: true, clientsClaim: true, navigationPreload: true, runtimeCaching: defaultCache
   - Add `push` event listener (for Plan 04 push notifications) that calls `showNotification` with title, body, icon from event data
   - Add `notificationclick` listener that navigates to `event.notification.data.url` (deep linking for MOB-04 prep)
   - Call `serwist.addEventListeners()`

5. Update `next.config.ts`:
   - Import `withSerwistInit` from `@serwist/next`
   - Wrap existing config with `withSerwist({ swSrc: "src/app/sw.ts", swDest: "public/sw.js", disable: process.env.NODE_ENV === "development" })`
   - Keep all existing config options intact

6. Create unit test `src/app/manifest.test.ts`:
   - Import manifest function, verify it returns object with name, short_name, start_url, display: "standalone", icons array with 3 entries
   - Verify theme_color and background_color are present

7. Update CSP in `src/middleware.ts`:
   - Add `worker-src 'self'` directive to `buildCsp` function for service worker
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test:run -- src/app/manifest.test.ts</automated>
  </verify>
  <done>Manifest served at /manifest.webmanifest, service worker configured with Serwist, CSP allows worker-src</done>
</task>

<task type="auto">
  <name>Task 2: Deferred install prompt and offline indicator components</name>
  <files>britv3.0/src/components/pwa/InstallPrompt.tsx, britv3.0/src/components/pwa/OfflineIndicator.tsx, britv3.0/src/app/layout.tsx</files>
  <action>
1. Create `src/components/pwa/InstallPrompt.tsx` ("use client"):
   - Listen for `beforeinstallprompt` event, store the event in state, prevent default
   - Track visit count in localStorage key `britestate_visit_count`
   - Only show install UI after 2nd visit (per MOB-02)
   - Render a dismissible bottom banner with "Install Britestate" button
   - On click, call `deferredPrompt.prompt()`, await `userChoice`, hide on accept or dismiss
   - Style with Tailwind: fixed bottom-0 left-0 right-0, z-50, bg-brand, text-white, shadow-lg
   - Hide the banner if app is already in standalone mode (`window.matchMedia('(display-mode: standalone)')`)

2. Create `src/components/pwa/OfflineIndicator.tsx` ("use client"):
   - Use `navigator.onLine` state with `online`/`offline` event listeners
   - When offline, show a small top banner: "You're offline -- some features may be unavailable"
   - Style: fixed top-0, z-50, bg-amber-500 text-white text-sm py-1 text-center
   - Auto-hide when back online

3. Add both components to root `src/app/layout.tsx`:
   - Import InstallPrompt and OfflineIndicator
   - Render them inside the body, after existing children
   - Use dynamic import with `{ ssr: false }` for both (client-only, browser APIs)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Install prompt shows after 2nd visit, offline indicator shows when connection lost, both render in root layout</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with Serwist integration
- Manifest test passes
- `public/sw.js` generated during build (not in dev mode)
- InstallPrompt and OfflineIndicator render without errors
</verification>

<success_criteria>
- Web app manifest is served with correct name, icons, display: standalone
- Service worker is generated by Serwist with precaching and runtime caching
- Install prompt defers until 2nd visit
- Offline indicator appears when connectivity is lost
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness/07-01-SUMMARY.md`
</output>

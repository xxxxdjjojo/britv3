---
phase: 07-production-readiness
plan: 07
type: execute
wave: 3
depends_on: ["07-01", "07-06"]
files_modified:
  - britv3.0/src/app/globals.css
  - britv3.0/src/components/mobile/PullToRefresh.tsx
  - britv3.0/src/app/(protected)/layout.tsx
autonomous: false
requirements: [MOB-08, MOB-09, MOB-11]

must_haves:
  truths:
    - "All interactive elements have minimum 44px touch targets"
    - "Layout is responsive and usable from 320px to 1280px"
    - "Pull-to-refresh works on dashboard pages"
    - "Core Web Vitals meet targets (LCP < 2.5s, FID < 100ms, CLS < 0.1)"
  artifacts:
    - path: "britv3.0/src/app/globals.css"
      provides: "Touch target utilities and responsive fixes"
    - path: "britv3.0/src/components/mobile/PullToRefresh.tsx"
      provides: "Pull-to-refresh component for mobile dashboard pages"
  key_links:
    - from: "britv3.0/src/app/globals.css"
      to: "All interactive components"
      via: "Tailwind utility classes for min touch targets"
      pattern: "min-h-11|min-w-11|touch-target"
---

<objective>
Responsive verification, touch optimization, pull-to-refresh, and Core Web Vitals audit with fixes.

Purpose: Ensure the app is usable and performant on mobile devices across all breakpoints.
Output: Mobile-optimized interactions, responsive fixes, and performance improvements.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-production-readiness/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Touch optimization, pull-to-refresh, and responsive CSS fixes</name>
  <files>britv3.0/src/app/globals.css, britv3.0/src/components/mobile/PullToRefresh.tsx, britv3.0/src/app/(protected)/layout.tsx</files>
  <action>
1. Add touch-target utility to `src/app/globals.css`:
   - Add `.touch-target` class: `min-height: 44px; min-width: 44px;` for WCAG/Apple HIG compliance
   - Add `@supports (padding: env(safe-area-inset-bottom))` block for `.pb-safe { padding-bottom: env(safe-area-inset-bottom); }`
   - Add `touch-action: manipulation` on interactive elements to remove 300ms tap delay
   - Add responsive fixes for 320px viewport: ensure no horizontal overflow, text truncation on long strings

2. Audit and fix existing interactive elements across the app:
   - Search all Button, Link, and clickable elements that may be smaller than 44px
   - Add `min-h-11 min-w-11` (44px with Tailwind default 4px base) to: icon buttons, tab items, nav links, form controls
   - Focus on: BottomTabBar tabs, sidebar nav items, card action buttons, pagination controls, modal close buttons
   - Add `touch-action: manipulation` to forms and interactive areas

3. Create `src/components/mobile/PullToRefresh.tsx` ("use client"):
   - Listens for touchstart/touchmove/touchend events at top of scrollable area
   - Only activates when scroll position is at top (scrollTop === 0)
   - Shows a pull indicator (rotating arrow icon) during pull
   - On release past threshold (60px), call `router.refresh()` to reload server components
   - Add visual feedback: spinner while refreshing
   - Style: absolute positioning, centered, z-50
   - Wrap with CSS `overscroll-behavior-y: contain` to prevent browser pull-to-refresh conflict

4. Add PullToRefresh to protected layout (`src/app/(protected)/layout.tsx`):
   - Import with dynamic import (ssr: false)
   - Wrap children with PullToRefresh wrapper
   - Only render on mobile (use CSS md:hidden or check window width)

5. Core Web Vitals optimization pass:
   - Verify Next.js Image component is used for all images (not raw <img>)
   - Add `priority` prop to above-fold images (hero images, first visible listing images)
   - Ensure fonts use `next/font` with display: swap
   - Check for layout shift causes: add explicit width/height to images, skeleton loaders for async content
   - Add `loading="lazy"` to below-fold images and iframes
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Touch targets are 44px minimum, pull-to-refresh works on mobile dashboards, responsive layout verified, CWV optimization applied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify responsive layout, touch targets, and Core Web Vitals</name>
  <files>britv3.0/src/app/globals.css</files>
  <action>
Human verifies responsive layout, touch optimization, and mobile bottom tab bar across all breakpoints (320px-1280px):
1. Run `cd britv3.0 && npm run dev`
2. Open Chrome DevTools (F12) -> Toggle Device Toolbar
3. Test at 320px (iPhone SE), 375px (iPhone), 768px (iPad), 1024px (desktop), 1280px (wide)
4. Verify at each breakpoint: no horizontal overflow, 44px touch targets, bottom tab bar visibility, text readability
5. Test pull-to-refresh on a dashboard page at mobile viewport
6. Run Lighthouse mobile audit on homepage (Performance tab) -- LCP < 2.5s, CLS < 0.1
  </action>
  <verify>Human approval of responsive behavior and Lighthouse scores</verify>
  <done>Responsive layout verified across breakpoints, touch targets confirmed, CWV targets met</done>
</task>

</tasks>

<verification>
- Build succeeds
- Responsive layout works from 320px to 1280px
- Touch targets meet 44px minimum
- Lighthouse mobile audit shows green CWV metrics
</verification>

<success_criteria>
- All interactive elements have 44px minimum touch targets
- No horizontal overflow at any breakpoint between 320px-1280px
- Pull-to-refresh works on mobile dashboard pages
- Lighthouse mobile score: LCP < 2.5s, FID < 100ms, CLS < 0.1
- Bottom tab bar visible on mobile, hidden on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness/07-07-SUMMARY.md`
</output>

---
phase: 07-production-readiness
plan: 05
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - britv3.0/src/app/(admin)/admin/users/page.tsx
  - britv3.0/src/components/admin/UserTable.tsx
  - britv3.0/src/components/admin/UserDetailModal.tsx
  - britv3.0/src/app/(admin)/admin/moderation/page.tsx
  - britv3.0/src/components/admin/ModerationQueue.tsx
  - britv3.0/src/app/(admin)/admin/verifications/page.tsx
  - britv3.0/src/components/admin/VerificationQueue.tsx
  - britv3.0/src/app/(admin)/admin/reviews/page.tsx
  - britv3.0/src/components/admin/ReviewModerationQueue.tsx
  - britv3.0/src/services/admin-service.ts
  - britv3.0/src/services/moderation-service.ts
  - britv3.0/src/services/moderation-service.test.ts
  - britv3.0/src/lib/profanity.ts
  - britv3.0/src/lib/profanity.test.ts
autonomous: true
requirements: [ADM-04, ADM-05, ADM-06, ADM-07]

must_haves:
  truths:
    - "Admin can search users by name/email and suspend/activate accounts"
    - "Listings are auto-flagged for profanity, price anomalies, and duplicates"
    - "Admin can review and approve/reject provider verification documents"
    - "Admin can review flagged/reported reviews and resolve reports"
  artifacts:
    - path: "britv3.0/src/lib/profanity.ts"
      provides: "Profanity word list filter"
      exports: ["containsProfanity", "findProfanity"]
    - path: "britv3.0/src/services/moderation-service.ts"
      provides: "Listing auto-flagging (profanity, price anomaly, duplicate), review moderation"
      exports: ["flagListing", "detectPriceAnomaly", "detectDuplicate"]
    - path: "britv3.0/src/services/admin-service.ts"
      provides: "Extended admin ops: user search, suspend, verification queue"
      exports: ["searchUsers", "suspendUser", "activateUser", "getVerificationQueue", "reviewVerification"]
  key_links:
    - from: "britv3.0/src/app/(admin)/admin/users/page.tsx"
      to: "britv3.0/src/services/admin-service.ts"
      via: "searchUsers, suspendUser, activateUser"
      pattern: "searchUsers|suspendUser"
    - from: "britv3.0/src/services/moderation-service.ts"
      to: "britv3.0/src/lib/profanity.ts"
      via: "containsProfanity check on listing title/description"
      pattern: "containsProfanity"
---

<objective>
Build admin management pages: user management (search, view, suspend), listing post-moderation with auto-flagging, provider verification queue, and review moderation.

Purpose: Give admins tools to manage users, moderate content, and verify providers.
Output: Four admin management sections with search, filtering, and action capabilities.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-production-readiness/07-RESEARCH.md
@.planning/phases/07-production-readiness/07-03-SUMMARY.md

@britv3.0/src/services/admin-service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profanity filter, moderation service, and admin service extensions</name>
  <files>britv3.0/src/lib/profanity.ts, britv3.0/src/lib/profanity.test.ts, britv3.0/src/services/moderation-service.ts, britv3.0/src/services/moderation-service.test.ts, britv3.0/src/services/admin-service.ts</files>
  <behavior>
    - containsProfanity("this is shit content") returns true
    - containsProfanity("this is clean content") returns false
    - containsProfanity is case-insensitive and handles word boundaries
    - findProfanity("damn this shit") returns ["damn", "shit"]
    - detectPriceAnomaly({ price: 100, type: "house", region: "london" }) flags as anomaly (too low)
    - detectPriceAnomaly({ price: 250000, type: "flat", region: "london" }) returns no anomaly
    - detectDuplicate checks address similarity and returns match score
  </behavior>
  <action>
1. Create `src/lib/profanity.ts`:
   - Export `PROFANITY_LIST: string[]` with ~100-150 common English profanity words (curated list, no external dependency)
   - Export `containsProfanity(text: string): boolean` -- case-insensitive word boundary check against list
   - Export `findProfanity(text: string): string[]` -- returns array of found profane words

2. Create `src/lib/profanity.test.ts`:
   - Test containsProfanity detects profanity
   - Test containsProfanity ignores clean text
   - Test case insensitivity
   - Test word boundaries (e.g., "class" should not flag "ass")

3. Create `src/services/moderation-service.ts`:
   - Export `flagListing(listing: { title: string; description: string; price: number; property_type: string; address: string })`: returns `{ flags: ModerationFlag[] }` where flags include reason, severity, details
   - Export `detectPriceAnomaly(price: number, propertyType: string, region?: string): ModerationFlag | null` -- uses rough UK property price ranges by type (flat: 50K-2M, house: 80K-5M, etc.) -- flags if outside 2x range
   - Export `detectDuplicate(address: string, existingAddresses: string[]): { isDuplicate: boolean; matchedAddress?: string; score: number }` -- Levenshtein-like similarity check on normalized addresses
   - flagListing combines: profanity check on title+description, price anomaly check, returns all flags

4. Create `src/services/moderation-service.test.ts`:
   - Test flagListing with profane title returns profanity flag
   - Test detectPriceAnomaly with unreasonable prices
   - Test detectDuplicate with similar addresses

5. Extend `src/services/admin-service.ts` with:
   - `searchUsers(supabase, query: string, page: number, limit: number)`: ILIKE search on full_name and email from profiles, with `.range()` pagination, returns users with role info
   - `suspendUser(supabase, userId: string)`: Update profiles set is_suspended = true
   - `activateUser(supabase, userId: string)`: Update profiles set is_suspended = false
   - `getVerificationQueue(supabase)`: Query profiles where verification_status = 'pending' with document info
   - `reviewVerification(supabase, userId: string, decision: "approved" | "rejected", notes?: string)`: Update verification status
   - `getReportedReviews(supabase)`: Query content_reports where entity_type = 'review' and status = 'open', join review data
   - `resolveReport(supabase, reportId: string, resolution: "resolved" | "dismissed", note?: string, adminId: string)`: Update content_report status
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm test:run -- src/lib/profanity.test.ts src/services/moderation-service.test.ts</automated>
  </verify>
  <done>Profanity filter catches bad words, moderation service flags price anomalies and duplicates, admin service supports user management and verification</done>
</task>

<task type="auto">
  <name>Task 2: Admin management pages (users, moderation, verifications, reviews)</name>
  <files>britv3.0/src/app/(admin)/admin/users/page.tsx, britv3.0/src/components/admin/UserTable.tsx, britv3.0/src/components/admin/UserDetailModal.tsx, britv3.0/src/app/(admin)/admin/moderation/page.tsx, britv3.0/src/components/admin/ModerationQueue.tsx, britv3.0/src/app/(admin)/admin/verifications/page.tsx, britv3.0/src/components/admin/VerificationQueue.tsx, britv3.0/src/app/(admin)/admin/reviews/page.tsx, britv3.0/src/components/admin/ReviewModerationQueue.tsx</files>
  <action>
1. Create `src/components/admin/UserTable.tsx` ("use client"):
   - Props: users array, onSuspend callback, onActivate callback
   - Render table with columns: Name, Email, Role(s), Status, Created, Actions
   - Actions column: Suspend button (if active), Activate button (if suspended), View details button
   - Search input at top that filters client-side + triggers server search on enter

2. Create `src/components/admin/UserDetailModal.tsx` ("use client"):
   - Props: user object, open boolean, onClose callback
   - Display full user details: name, email, phone, roles, verification status, created date, last login
   - Use Shadcn Dialog/Sheet component

3. Create `src/app/(admin)/admin/users/page.tsx`:
   - Server component that calls searchUsers with default params
   - Render UserTable with search form (form action for server-side search via searchParams)
   - Support pagination via page query param
   - Suspend/activate actions via server actions calling admin-service

4. Create `src/components/admin/ModerationQueue.tsx` ("use client"):
   - Props: listings array with moderation flags
   - Show listing title, flags (profanity, price anomaly, duplicate) with severity badges
   - Actions: Approve (mark as reviewed), Reject (remove listing), View listing link

5. Create `src/app/(admin)/admin/moderation/page.tsx`:
   - Server component querying listing_moderation table joined with properties
   - Pass flagged listings to ModerationQueue component
   - Show "No items to review" when queue is empty

6. Create `src/components/admin/VerificationQueue.tsx` ("use client"):
   - Props: pending verifications array
   - Show provider name, business name, submitted date, document links
   - Document links use Supabase signed URLs (ADM-06) for secure viewing
   - Actions: Approve, Reject with optional notes textarea

7. Create `src/app/(admin)/admin/verifications/page.tsx`:
   - Server component calling getVerificationQueue
   - Render VerificationQueue with approve/reject server actions

8. Create `src/components/admin/ReviewModerationQueue.tsx` ("use client"):
   - Props: reported reviews array
   - Show review content, reporter reason, report count
   - Actions: Remove review, Dismiss report

9. Create `src/app/(admin)/admin/reviews/page.tsx`:
   - Server component calling getReportedReviews
   - Render ReviewModerationQueue with resolve server actions
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>Admin can search/manage users, review flagged listings, process verification queue, and moderate reported reviews</done>
</task>

</tasks>

<verification>
- Profanity and moderation tests pass
- Build succeeds with all admin pages
- All four admin sections render correctly
</verification>

<success_criteria>
- Admin can search users by name/email, view details, suspend/activate
- Listings auto-flagged for profanity, price anomaly, duplicates appear in moderation queue
- Provider verification queue shows pending applications with document viewing via signed URLs
- Reported reviews appear in review moderation queue with resolve/dismiss actions
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness/07-05-SUMMARY.md`
</output>

---
phase: 15-estate-agent-dashboard
plan: 11
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - britv3.0/src/app/(protected)/dashboard/agent/team/page.tsx
  - britv3.0/src/components/dashboard/agent/team/TeamMemberList.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/team/roles/page.tsx
  - britv3.0/src/components/dashboard/agent/team/RolesPermissions.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/team/branches/page.tsx
  - britv3.0/src/components/dashboard/agent/team/BranchManager.tsx
autonomous: true
requirements:
  - AGT-21
  - AGT-22
  - AGT-23

must_haves:
  truths:
    - "Team members can be invited by email and appear with pending status"
    - "Roles (5 tiers) can be assigned with granular permission toggles"
    - "Branches can be created with team members assigned to them"
    - "Performance metrics show per-member lead and viewing counts"
  artifacts:
    - path: "britv3.0/src/components/dashboard/agent/team/TeamMemberList.tsx"
      provides: "Team member grid with invite, role assignment, performance metrics"
    - path: "britv3.0/src/components/dashboard/agent/team/RolesPermissions.tsx"
      provides: "5-tier role editor with permission toggles"
    - path: "britv3.0/src/components/dashboard/agent/team/BranchManager.tsx"
      provides: "Branch CRUD with team member assignment"
  key_links:
    - from: "britv3.0/src/components/dashboard/agent/team/TeamMemberList.tsx"
      to: "britv3.0/src/app/api/agent/team/route.ts"
      via: "POST to invite, PATCH to update role"
      pattern: "fetch.*api/agent/team"
---

<objective>
Build Team Members, Roles & Permissions, and Branch Management pages.

Purpose: These pages let agency managers control their team -- inviting new members, setting roles with granular permissions, and organizing people across multiple branch offices. This is critical for multi-office agencies.

Output: 3 page routes + 3 client components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-estate-agent-dashboard/15-CONTEXT.md
@.planning/phases/15-estate-agent-dashboard/15-04-SUMMARY.md
@britv3.0/src/types/agent.ts
@britv3.0/src/services/agent/agent-team-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Team Members page with invite and performance</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/team/page.tsx, britv3.0/src/components/dashboard/agent/team/TeamMemberList.tsx</files>
  <action>
**team/page.tsx** (Server Component):
- Fetch team members via getTeamMembers(supabase, user.id)
- Pass to TeamMemberList

**TeamMemberList.tsx** ("use client"):
- Grid of team member cards (responsive: 3 cols lg, 2 md, 1 sm)
- Each card: avatar (initials if no photo), name, email, role badge (colour-coded by tier), branch name, status badge (active=green, inactive=grey, pending=yellow)
- Performance metrics per member: leads assigned count, viewings conducted count, deals closed (if available)
- "Invite Team Member" button: Shadcn Dialog with form fields: email, name, role (Select from TEAM_ROLES), branch (Select from branches). Submit calls POST /api/agent/team.
- Per-member actions (dropdown menu): Change Role, Assign to Branch, Remove Member
- Change Role: Shadcn Select dropdown of TEAM_ROLES, on change calls PATCH
- Remove: confirmation dialog, calls DELETE (sets inactive)
- Filter by branch (dropdown), filter by role, filter by status
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/team/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Team member grid renders with invite dialog, role changes, branch assignment, performance metrics</done>
</task>

<task type="auto">
  <name>Task 2: Build Roles & Permissions and Branch Management pages</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/team/roles/page.tsx, britv3.0/src/components/dashboard/agent/team/RolesPermissions.tsx, britv3.0/src/app/(protected)/dashboard/agent/team/branches/page.tsx, britv3.0/src/components/dashboard/agent/team/BranchManager.tsx</files>
  <action>
**Roles & Permissions (roles/page.tsx + RolesPermissions.tsx):**
- Static page showing 5 roles in a table/grid: Admin, Senior Negotiator, Negotiator, Lettings Manager, Viewer
- Each role: description of access level, toggleable permissions matrix
- Permission categories: Listings (view/create/edit/archive), Leads (view/manage/assign), Offers (view/manage/negotiate), Viewings (view/manage), CRM (view/edit), Analytics (view), Team (manage), Billing (view/manage)
- Matrix display: roles as columns, permissions as rows, with Shadcn Switch toggles
- Note: permissions are informational in this phase (enforced via role check in service layer, not a dynamic permissions table). Store custom permission overrides in JSONB on agent_team_members if needed.
- Save button persists any custom overrides

**Branch Management (branches/page.tsx + BranchManager.tsx):**
- Server Component fetches branches via getBranches(supabase, user.id)
- BranchManager ("use client"):
  - List of branch cards: name, address, phone, email, head office badge, team member count
  - "Add Branch" button: Shadcn Dialog with form (name, address fields, phone, email, is_head_office toggle). Submit calls createBranch via API.
  - Edit button per branch: opens pre-filled form dialog
  - Each branch shows assigned team members as avatar row
  - "Assign Member" button per branch: Select dropdown of unassigned team members, on select calls assignMemberToBranch
  - Delete branch: confirmation dialog (only if no members assigned)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/team/roles/page.tsx src/app/\(protected\)/dashboard/agent/team/branches/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Roles page shows 5-tier permission matrix, branch management supports CRUD with team assignment</done>
</task>

</tasks>

<verification>
- All three team pages compile
- Invite sends email via Resend
- Permission matrix renders correctly
- Branch CRUD works
</verification>

<success_criteria>
- Team members page with invite, role assignment, and performance metrics
- Roles & permissions page with 5-tier matrix
- Branch management with CRUD and member assignment
</success_criteria>

<output>
After completion, create `.planning/phases/15-estate-agent-dashboard/15-11-SUMMARY.md`
</output>

---
phase: 15-estate-agent-dashboard
plan: 14
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - britv3.0/src/app/(protected)/dashboard/agent/integrations/page.tsx
  - britv3.0/src/components/dashboard/agent/integrations/ApiKeyManager.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/integrations/feeds/page.tsx
  - britv3.0/src/components/dashboard/agent/integrations/FeedIntegrationConfig.tsx
  - britv3.0/next.config.ts
autonomous: true
requirements:
  - AGT-31
  - AGT-32

must_haves:
  truths:
    - "Agent can generate API keys showing the full key only once"
    - "Agent can revoke API keys and see usage stats"
    - "Agent can configure property feed integration with Reapit, Alto, or Jupix"
    - "Feed sync status is visible with error log display"
  artifacts:
    - path: "britv3.0/src/components/dashboard/agent/integrations/ApiKeyManager.tsx"
      provides: "API key generation, revocation, usage display"
    - path: "britv3.0/src/components/dashboard/agent/integrations/FeedIntegrationConfig.tsx"
      provides: "Feed provider selection, credentials, field mapping, sync status"
  key_links:
    - from: "britv3.0/src/components/dashboard/agent/integrations/ApiKeyManager.tsx"
      to: "britv3.0/src/app/api/agent/billing/route.ts"
      via: "POST to generate key, DELETE to revoke"
      pattern: "fetch.*api/agent"
---

<objective>
Build API Key Management and Property Feed Integration pages, plus final config updates.

Purpose: These pages complete the agent dashboard with developer-oriented features: API access for programmatic integrations and property feed sync with major UK estate agent software (Reapit, Alto, Jupix). Also applies any needed next.config.ts changes (e.g., serverComponentsExternalPackages for @react-pdf/renderer).

Output: 2 page routes + 2 client components + config update.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-estate-agent-dashboard/15-CONTEXT.md
@.planning/phases/15-estate-agent-dashboard/15-04-SUMMARY.md
@britv3.0/src/types/agent.ts
@britv3.0/src/services/agent/agent-billing-service.ts
@britv3.0/src/services/agent/agent-feed-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build API Key Management page</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/integrations/page.tsx, britv3.0/src/components/dashboard/agent/integrations/ApiKeyManager.tsx</files>
  <action>
**integrations/page.tsx** (Server Component):
- Fetch existing API keys via getApiKeys(supabase, user.id)
- Pass to ApiKeyManager

**ApiKeyManager.tsx** ("use client"):
- Header: "API Keys" with description text explaining what API keys are for
- "Generate New Key" button: opens Shadcn Dialog with name input. On submit: calls generateApiKey via POST /api/agent/billing (action='generate_key')
- **Critical UX:** When key is generated, show it in a highlighted box with "Copy" button and warning: "This is the only time you'll see the full key. Copy it now." After dialog close, only the prefix (first 8 chars) is visible.
- Key list table: Name, Key Prefix (e.g., "bst_a1b2..."), Rate Limit (/min), Created, Last Used, Usage Count, Status (active/revoked), Actions
- Actions: "Revoke" button (red, confirmation dialog), "Copy Prefix" (for reference)
- Revoked keys shown greyed out with revoked_at date
- Rate limit display: shows configured rate limit per minute with a "Change" option (updates via API)
- Usage stats: simple count of total API calls
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/integrations/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>API key management page shows keys with prefix only, generates new keys with one-time display, supports revocation</done>
</task>

<task type="auto">
  <name>Task 2: Build Property Feed Integration page and update config</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/integrations/feeds/page.tsx, britv3.0/src/components/dashboard/agent/integrations/FeedIntegrationConfig.tsx, britv3.0/next.config.ts</files>
  <action>
**feeds/page.tsx** (Server Component):
- Fetch feed integrations via getFeedIntegrations(supabase, user.id)
- Pass to FeedIntegrationConfig

**FeedIntegrationConfig.tsx** ("use client"):
- Header: "Property Feed Integrations" with description
- "Add Integration" button: Shadcn Dialog with:
  - Provider select: Reapit, Alto, Jupix (with logo/icon for each)
  - API Key input (password type, stored encrypted)
  - Webhook URL display (auto-generated: `${APP_URL}/api/agent/feeds/webhook/${integration_id}`)
  - Field mapping section: table showing source fields -> Britestate fields mapping. Default mapping pre-filled, editable.
  - "Test Connection" button (calls a health check endpoint if available, otherwise just validates credentials format)
  - "Save" button

- Integration list: cards for each configured feed
  - Provider logo + name
  - Sync status badge: disconnected (grey), connected (green), syncing (blue with spinner), error (red)
  - Last sync timestamp
  - Property count synced
  - Actions: Edit, Delete, "Sync Now" button, "View Error Log"
  - Error log: expandable section showing last 10 errors from error_log JSONB array (timestamp, error message, affected property)

**next.config.ts update:**
- Add `@react-pdf/renderer` to `serverComponentsExternalPackages` if not already present (required for vendor report PDF generation in Plan 09)
- Ensure the config update is minimal and doesn't break existing settings

Note per research: actual property feed API connections require agency-specific Reapit/Alto/Jupix subscriptions. The integration UI stores credentials and shows sync status, but actual sync is a stub/webhook receiver that logs incoming data. Real sync happens when agency provides valid credentials.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/integrations/feeds/page.tsx 2>&1 | head -10 && grep -c "serverComponentsExternalPackages" next.config.ts</automated>
  </verify>
  <done>Feed integration page shows provider config with sync status, next.config.ts updated for @react-pdf/renderer</done>
</task>

</tasks>

<verification>
- Both integration pages compile
- API key shown only once on generation
- Feed config supports all 3 providers
- next.config.ts includes serverComponentsExternalPackages
</verification>

<success_criteria>
- API key management with secure one-time display and revocation
- Property feed integration for Reapit/Alto/Jupix with sync status and error log
- next.config.ts updated for @react-pdf/renderer compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/15-estate-agent-dashboard/15-14-SUMMARY.md`
</output>

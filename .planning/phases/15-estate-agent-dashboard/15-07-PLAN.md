---
phase: 15-estate-agent-dashboard
plan: 07
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - britv3.0/src/app/(protected)/dashboard/agent/leads/page.tsx
  - britv3.0/src/components/dashboard/agent/leads/LeadPipelineKanban.tsx
  - britv3.0/src/components/dashboard/agent/leads/LeadCard.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/leads/[id]/page.tsx
  - britv3.0/src/components/dashboard/agent/leads/LeadDetailTimeline.tsx
autonomous: true
requirements:
  - AGT-09
  - AGT-10
  - AGT-11

must_haves:
  truths:
    - "Lead pipeline shows Kanban board with 5 columns and drag-and-drop between stages"
    - "Drag-and-drop uses optimistic updates (card moves instantly, reverts on error)"
    - "Lead detail page shows full timeline of activities"
    - "Lead assignment updates assigned_to and creates activity entry"
  artifacts:
    - path: "britv3.0/src/components/dashboard/agent/leads/LeadPipelineKanban.tsx"
      provides: "Drag-and-drop Kanban board with @dnd-kit"
    - path: "britv3.0/src/components/dashboard/agent/leads/LeadCard.tsx"
      provides: "Individual lead card with contact info and stage indicator"
    - path: "britv3.0/src/components/dashboard/agent/leads/LeadDetailTimeline.tsx"
      provides: "Lead detail with activity timeline, assignment, notes"
  key_links:
    - from: "britv3.0/src/components/dashboard/agent/leads/LeadPipelineKanban.tsx"
      to: "britv3.0/src/app/api/agent/leads/route.ts"
      via: "PATCH on drag-end to update stage"
      pattern: "fetch.*api/agent/leads"
---

<objective>
Build Lead Management pages: Pipeline Kanban, Lead Detail/Timeline, and Lead Assignment.

Purpose: The lead pipeline is the agent's primary workflow tool. The Kanban board with drag-and-drop lets agents visually manage leads through stages. The detail page provides full context on each lead with activity history.

Output: 2 page routes + 3 client components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-estate-agent-dashboard/15-CONTEXT.md
@.planning/phases/15-estate-agent-dashboard/15-02-SUMMARY.md
@britv3.0/src/types/agent.ts
@britv3.0/src/services/agent/agent-lead-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Lead Pipeline Kanban board</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/leads/page.tsx, britv3.0/src/components/dashboard/agent/leads/LeadPipelineKanban.tsx, britv3.0/src/components/dashboard/agent/leads/LeadCard.tsx</files>
  <action>
**leads/page.tsx** (Server Component):
- Fetch all leads via getAgentLeads(supabase, user.id)
- Group leads by stage into a Record of LeadStage -> AgentLead[]
- Pass grouped leads to LeadPipelineKanban

**LeadPipelineKanban.tsx** ("use client"):
- Import from @dnd-kit/core: DndContext, DragOverlay, closestCorners, PointerSensor, useSensor, useSensors
- Import from @dnd-kit/sortable: SortableContext, verticalListSortingStrategy
- 5 columns: New Enquiry, Qualified, Viewing Booked, Offer Made, Closed
- Each column is a droppable area containing SortableContext of LeadCards
- DragOverlay shows a ghost LeadCard during drag
- OnDragEnd: optimistic update -- move card in local state immediately, call PATCH /api/agent/leads with { id, stage: newStage }. On error, revert via React Query invalidation.
- Header row shows column count badges
- "Add Lead" button opens a Shadcn Dialog with a quick-create form (name, email, phone, source, property link optional)
- Search/filter bar at top: text search on name/email, filter by source
- Use Supabase Realtime subscription to listen for agent_leads changes and update the board in real time (useRealtimeLeads pattern from research)

**LeadCard.tsx** ("use client"):
- Uses @dnd-kit/sortable useSortable() hook
- Displays: contact_name, contact_email (truncated), source badge, property link (if set), assigned_to avatar, relative time
- Click navigates to lead detail page
- Shadcn Card with hover effect
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/leads/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Kanban board renders 5 columns, drag-and-drop works with @dnd-kit, optimistic updates on stage change, real-time subscription active</done>
</task>

<task type="auto">
  <name>Task 2: Build Lead Detail page with timeline and assignment</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/leads/[id]/page.tsx, britv3.0/src/components/dashboard/agent/leads/LeadDetailTimeline.tsx</files>
  <action>
**leads/[id]/page.tsx** (Server Component):
- Fetch lead by ID via getLeadById(supabase, params.id, user.id)
- Fetch lead activities via getLeadActivities(supabase, params.id)
- Fetch team members via getTeamMembers for assignment dropdown
- Pass all to LeadDetailTimeline

**LeadDetailTimeline.tsx** ("use client"):
- Left panel (2/3 width): Activity timeline showing all lead activities in chronological order. Each activity: icon (by type), description, actor name, relative timestamp. Types: lead_created, stage_changed, lead_assigned, note_added, email_sent, viewing_scheduled.
- Right panel (1/3 width): Lead info card with contact details (name, email, phone with click-to-call), current stage badge, source, assigned team member
- Assignment: Shadcn Select dropdown of team members, on change calls PATCH to assignLead
- Stage: dropdown to manually change stage (calls updateLeadStage)
- Notes: textarea to add notes (calls addLeadActivity with type 'note_added')
- Property link: if property_id set, show mini property card with address and image
- Actions row: "Send Email" button (opens mailto), "Schedule Viewing" button (links to /dashboard/agent/viewings with lead context)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/leads/\\[id\\]/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Lead detail shows full timeline, assignment works via dropdown, notes can be added, stage can be changed</done>
</task>

</tasks>

<verification>
- Kanban board compiles and renders 5 columns
- Drag-and-drop uses @dnd-kit with optimistic updates
- Lead detail page shows activity timeline
- Assignment and stage changes create activity entries
</verification>

<success_criteria>
- Lead pipeline Kanban with drag-and-drop stage transitions
- Lead detail page with full activity timeline
- Team member assignment with dropdown
- Real-time updates via Supabase Realtime
</success_criteria>

<output>
After completion, create `.planning/phases/15-estate-agent-dashboard/15-07-SUMMARY.md`
</output>

---
phase: 15-estate-agent-dashboard
plan: 10
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - britv3.0/src/app/(protected)/dashboard/agent/crm/page.tsx
  - britv3.0/src/components/dashboard/agent/crm/ClientList.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/crm/[id]/page.tsx
  - britv3.0/src/components/dashboard/agent/crm/ClientProfile.tsx
autonomous: true
requirements:
  - AGT-19
  - AGT-20

must_haves:
  truths:
    - "CRM client list shows searchable, filterable table with client type badges"
    - "Client profile shows contact details, linked properties, communication history, and preferences"
    - "Bulk actions (email) work on selected clients"
  artifacts:
    - path: "britv3.0/src/components/dashboard/agent/crm/ClientList.tsx"
      provides: "Data table with @tanstack/react-table for CRM clients"
    - path: "britv3.0/src/components/dashboard/agent/crm/ClientProfile.tsx"
      provides: "Client detail page with tabs for different info sections"
  key_links:
    - from: "britv3.0/src/components/dashboard/agent/crm/ClientList.tsx"
      to: "britv3.0/src/app/api/agent/crm/route.ts"
      via: "GET with search/filter params"
      pattern: "fetch.*api/agent/crm"
---

<objective>
Build CRM Client List and Client Profile pages.

Purpose: The CRM gives agents a central place to manage all client relationships -- buyers, sellers, landlords, tenants. The client list supports search and bulk operations. The profile page shows full client context including communication history and linked properties.

Output: 2 page routes + 2 client components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-estate-agent-dashboard/15-CONTEXT.md
@.planning/phases/15-estate-agent-dashboard/15-04-SUMMARY.md
@britv3.0/src/types/agent.ts
@britv3.0/src/services/agent/agent-crm-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CRM Client List page</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/crm/page.tsx, britv3.0/src/components/dashboard/agent/crm/ClientList.tsx</files>
  <action>
**crm/page.tsx** (Server Component):
- Fetch CRM clients via getCrmClients(supabase, user.id)
- Pass to ClientList

**ClientList.tsx** ("use client"):
- Use @tanstack/react-table for the data table
- Columns: Name, Email, Phone, Type (buyer/seller/landlord/tenant badge), Tags, Last Contact, Actions
- Features:
  - Search bar: text search on name/email/phone (debounced, calls API)
  - Filter by client type (multi-select dropdown)
  - Sort by any column (built into react-table)
  - Pagination (cursor-based via API, 25 per page)
  - Row selection checkboxes for bulk actions
  - Bulk action bar (appears when rows selected): "Send Email" (opens mailto with all selected emails), "Add Tag" (dialog to add tag to all)
- "Add Client" button: Shadcn Dialog with form (name, email, phone, type, notes)
- Row click navigates to client profile page
- Shadcn Table, Input, Select, Badge, Button, Dialog components
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/crm/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Client list renders with react-table, search/filter/sort/pagination work, bulk actions available</done>
</task>

<task type="auto">
  <name>Task 2: Build CRM Client Profile page</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/crm/[id]/page.tsx, britv3.0/src/components/dashboard/agent/crm/ClientProfile.tsx</files>
  <action>
**crm/[id]/page.tsx** (Server Component):
- Fetch client by ID via getCrmClientById(supabase, params.id, user.id)
- Pass to ClientProfile

**ClientProfile.tsx** ("use client"):
- Header: Client name, type badge, last contact date, edit button
- Tabbed interface (Shadcn Tabs):
  - **Overview tab**: Contact details card (name, email with mailto link, phone with tel link), preferences (JSONB rendered as key-value pairs), tags as removable badges, notes textarea (editable, saves on blur)
  - **Properties tab**: Linked properties list -- for buyers: properties they enquired about; for sellers: properties they're selling. Each as a mini property card.
  - **Communication tab**: Timeline of messages exchanged with this client (from conversations/messages tables filtered by user_id match). Each message shows sender, content preview, date.
  - **Transactions tab**: Any offers, viewings, or sales involving this client. Links to respective detail pages.
- Edit mode: pencil icon toggles form fields to editable (react-hook-form), save button appears
- "Send Email" action button, "Schedule Viewing" action button
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/crm/\\[id\\]/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Client profile shows tabbed interface with overview, properties, communication history, and transactions</done>
</task>

</tasks>

<verification>
- Both pages compile
- Client list uses @tanstack/react-table
- Client profile has 4 tabs with real data
</verification>

<success_criteria>
- CRM client list with search, filter, sort, pagination, bulk actions
- Client profile with tabbed interface showing full client context
</success_criteria>

<output>
After completion, create `.planning/phases/15-estate-agent-dashboard/15-10-SUMMARY.md`
</output>

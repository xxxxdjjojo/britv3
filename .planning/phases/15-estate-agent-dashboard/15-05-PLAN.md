---
phase: 15-estate-agent-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - britv3.0/src/app/(protected)/dashboard/agent/page.tsx
  - britv3.0/src/components/dashboard/agent/AgentDashboardHome.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/profile/page.tsx
  - britv3.0/src/components/dashboard/agent/AgencyProfileForm.tsx
  - britv3.0/src/app/(protected)/dashboard/agent/profile/branding/page.tsx
  - britv3.0/src/components/dashboard/agent/AgencyBrandingForm.tsx
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-03

must_haves:
  truths:
    - "Dashboard home shows real KPIs from Supabase with trend indicators"
    - "Activity chart renders with Recharts showing 30-day data"
    - "Agency profile form saves to agent_agency_profiles table"
    - "Branding page supports logo upload and colour picker"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/agent/page.tsx"
      provides: "Server component fetching KPIs and activity feed"
    - path: "britv3.0/src/components/dashboard/agent/AgentDashboardHome.tsx"
      provides: "Client component rendering KPI cards, activity chart, recent activity"
    - path: "britv3.0/src/components/dashboard/agent/AgencyProfileForm.tsx"
      provides: "Form with react-hook-form + zod for agency profile editing"
  key_links:
    - from: "britv3.0/src/app/(protected)/dashboard/agent/page.tsx"
      to: "britv3.0/src/services/agent/agent-dashboard-service.ts"
      via: "server-side data fetch"
      pattern: "getAgentDashboardKpis"
---

<objective>
Build the Dashboard Home, Agency Profile Edit, and Agency Branding pages.

Purpose: These are the agent's landing pages -- the first thing they see (dashboard home with KPIs) and their agency identity (profile and branding). Dashboard home replaces the existing mock AgentDashboard.tsx with real data.

Output: 3 page routes + 3 client components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-estate-agent-dashboard/15-CONTEXT.md
@.planning/phases/15-estate-agent-dashboard/15-02-SUMMARY.md
@britv3.0/src/types/agent.ts
@britv3.0/src/services/agent/agent-dashboard-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard Home page with real KPIs</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/page.tsx, britv3.0/src/components/dashboard/agent/AgentDashboardHome.tsx</files>
  <action>
**page.tsx** (Server Component):
- Import createClient from @/lib/supabase/server
- Authenticate with supabase.auth.getUser(), redirect to /login if not authenticated
- Call getAgentDashboardKpis(supabase, user.id) and getAgentActivityFeed(supabase, user.id)
- Pass data to AgentDashboardHome client component
- Replace existing mock AgentDashboard.tsx content

**AgentDashboardHome.tsx** ("use client"):
- Render 5 KPI stat cards in a responsive grid (3 cols on lg, 2 on md, 1 on mobile): Active Listings, New Leads, Viewings This Week, Pending Offers, Performance Score
- Each card shows the count with a trend indicator (up/down arrow with green/red colour -- compare to previous period if available, otherwise just show count)
- Below cards: 30-day activity chart using Recharts AreaChart (x-axis: dates, y-axis: activity count)
- Below chart: Recent Activity feed as a timeline list (icon + description + relative time) using data from activity feed
- Use Shadcn Card, Badge components. Lucide icons for stat types.
- Use Tailwind v4 for all styling. Follow Stitch reference screen "Estate Agent Dashboard - Home"
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Dashboard home renders KPI cards with real data, activity chart with Recharts, activity feed timeline</done>
</task>

<task type="auto">
  <name>Task 2: Build Agency Profile and Branding pages</name>
  <files>britv3.0/src/app/(protected)/dashboard/agent/profile/page.tsx, britv3.0/src/components/dashboard/agent/AgencyProfileForm.tsx, britv3.0/src/app/(protected)/dashboard/agent/profile/branding/page.tsx, britv3.0/src/components/dashboard/agent/AgencyBrandingForm.tsx</files>
  <action>
**Profile page.tsx** (Server Component):
- Fetch existing agency profile from agent_agency_profiles via supabase query
- Pass to AgencyProfileForm

**AgencyProfileForm.tsx** ("use client"):
- react-hook-form with zod validation (agencyProfileSchema from types/agent.ts)
- Fields: agency_name (required), contact_email, contact_phone, address fields, description (textarea), specializations (multi-select or tag input), coverage_areas (tag input)
- Submit via fetch to PATCH /api/agent/dashboard with profile data (add PATCH handler to dashboard route)
- Show sonner toast on success/error
- Shadcn Input, Textarea, Button components

**Branding page.tsx** (Server Component):
- Fetch existing branding data from agent_agency_profiles
- Pass to AgencyBrandingForm

**AgencyBrandingForm.tsx** ("use client"):
- Logo upload: file input with preview, compress with browser-image-compression, upload to Supabase Storage agent-logos bucket, save URL
- Brand colours: two colour picker inputs (primary, secondary) using native HTML input type="color"
- Social links: text inputs for facebook, twitter, instagram, linkedin, website_url
- Agency description: textarea with character count
- Save via PATCH to same endpoint
- Show live preview of logo + brand colours in a mini card
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && npx tsc --noEmit src/app/\(protected\)/dashboard/agent/profile/page.tsx src/app/\(protected\)/dashboard/agent/profile/branding/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Profile form saves all agency fields, branding form uploads logo and saves colours, both pages compile</done>
</task>

</tasks>

<verification>
- Dashboard home page compiles and renders KPI cards
- Profile and branding pages compile
- Forms use react-hook-form + zod validation
- Logo upload uses browser-image-compression
</verification>

<success_criteria>
- Agent dashboard home shows 5 real KPI cards + Recharts activity chart + activity feed
- Agency profile form saves all fields with validation
- Agency branding supports logo upload, colour picker, social links
</success_criteria>

<output>
After completion, create `.planning/phases/15-estate-agent-dashboard/15-05-SUMMARY.md`
</output>

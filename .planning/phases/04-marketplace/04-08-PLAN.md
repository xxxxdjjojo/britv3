---
phase: 04-marketplace
plan: 08
type: execute
wave: 4
depends_on: [04-05, 04-06, 04-07]
files_modified:
  - britv3.0/src/app/(protected)/dashboard/rfqs/page.tsx
  - britv3.0/src/app/(protected)/dashboard/rfqs/create/page.tsx
  - britv3.0/src/app/(protected)/dashboard/rfqs/[id]/page.tsx
  - britv3.0/src/app/(protected)/dashboard/bookings/page.tsx
  - britv3.0/src/app/(protected)/dashboard/bookings/[id]/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/profile/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/documents/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/quotes/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/availability/page.tsx
  - britv3.0/src/app/(protected)/dashboard/reviews/page.tsx
  - britv3.0/src/components/bookings/BookingTimeline.tsx
  - britv3.0/src/components/bookings/BookingStatusBadge.tsx
  - britv3.0/src/components/reviews/ReviewForm.tsx
  - britv3.0/src/components/provider/ProviderProfileForm.tsx
  - britv3.0/src/components/provider/DocumentUpload.tsx
  - britv3.0/src/components/provider/AvailabilityCalendar.tsx
autonomous: false
requirements: [MKT-02, MKT-07, MKT-09, MKT-10, MKT-12]

must_haves:
  truths:
    - "Provider can manage profile, documents, and availability from dashboard"
    - "User can view and manage their RFQs and received quotes"
    - "Booking timeline shows full status history with transition actions"
    - "User can submit a multi-dimensional review for a completed booking"
    - "Review form captures overall, punctuality, quality, value, and professionalism ratings"
  artifacts:
    - path: "britv3.0/src/components/bookings/BookingTimeline.tsx"
      provides: "Booking status history timeline"
      min_lines: 30
    - path: "britv3.0/src/components/reviews/ReviewForm.tsx"
      provides: "Multi-dimensional review form"
      min_lines: 50
    - path: "britv3.0/src/components/provider/ProviderProfileForm.tsx"
      provides: "Provider profile edit form"
      min_lines: 40
  key_links:
    - from: "britv3.0/src/app/(protected)/dashboard/bookings/[id]/page.tsx"
      to: "britv3.0/src/components/bookings/BookingTimeline.tsx"
      via: "Renders timeline with booking status history"
      pattern: "BookingTimeline"
    - from: "britv3.0/src/components/reviews/ReviewForm.tsx"
      to: "/api/reviews/create"
      via: "Form submit handler"
      pattern: "api/reviews/create"
    - from: "britv3.0/src/components/provider/DocumentUpload.tsx"
      to: "/api/providers/documents/upload"
      via: "File upload handler"
      pattern: "api/providers/documents"
---

<objective>
Build all marketplace dashboard pages and remaining components: RFQ management, booking management with timeline, provider dashboard (profile, documents, quotes, availability), review submission, and human verification checkpoint.

Purpose: Gives authenticated users full control over their marketplace interactions -- managing RFQs, bookings, provider profiles, and reviews.
Output: 10 dashboard pages, 6 components, human verification checkpoint.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-marketplace/04-RESEARCH.md
@.planning/phases/04-marketplace/04-01-SUMMARY.md
@.planning/phases/04-marketplace/04-05-SUMMARY.md
@.planning/phases/04-marketplace/04-06-SUMMARY.md
@.planning/phases/04-marketplace/04-07-SUMMARY.md
@britv3.0/src/types/marketplace.ts
@britv3.0/src/lib/validators/marketplace-schemas.ts
@britv3.0/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build booking and review components plus provider dashboard components</name>
  <files>britv3.0/src/components/bookings/BookingTimeline.tsx, britv3.0/src/components/bookings/BookingStatusBadge.tsx, britv3.0/src/components/reviews/ReviewForm.tsx, britv3.0/src/components/provider/ProviderProfileForm.tsx, britv3.0/src/components/provider/DocumentUpload.tsx, britv3.0/src/components/provider/AvailabilityCalendar.tsx</files>
  <action>
**Components:**

1. **BookingTimeline.tsx** -- Vertical timeline showing booking_status_history entries. Each entry: status badge, timestamp, changed_by name, reason (if provided). Current status highlighted. Uses Shadcn Badge for status colors.

2. **BookingStatusBadge.tsx** -- Colored badge for booking status. Colors: pending_confirmation=yellow, confirmed=blue, in_progress=orange, completed=green, cancelled=red, disputed=purple. Uses Shadcn Badge with variant.

3. **ReviewForm.tsx** -- "use client". react-hook-form with reviewCreateSchema. Star rating inputs for: overall (required), punctuality, quality, value, professionalism (all optional but encouraged). Title input. Review text textarea (20-2000 chars with counter). Submit to POST /api/reviews/create. Success state shows review pending moderation message.

4. **ProviderProfileForm.tsx** -- "use client". react-hook-form with providerProfileSchema. Business name, description textarea, services multi-select (16 categories), service postcodes input (comma-separated or tag input), service_radius slider, pricing fields (call_out_fee, hourly_rate, day_rate as optional numbers), qualifications/accreditations textarea. Submit to POST/PUT provider profile API.

5. **DocumentUpload.tsx** -- "use client". Drag-and-drop file upload zone. Shows allowed types (PDF, JPEG, PNG, WebP) and 10MB limit. File list with type selector dropdown (6 document types). Upload progress indicator. Status badges (pending, approved, rejected). Calls POST /api/providers/documents/upload.

6. **AvailabilityCalendar.tsx** -- "use client". Simple date range picker for marking unavailable periods. Shows existing unavailable periods and confirmed bookings. Uses Shadcn Calendar component for date selection. "Add unavailable period" form with start/end dates and optional reason.

All components use Tailwind classes, Shadcn UI, consistent with globals.css theme.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <done>All 6 components compile and build. BookingTimeline renders status history. ReviewForm captures multi-dimensional ratings. Provider forms handle profile, documents, and availability.</done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard pages for RFQs, bookings, provider management, and reviews</name>
  <files>britv3.0/src/app/(protected)/dashboard/rfqs/page.tsx, britv3.0/src/app/(protected)/dashboard/rfqs/create/page.tsx, britv3.0/src/app/(protected)/dashboard/rfqs/[id]/page.tsx, britv3.0/src/app/(protected)/dashboard/bookings/page.tsx, britv3.0/src/app/(protected)/dashboard/bookings/[id]/page.tsx, britv3.0/src/app/(protected)/dashboard/provider/profile/page.tsx, britv3.0/src/app/(protected)/dashboard/provider/documents/page.tsx, britv3.0/src/app/(protected)/dashboard/provider/quotes/page.tsx, britv3.0/src/app/(protected)/dashboard/provider/availability/page.tsx, britv3.0/src/app/(protected)/dashboard/reviews/page.tsx</files>
  <action>
**Dashboard Pages (all "use client" or Server Component as appropriate):**

1. **rfqs/page.tsx** -- Lists user's RFQs with status badges. Filter by status. Each row: title, category, quote_count, status, created date. Link to detail page.

2. **rfqs/create/page.tsx** -- Renders RFQCreateForm component (from Plan 07).

3. **rfqs/[id]/page.tsx** -- RFQ detail with all info. Below: list of received quotes with QuoteComparison component (from Plan 07). "Accept Quote" action buttons.

4. **bookings/page.tsx** -- Lists bookings (user view or provider view based on role). Status filter tabs. Stats cards: pending/confirmed/in_progress/completed counts. Each row: reference, service title, other party name, dates, amount, BookingStatusBadge. Quick-action buttons (confirm for provider, cancel for either).

5. **bookings/[id]/page.tsx** -- Full booking detail. BookingTimeline component. Status transition buttons based on current status and user role (uses getValidNextStatuses logic). Booking info card. Link to "Leave Review" for completed bookings.

6. **provider/profile/page.tsx** -- Renders ProviderProfileForm. Shows verification status badge.

7. **provider/documents/page.tsx** -- Renders DocumentUpload. Shows existing documents with status.

8. **provider/quotes/page.tsx** -- Lists provider's submitted quotes. Filter by status. Each row: quote number, RFQ title, amount, status, date. Link to QuoteCreateForm for new quotes.

9. **provider/availability/page.tsx** -- Renders AvailabilityCalendar.

10. **reviews/page.tsx** -- Shows reviews the user has written AND reviews received (for providers). ReviewForm accessible from here for eligible bookings.

All pages use Tailwind, follow responsive patterns, use Shadcn components for consistency.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <done>All 10 dashboard pages compile and build. RFQ pages list and detail with quote comparison. Booking pages show timeline and status transitions. Provider pages manage profile, documents, quotes, and availability. Reviews page shows written and received reviews.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete marketplace UI renders correctly</name>
  <files></files>
  <action>Human verification of all marketplace pages and components rendering correctly in the browser.</action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build && pnpm vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All marketplace pages render without errors, forms are interactive, and all tests pass.</done>
  <what-built>Complete marketplace UI with provider search, RFQ pipeline, booking lifecycle, provider dashboard, and review system. 12 pages, 14 components, 6 services, 17 API routes.</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/marketplace -- verify search page renders with filters
    2. Click a provider card (if seed data exists) -- verify profile page with ratings
    3. Navigate to /dashboard/rfqs/create -- verify RFQ form renders with all fields
    4. Navigate to /dashboard/bookings -- verify booking list page renders
    5. Navigate to /dashboard/provider/profile -- verify provider profile form renders
    6. Navigate to /dashboard/provider/documents -- verify document upload zone renders
    7. Navigate to /dashboard/reviews -- verify reviews page renders
    8. Run `cd britv3.0 && pnpm build` -- verify no build errors
    9. Run `cd britv3.0 && pnpm vitest run --reporter=verbose` -- verify all marketplace tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with all dashboard pages
- `pnpm vitest run` -- all marketplace tests pass
- All pages render without runtime errors
- Booking timeline displays status history
- Review form captures multi-dimensional ratings
</verification>

<success_criteria>
- 10 dashboard page routes created under (protected)/dashboard
- 6 components created across bookings, reviews, provider directories
- All components use Shadcn UI and Tailwind classes
- Responsive layouts work at mobile and desktop breakpoints
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace/04-08-SUMMARY.md`
</output>

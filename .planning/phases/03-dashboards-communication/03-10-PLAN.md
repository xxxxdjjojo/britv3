---
phase: 03-dashboards-communication
plan: 10
type: execute
wave: 4
depends_on: ["03-06", "03-07", "03-08", "03-09"]
files_modified:
  - britv3.0/src/app/(protected)/layout.tsx
  - britv3.0/src/lib/constants.ts
  - britv3.0/src/__tests__/dashboard/dashboard-api.test.ts
  - britv3.0/src/__tests__/messaging/inbox.test.ts
  - britv3.0/src/__tests__/notifications/feed.test.ts
  - britv3.0/src/__tests__/milestones/milestone-service.test.ts
autonomous: false
requirements: [DASH-14, COM-06, COM-07]

must_haves:
  truths:
    - "Protected layout wraps children with QueryProvider and includes NotificationBell and UnreadBadge"
    - "Navigation includes inbox and notifications links for authenticated users"
    - "Core service layer tests pass for dashboard, messaging, notifications, and milestones"
    - "AI quote drafting service is callable (with mock or real API key)"
  artifacts:
    - path: "britv3.0/src/__tests__/dashboard/dashboard-api.test.ts"
      provides: "Dashboard service unit tests"
    - path: "britv3.0/src/__tests__/messaging/inbox.test.ts"
      provides: "Messaging service unit tests"
    - path: "britv3.0/src/__tests__/notifications/feed.test.ts"
      provides: "Notification service unit tests"
    - path: "britv3.0/src/__tests__/milestones/milestone-service.test.ts"
      provides: "Milestone service unit tests"
  key_links:
    - from: "britv3.0/src/app/(protected)/layout.tsx"
      to: "britv3.0/src/lib/providers/QueryProvider.tsx"
      via: "wraps children"
      pattern: "QueryProvider"
    - from: "britv3.0/src/app/(protected)/layout.tsx"
      to: "britv3.0/src/components/notifications/NotificationBell.tsx"
      via: "renders in header"
      pattern: "NotificationBell"
---

<objective>
Wire Phase 3 features into the application shell: integrate QueryProvider, NotificationBell, and UnreadBadge into the protected layout, update navigation with inbox/notifications links, and create unit tests for all core services.

Purpose: Integration ensures all Phase 3 features are accessible and discoverable. Tests validate service logic.
Output: Updated layout and navigation, 4 test files, integration checkpoint.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboards-communication/03-RESEARCH.md
@.planning/phases/03-dashboards-communication/03-06-SUMMARY.md

<interfaces>
From britv3.0/src/lib/providers/QueryProvider.tsx (created in 03-02):
```typescript
export function QueryProvider({ children }: Readonly<{ children: React.ReactNode }>): JSX.Element;
```

From britv3.0/src/components/notifications/NotificationBell.tsx (created in 03-05):
```typescript
// Bell icon with popover dropdown showing notification feed
```

From britv3.0/src/components/messaging/UnreadBadge.tsx (created in 03-04):
```typescript
// Red badge with unread message count
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Phase 3 features into protected layout and navigation</name>
  <files>britv3.0/src/app/(protected)/layout.tsx, britv3.0/src/lib/constants.ts</files>
  <action>
**Update protected layout (britv3.0/src/app/(protected)/layout.tsx):**
- Wrap children with QueryProvider at the top level of the protected layout
- Add to the header/nav area:
  - NotificationBell component (bell icon with dropdown)
  - Inbox link with UnreadBadge component
- Ensure these render as client components within the server layout (use a client wrapper component if needed)

**Update constants.ts:**
- Add to PROTECTED_ROUTES: "/inbox", "/notifications", "/profile", "/milestones"
- Ensure middleware protects these routes

**Navigation updates:**
- If a navigation component (Header or sidebar) exists, add links:
  - "Inbox" with envelope icon + UnreadBadge
  - "Notifications" with bell icon (or just the NotificationBell)
  - "Profile" with user icon
  - "Dashboard" with layout-dashboard icon
- These should be visible in both desktop nav and MobileNav
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm build</automated>
  </verify>
  <done>Protected layout wraps with QueryProvider, header shows NotificationBell and inbox link with unread badge, navigation updated with Phase 3 links, middleware protects new routes</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for core Phase 3 services</name>
  <files>britv3.0/src/__tests__/dashboard/dashboard-api.test.ts, britv3.0/src/__tests__/messaging/inbox.test.ts, britv3.0/src/__tests__/notifications/feed.test.ts, britv3.0/src/__tests__/milestones/milestone-service.test.ts</files>
  <action>
Use Vitest with mocks from __tests__/mocks/ (supabase, redis, anthropic, resend).

**dashboard-api.test.ts:**
- Test getDashboardData returns role-appropriate data shape
- Test cache hit returns without DB query (mock Redis get to return data)
- Test cache miss queries DB and sets cache (mock Redis get to return null, verify setex called)
- Test invalidateDashboardCache calls Redis del
- Test getActivityLog with cursor pagination (verify WHERE created_at < cursor)

**inbox.test.ts:**
- Test getConversations returns conversations with participant names and unread counts
- Test getMessages with cursor pagination
- Test sendMessage sanitizes content before insert
- Test sendMessage with missing conversation_id creates conversation first
- Test updateReadStatus upserts correctly
- Test attachment validation rejects files > 2MB
- Test attachment validation rejects non-image/pdf files

**feed.test.ts:**
- Test createPlatformEvent inserts single row (O(1))
- Test getNotificationFeed filters by entity_ids and excludes own actions
- Test getNotificationFeed cursor pagination
- Test markAllRead updates profiles.notifications_read_at
- Test shouldSendEmail respects preferences (returns false when email disabled for event type)
- Test shouldSendEmail respects quiet hours

**milestone-service.test.ts:**
- Test initializeTransactionMilestones creates 8 rows
- Test initializeJobMilestones creates 5 rows
- Test updateTransactionMilestone changes status and creates platform event
- Test updateTransactionMilestone to 'completed' sets completed_date
- Test getTransactionProgress returns correct percentage

Mock Supabase using createMockSupabaseClient, Redis using createMockRedis, Anthropic using createMockAnthropic.
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm test --run</automated>
  </verify>
  <done>All 4 test files pass: dashboard caching, messaging CRUD, notification event writes, milestone lifecycle</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 3 integration</name>
  <files>britv3.0/src/app/(protected)/layout.tsx</files>
  <action>
Human verifies the complete Phase 3 integration by checking all pages render and tests pass.
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm build && pnpm test --run</automated>
  </verify>
  <done>All Phase 3 pages render, tests pass, build succeeds</done>
  <what-built>
Complete Phase 3: Dashboards and Communication system including:
- 6 role-specific dashboards with aggregated API + Redis caching
- Polling-based messaging with 30s refresh, cursor pagination, file attachments
- Profile management with avatar upload and provider extended profile
- AI quote drafting via Claude Haiku for tradespeople and agents
- Event-based notification system with email (immediate + daily digest)
- Transaction milestones (8-step) and service job milestones (5-step)
- Files tab for document aggregation
- Notification preferences per user
  </what-built>
  <how-to-verify>
1. Start dev server: `cd britv3.0 && pnpm dev`
2. Navigate to /dashboard/homebuyer -- should show empty dashboard with stat cards
3. Navigate to /profile -- should show profile form with avatar upload
4. Navigate to /inbox -- should show empty inbox
5. Navigate to /notifications -- should show empty notification feed
6. Verify notification bell in header
7. Check build passes: `cd britv3.0 && pnpm build`
8. Run tests: `cd britv3.0 && pnpm test --run`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Protected layout has QueryProvider, NotificationBell, UnreadBadge
- All new routes are protected by middleware
- Unit tests pass for all 4 core services
- Build passes cleanly
- Dev server renders Phase 3 pages without errors
</verification>

<success_criteria>
- QueryProvider wraps protected routes
- Inbox link shows unread count in navigation
- NotificationBell shows in header for authenticated users
- 4 test files with 20+ assertions pass
- Full build succeeds
- User verifies visual output in dev mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboards-communication/03-10-SUMMARY.md`
</output>

---
phase: 03-dashboards-communication
plan: 09
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - britv3.0/src/components/dashboard/homebuyer/HomebuyerDashboard.tsx
  - britv3.0/src/components/dashboard/renter/RenterDashboard.tsx
  - britv3.0/src/components/dashboard/seller/SellerDashboard.tsx
  - britv3.0/src/components/dashboard/landlord/LandlordDashboard.tsx
  - britv3.0/src/components/dashboard/agent/AgentDashboard.tsx
  - britv3.0/src/components/dashboard/provider/ProviderDashboard.tsx
  - britv3.0/src/app/(protected)/dashboard/[role]/page.tsx
  - britv3.0/src/app/(protected)/dashboard/[role]/layout.tsx
  - britv3.0/src/app/(protected)/dashboard/layout.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06]

must_haves:
  truths:
    - "Homebuyer sees saved properties, active searches, upcoming viewings"
    - "Renter sees saved rentals, application status, tenancy details"
    - "Seller sees listing performance, viewing requests, offers"
    - "Landlord sees portfolio overview, occupancy, income"
    - "Agent sees active listings, leads pipeline, viewings, revenue"
    - "Provider sees verification status, active jobs, rating, earnings"
  artifacts:
    - path: "britv3.0/src/components/dashboard/homebuyer/HomebuyerDashboard.tsx"
      provides: "Homebuyer dashboard widget"
    - path: "britv3.0/src/components/dashboard/renter/RenterDashboard.tsx"
      provides: "Renter dashboard widget"
    - path: "britv3.0/src/components/dashboard/seller/SellerDashboard.tsx"
      provides: "Seller dashboard widget"
    - path: "britv3.0/src/components/dashboard/landlord/LandlordDashboard.tsx"
      provides: "Landlord dashboard widget"
    - path: "britv3.0/src/components/dashboard/agent/AgentDashboard.tsx"
      provides: "Agent dashboard widget"
    - path: "britv3.0/src/components/dashboard/provider/ProviderDashboard.tsx"
      provides: "Provider dashboard widget"
    - path: "britv3.0/src/app/(protected)/dashboard/[role]/page.tsx"
      provides: "Dynamic role-based dashboard page"
  key_links:
    - from: "britv3.0/src/app/(protected)/dashboard/[role]/page.tsx"
      to: "britv3.0/src/hooks/useDashboard.ts"
      via: "useDashboard hook for data"
      pattern: "useDashboard"
    - from: "britv3.0/src/app/(protected)/dashboard/[role]/page.tsx"
      to: "britv3.0/src/components/dashboard/DashboardShell.tsx"
      via: "renders inside DashboardShell"
      pattern: "DashboardShell"
---

<objective>
Build all 6 role-specific dashboard pages with their unique widgets, stat cards, and content areas using the shared DashboardShell infrastructure.

Purpose: Dashboards are the home for each role -- the first thing users see after login.
Output: 6 role-specific dashboard components, dynamic route page, layouts.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboards-communication/03-RESEARCH.md
@.planning/phases/03-dashboards-communication/03-06-SUMMARY.md

<interfaces>
From britv3.0/src/types/dashboard.ts (created in 03-01):
```typescript
export type HomebuyerDashboard = Readonly<{ saved_properties: number; active_searches: number; upcoming_viewings: Array<...>; recent_activity: Array<...> }>;
// Similar types for Renter, Seller, Landlord, Agent, Provider
export type StatCardData = Readonly<{ label: string; value: string | number; change?: number; trend?: 'up' | 'down' | 'neutral'; icon?: string }>;
```

From britv3.0/src/hooks/useDashboard.ts (created in 03-06):
```typescript
export function useDashboard(): UseQueryResult<DashboardData>;
export function useRefreshDashboard(): () => void;
export function useActivityLog(): UseInfiniteQueryResult<ActivityLogEntry>;
```

From britv3.0/src/components/dashboard/DashboardShell.tsx (created in 03-06):
```typescript
// Props: role, title, children
// Provides: greeting, stat card grid, activity feed sidebar
```

From britv3.0/src/components/dashboard/StatCard.tsx (created in 03-06):
```typescript
// Props: StatCardData
```

From britv3.0/src/types/auth.ts:
```typescript
export type UserRole = "homebuyer" | "renter" | "seller" | "landlord" | "agent" | "service_provider";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard layouts and dynamic route page</name>
  <files>britv3.0/src/app/(protected)/dashboard/layout.tsx, britv3.0/src/app/(protected)/dashboard/[role]/layout.tsx, britv3.0/src/app/(protected)/dashboard/[role]/page.tsx</files>
  <action>
**dashboard/layout.tsx:**
- Wrap children with QueryProvider (from lib/providers/QueryProvider)
- This ensures all dashboard pages have React Query context
- Add NotificationBell to the header area if not already present in the protected layout

**dashboard/[role]/layout.tsx:**
- Validate that params.role is a valid UserRole; redirect to /dashboard/homebuyer if invalid
- Check that user actually has this role in their user_roles; redirect to their active_role dashboard if not
- Pass role to children via React context or server component prop

**dashboard/[role]/page.tsx** ("use client"):
- Uses useDashboard() hook to fetch aggregated data
- Renders DashboardShell with role-specific title
- Maps dashboard data to StatCard components based on role
- Renders the appropriate role-specific dashboard component:
  - homebuyer -> HomebuyerDashboard
  - renter -> RenterDashboard
  - seller -> SellerDashboard
  - landlord -> LandlordDashboard
  - agent -> AgentDashboard
  - service_provider -> ProviderDashboard
- Skeleton loading state while data loads
- Error state with retry button
- Refresh button calls useRefreshDashboard()
  </action>
  <verify>
    <automated>cd britv3.0 && npx tsc --noEmit && pnpm build</automated>
  </verify>
  <done>Dashboard route validates role, loads aggregated data via hook, renders correct role component inside DashboardShell</done>
</task>

<task type="auto">
  <name>Task 2: Six role-specific dashboard components</name>
  <files>britv3.0/src/components/dashboard/homebuyer/HomebuyerDashboard.tsx, britv3.0/src/components/dashboard/renter/RenterDashboard.tsx, britv3.0/src/components/dashboard/seller/SellerDashboard.tsx, britv3.0/src/components/dashboard/landlord/LandlordDashboard.tsx, britv3.0/src/components/dashboard/agent/AgentDashboard.tsx, britv3.0/src/components/dashboard/provider/ProviderDashboard.tsx</files>
  <action>
Each component receives its typed dashboard data as props and renders role-specific widgets. Use Shadcn Card, Table, Badge, and the shared StatCard component.

**HomebuyerDashboard.tsx:**
- Stat cards: Saved Properties, Active Searches, Upcoming Viewings, Recent Views
- Content: "Upcoming Viewings" list (Card per viewing with property address, date, agent name), "Saved Properties" quick list (first 5 with links), "Quick Actions" (Search Properties, View Shortlist)
- Empty states: "Save your first property" with link to search

**RenterDashboard.tsx:**
- Stat cards: Saved Rentals, Applications Sent, Active Tenancy, Rent Due In
- Content: "Application Status" list (Badge per status: pending, accepted, rejected), "Tenancy Details" card (current address, landlord, lease end), "Quick Actions" (Search Rentals, View Applications)
- Empty states: "Find your next rental" with link to search

**SellerDashboard.tsx:**
- Stat cards: Active Listings, Total Views, Total Saves, Total Enquiries
- Content: "Listing Performance" table (property address, views, saves, enquiries per listing), "Viewing Requests" list (pending requests with accept/decline buttons -- placeholder), "Offers" list (amount, buyer, status)
- Empty states: "List your first property"

**LandlordDashboard.tsx:**
- Stat cards: Properties, Occupancy Rate, Monthly Income, Maintenance Open
- Content: "Portfolio Overview" table (property, tenant, rent status, lease end), "Income Summary" chart area (placeholder for now), "Quick Actions" (Add Property, View Tenants)
- Empty states: "Add your first rental property"

**AgentDashboard.tsx:**
- Stat cards: Active Listings, Total Leads, Viewings This Week, Revenue MTD
- Content: "Leads Pipeline" visual (count badges per stage: new, contacted, viewing, offer, sold), "Today's Viewings" list, "Recent Activity" from dashboard data
- Empty states: "Create your first listing"

**ProviderDashboard.tsx:**
- Stat cards: Verification Status (badge), Active Jobs, Average Rating (stars), Earnings MTD
- Content: "Pending Quotes" list (RFQ description, deadline), "Active Jobs" list (service, status, client), "Verification Progress" (MilestoneTracker-style display of verification stages from auth types)
- Empty states: "Complete your profile to start receiving jobs"

**IMPORTANT:** Many data points will show 0/empty since underlying tables (listings, bookings, etc.) don't exist yet. Components MUST handle this gracefully:
- Show empty states when data is empty/zero
- Never crash on undefined data
- Use optional chaining and default values throughout
- Show helpful CTAs directing users to relevant actions
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm build</automated>
  </verify>
  <done>All 6 role dashboards render with stat cards, content sections, and empty states. Components handle missing data gracefully with helpful CTAs.</done>
</task>

</tasks>

<verification>
- Each role dashboard renders without errors
- Empty states display when no data exists
- Dashboard data maps correctly to stat cards per role
- Dynamic route validates role and redirects invalid roles
- Build passes
</verification>

<success_criteria>
- /dashboard/homebuyer shows saved properties, searches, viewings
- /dashboard/renter shows rentals, applications, tenancy
- /dashboard/seller shows listings with analytics
- /dashboard/landlord shows portfolio, occupancy, income
- /dashboard/agent shows listings, leads, viewings, revenue
- /dashboard/service_provider shows verification, jobs, rating, earnings
- All handle empty data without crashing
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboards-communication/03-09-SUMMARY.md`
</output>

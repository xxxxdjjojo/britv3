---
phase: 03-dashboards-communication
plan: 06
type: execute
wave: 3
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - britv3.0/src/services/dashboard/dashboard-service.ts
  - britv3.0/src/app/api/dashboard/route.ts
  - britv3.0/src/hooks/useDashboard.ts
  - britv3.0/src/components/dashboard/DashboardShell.tsx
  - britv3.0/src/components/dashboard/StatCard.tsx
  - britv3.0/src/components/dashboard/ActivityFeed.tsx
autonomous: true
requirements: [DASH-07, DASH-08, DASH-12, DASH-14]

must_haves:
  truths:
    - "Dashboard API returns aggregated data in 1-2 calls, not 8-12"
    - "Dashboard data is cached in Redis with 5-minute TTL"
    - "Activity log loads with cursor-based pagination"
    - "Dashboard receives real-time updates via Supabase Realtime"
  artifacts:
    - path: "britv3.0/src/services/dashboard/dashboard-service.ts"
      provides: "Aggregated dashboard data service with Redis caching"
      exports: ["getDashboardData", "invalidateDashboardCache"]
    - path: "britv3.0/src/app/api/dashboard/route.ts"
      provides: "Aggregated dashboard API endpoint"
    - path: "britv3.0/src/hooks/useDashboard.ts"
      provides: "Dashboard data hook with Realtime subscription"
      exports: ["useDashboard"]
    - path: "britv3.0/src/components/dashboard/DashboardShell.tsx"
      provides: "Shared dashboard layout shell"
    - path: "britv3.0/src/components/dashboard/StatCard.tsx"
      provides: "Metric display card"
    - path: "britv3.0/src/components/dashboard/ActivityFeed.tsx"
      provides: "Activity log with cursor pagination"
  key_links:
    - from: "britv3.0/src/services/dashboard/dashboard-service.ts"
      to: "britv3.0/src/lib/cache/redis.ts"
      via: "getCached/setCache with 5-min TTL"
      pattern: "setCache.*300"
    - from: "britv3.0/src/hooks/useDashboard.ts"
      to: "britv3.0/src/app/api/dashboard/route.ts"
      via: "useQuery fetch"
      pattern: "fetch.*api/dashboard"
---

<objective>
Build the aggregated dashboard infrastructure: service layer with Redis caching, API endpoint, React hook with Supabase Realtime, and shared dashboard components (shell, stat cards, activity feed).

Purpose: Shared dashboard infrastructure must exist before building the 6 role-specific dashboards in Plan 09.
Output: Dashboard service, API route, hook, 3 shared components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboards-communication/03-RESEARCH.md
@.planning/phases/03-dashboards-communication/03-01-SUMMARY.md
@.planning/phases/03-dashboards-communication/03-02-SUMMARY.md

<interfaces>
From britv3.0/src/types/dashboard.ts (created in 03-01):
```typescript
export type DashboardData = HomebuyerDashboard | RenterDashboard | SellerDashboard | LandlordDashboard | AgentDashboard | ProviderDashboard;
export type StatCardData = Readonly<{ label: string; value: string | number; change?: number; trend?: 'up' | 'down' | 'neutral'; icon?: string }>;
export type ActivityLogEntry = Readonly<{ id: string; event_type: string; description: string; metadata: Record<string, unknown>; created_at: Date }>;
```

From britv3.0/src/types/auth.ts:
```typescript
export type UserRole = "homebuyer" | "renter" | "seller" | "landlord" | "agent" | "service_provider";
```

From britv3.0/src/lib/cache/redis.ts (created in 03-02):
```typescript
export function getCached<T>(key: string): Promise<T | null>;
export function setCache(key: string, value: unknown, ttlSeconds: number): Promise<void>;
export function invalidateCache(key: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard service layer and API route</name>
  <files>britv3.0/src/services/dashboard/dashboard-service.ts, britv3.0/src/app/api/dashboard/route.ts</files>
  <action>
**dashboard-service.ts:**
- `getDashboardData(userId: string, role: UserRole)` -- check Redis cache first with key `dashboard:${userId}`. If cached, return {data, cached: true}. If not, build aggregated query based on role:
  - For each role, query relevant tables and aggregate into the role-specific dashboard shape
  - homebuyer: count saved_properties, count active searches, list upcoming viewings, recent activity
  - renter: count saved_rentals, list application statuses, tenancy details
  - seller: list user's listings with views/saves/enquiries counts, viewing requests, offers received
  - landlord: portfolio property count, occupancy rate, total income, properties summary
  - agent: active listings count, leads by stage, upcoming viewings, revenue total
  - provider: verification status, active jobs count, avg rating, total earnings, pending quotes
  - NOTE: Many of these will return placeholder/stub data since the underlying tables (listings, bookings, etc.) may not exist yet. The service should handle missing tables gracefully with try/catch and return zero/empty defaults.
  - Cache result in Redis with 300s (5-minute) TTL
  - Return {data, cached: false}

- `invalidateDashboardCache(userId: string)` -- delete Redis key `dashboard:${userId}`

- `getActivityLog(userId: string, cursor?: string, limit?: number)` -- query activity_log WHERE user_id = userId, cursor-based pagination on created_at. Default limit 20. Return {entries: ActivityLogEntry[], nextCursor: string | null}.

- `logActivity(userId: string, eventType: string, description: string, metadata?: Record<string, unknown>)` -- INSERT into activity_log. Used by other services to record user actions.

**API route GET /api/dashboard:**
- Authenticate user
- Get user's active_role from profile
- Call getDashboardData(userId, role)
- Return JSON with data + cached flag
- Query param `?refresh=true` bypasses cache (invalidates first)
- Query param `?activity=true&cursor=X` returns activity log instead of dashboard data
  </action>
  <verify>
    <automated>cd britv3.0 && npx tsc --noEmit && pnpm build</automated>
  </verify>
  <done>Dashboard service aggregates data per role with Redis caching (5-min TTL), activity log uses cursor pagination, API route handles both dashboard data and activity requests</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard shared components and Realtime hook</name>
  <files>britv3.0/src/hooks/useDashboard.ts, britv3.0/src/hooks/useRealtime.ts, britv3.0/src/components/dashboard/DashboardShell.tsx, britv3.0/src/components/dashboard/StatCard.tsx, britv3.0/src/components/dashboard/ActivityFeed.tsx</files>
  <action>
**useDashboard.ts** ("use client" hook):
- `useDashboard()` -- useQuery with queryKey ["dashboard"], fetches GET /api/dashboard, staleTime: 300_000 (5 min, matches Redis TTL)
- `useRefreshDashboard()` -- function that invalidates query cache and refetches with ?refresh=true
- `useActivityLog()` -- useInfiniteQuery with queryKey ["activity-log"], fetches GET /api/dashboard?activity=true&cursor=X

**useRealtime.ts** ("use client" hook):
- `useRealtimeSubscription(userId: string, onUpdate: () => void)` -- subscribes to Supabase Realtime channel `dashboard:${userId}`. Listens for postgres_changes on relevant tables (conversations, platform_events). On change, calls onUpdate (which should invalidate dashboard query).
- Uses a single channel per user (not per table) to minimize connection count.
- Cleanup on unmount via useEffect return.

**DashboardShell.tsx** (server component wrapper, with client children):
- Props: role (UserRole), title, children
- Layout: greeting "Welcome back, {displayName}" at top
- Grid container for stat cards (responsive: 1 col mobile, 2 cols md, 3-4 cols lg)
- Below grid: children slot for role-specific content
- Activity feed sidebar on desktop, below content on mobile

**StatCard.tsx:**
- Props: StatCardData (label, value, change, trend, icon)
- Shadcn Card with Lucide icon, value in large text, label below
- Optional trend indicator: green arrow up, red arrow down, grey neutral
- Optional change percentage badge
- Skeleton variant for loading state

**ActivityFeed.tsx** ("use client"):
- Uses useActivityLog() hook
- ScrollArea with max height
- Each entry: icon based on event_type, description text, time ago
- "Load more" button at bottom for cursor pagination
- Skeleton loading state (3 skeleton items)
- Empty state: "No recent activity"
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm build</automated>
  </verify>
  <done>Dashboard hook fetches with React Query and staleTime matching Redis TTL, Realtime hook subscribes to single user channel, shell provides responsive grid layout, stat cards show metrics with trends, activity feed paginates with cursors</done>
</task>

</tasks>

<verification>
- Dashboard API returns role-appropriate data in single call
- Redis cache hit returns data without DB query
- Cache invalidation clears specific user's data
- Activity log paginates correctly with cursor
- Realtime subscription triggers dashboard refresh
- Build passes
</verification>

<success_criteria>
- GET /api/dashboard returns aggregated data per role
- Second call within 5 minutes returns cached: true
- Activity log returns 20 entries per page with nextCursor
- DashboardShell renders responsive grid layout
- StatCard displays value, trend, and icon
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboards-communication/03-06-SUMMARY.md`
</output>

---
phase: 08-db-foundation-security
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/foundation/package-imports.test.ts
autonomous: true
requirements:
  - FOUND-04
gap_closure: true

must_haves:
  truths:
    - "All four package-import smoke tests pass without timeout errors"
    - "react-day-picker and tus-js-client tests complete within 15000ms in the Vitest suite"
  artifacts:
    - path: "src/__tests__/foundation/package-imports.test.ts"
      provides: "Smoke tests for all 4 required packages — all passing"
      contains: "15000"
  key_links:
    - from: "src/__tests__/foundation/package-imports.test.ts"
      to: "node_modules/react-day-picker"
      via: "dynamic import() with 15000ms timeout"
      pattern: "import.*react-day-picker"
    - from: "src/__tests__/foundation/package-imports.test.ts"
      to: "node_modules/tus-js-client"
      via: "dynamic import() with 15000ms timeout"
      pattern: "import.*tus-js-client"
---

<objective>
Fix the two timed-out test cases in the FOUND-04 package-imports smoke test suite by increasing their per-test timeout to 15000ms.

Purpose: The happy-dom Vitest environment requires more than the default 5000ms to resolve the ESM/browser-API initialization for react-day-picker and tus-js-client. The packages are correctly installed — only the test infrastructure timeout is broken.
Output: All 4 package-import tests pass; FOUND-04 has complete automated regression protection.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-db-foundation-security/08-VERIFICATION.md
@src/__tests__/foundation/package-imports.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase timeout for react-day-picker and tus-js-client test cases</name>
  <files>src/__tests__/foundation/package-imports.test.ts</files>
  <action>
The file currently has 4 `it()` calls. The two that time out need a third argument of `15000` (milliseconds). Do NOT change the test logic or assertions — only add the timeout argument.

Current lines 19-27:
```typescript
  it("react-day-picker exports DayPicker component", async () => {
    const rdp = await import("react-day-picker");
    expect(rdp.DayPicker).toBeDefined();
  });

  it("tus-js-client exports Upload class", async () => {
    const tus = await import("tus-js-client");
    expect(tus.Upload).toBeDefined();
  });
```

Replace with:
```typescript
  it("react-day-picker exports DayPicker component", async () => {
    const rdp = await import("react-day-picker");
    expect(rdp.DayPicker).toBeDefined();
  }, 15000);

  it("tus-js-client exports Upload class", async () => {
    const tus = await import("tus-js-client");
    expect(tus.Upload).toBeDefined();
  }, 15000);
```

The nanoid and date-fns tests already pass at the default 5000ms — do not change them.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm test --run src/__tests__/foundation/package-imports.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>pnpm test --run reports 4 passing tests and 0 failures for package-imports.test.ts. Output shows "react-day-picker exports DayPicker component" and "tus-js-client exports Upload class" as passed, not timed out.</done>
</task>

</tasks>

<verification>
1. `pnpm test --run src/__tests__/foundation/package-imports.test.ts` exits 0 with 4 passing tests
2. No "timeout" or "exceeded" in Vitest output for any of the 4 test cases
3. File contains exactly two occurrences of `15000` (one per fixed test case)
4. nanoid and date-fns test signatures are unchanged (no third argument added to them)
</verification>

<success_criteria>
- FOUND-04 achieves full automated regression protection: all 4 package smoke tests green
- The fix is minimal: only the timeout argument changes — no test logic altered
- pnpm test --run exits 0 for this file
</success_criteria>

<output>
After completion, create `.planning/phases/08-db-foundation-security/08-03-SUMMARY.md`
</output>

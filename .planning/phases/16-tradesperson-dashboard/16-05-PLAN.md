---
phase: 16-tradesperson-dashboard
plan: "05"
type: execute
wave: 3
depends_on:
  - "16-02"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/layout.tsx
  - britv3.0/src/components/dashboard/provider/ProviderSidebar.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/page.tsx
  - britv3.0/src/components/dashboard/provider/KPICard.tsx
  - britv3.0/src/components/dashboard/provider/ActivityFeed.tsx
  - britv3.0/src/components/dashboard/provider/UpcomingJobsList.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/profile/page.tsx
  - britv3.0/src/components/dashboard/provider/ProfileEditForm.tsx
autonomous: false
requirements:
  - TPD-01
  - TPD-02

must_haves:
  truths:
    - "Navigating to /dashboard/provider redirects non-authenticated users to /login"
    - "Navigating to /dashboard/provider redirects users without service_provider role to /dashboard"
    - "Dashboard home shows real KPI cards populated from getProviderDashboardStats"
    - "Sidebar contains all 25 nav items grouped by section, matching Stitch design layout"
    - "Profile edit form saves changes to Supabase and shows success toast"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/layout.tsx"
      provides: "Auth guard, role check, ProviderSidebar mount — used by all 25 provider pages"
      min_lines: 35
    - path: "britv3.0/src/components/dashboard/provider/ProviderSidebar.tsx"
      provides: "Left sidebar with logo, 25 nav items in 7 groups, user profile at bottom"
      min_lines: 120
    - path: "britv3.0/src/app/(protected)/dashboard/provider/page.tsx"
      provides: "Dashboard Home — KPI cards, verification banner, activity feed, upcoming jobs"
      min_lines: 50
    - path: "britv3.0/src/app/(protected)/dashboard/provider/profile/page.tsx"
      provides: "Profile Edit — bio, avatar, qualifications, service categories form"
  key_links:
    - from: "britv3.0/src/app/(protected)/dashboard/provider/layout.tsx"
      to: "britv3.0/src/components/dashboard/provider/ProviderSidebar.tsx"
      via: "layout renders ProviderSidebar as the left column"
      pattern: "ProviderSidebar"
    - from: "britv3.0/src/app/(protected)/dashboard/provider/page.tsx"
      to: "britv3.0/src/services/provider/provider-dashboard-service.ts"
      via: "Server Component calls getProviderDashboardStats"
      pattern: "getProviderDashboardStats"
---

<objective>
Create the provider dashboard layout with sidebar, then implement the Dashboard Home (11.1) and My Profile Edit (11.2) pages.

Purpose: The layout.tsx created here is the foundation that ALL 25 provider pages depend on. It must be pixel-accurate to Stitch with Britestate colours. Dashboard Home and Profile are the highest-traffic pages.
Output: Layout, sidebar, 2 page files, 4 components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-02-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch references (read these files for exact layout and component structure):
@.planning/phases/16-tradesperson-dashboard/stitch/dashboard-overview.html
@.planning/phases/16-tradesperson-dashboard/stitch/profile-settings.html

<interfaces>
From provider-dashboard-service.ts (Plan 02):
- getProviderDashboardStats(supabase, providerId): Promise<ProviderDashboardStats>
- getRecentActivity(supabase, providerId, limit?): Promise<RecentActivityItem[]>
- getUpcomingJobs(supabase, providerId, limit?): Promise<UpcomingJob[]>

From provider-profile-service.ts (Plan 02):
- getProviderProfile(supabase, userId): Promise<ServiceProviderDetails | null>
- updateProviderProfile(supabase, providerId, updates): Promise<ServiceProviderDetails>
- uploadAvatar(supabase, userId, file): Promise<string> // returns public URL

ProviderDashboardStats: { new_leads_count, active_jobs_count, pending_reviews_count, monthly_earnings_pence, verification_complete_pct }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider layout.tsx + ProviderSidebar component</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/layout.tsx,
    britv3.0/src/components/dashboard/provider/ProviderSidebar.tsx
  </files>
  <action>
    **layout.tsx** (Server Component — no "use client"):
    - Import createClient from @/lib/supabase/server
    - Call supabase.auth.getUser() — redirect to /login if no user
    - Query user_roles WHERE user_id = user.id AND role = 'service_provider' — redirect to /dashboard if no match
    - Fetch service_provider_details for sidebar user info (business_name, avatar_url)
    - Render: `<div className="flex min-h-screen bg-neutral-50">` wrapping `<ProviderSidebar>` + `<main className="flex-1 overflow-y-auto bg-neutral-50">{children}</main>`
    - ProviderSidebar receives: { userId, businessName, avatarUrl, currentPath }

    **ProviderSidebar.tsx** ("use client" — for active nav state with usePathname):
    Read stitch/dashboard-overview.html for exact nav group structure. Map ALL nav items using Britestate colour tokens:

    Stitch colour mapping (MANDATORY):
    - Stitch `#11d432`, `#17cf97` background → Britestate `#1B4D3E` (brand-primary)
    - Stitch `bg-card-dark` (#1a2e1d) → Britestate `bg-[#152C22]` (dark sidebar background, dark mode)
    - Stitch `border-dark` (#2a3f2d) → Britestate `border-[#1B4D3E]/30`
    - Active nav item: `bg-brand-primary-lighter text-brand-primary` (light mode) / `bg-[#1B4D3E] text-white` (dark)
    - Hover: `hover:bg-neutral-100 dark:hover:bg-[#1B4D3E]/20`

    Sidebar structure (7 nav groups with all 25 routes from CONTEXT.md):
    Group 1 — Overview: Dashboard Home (/)
    Group 2 — Profile & Trust: My Profile, Verification Centre
    Group 3 — Services: Manage Services, Service Areas, Availability
    Group 4 — Jobs: New Leads, Active Jobs, Completed Jobs
    Group 5 — Financial: Quote Builder, Invoice Generator, Payments
    Group 6 — Growth: Portfolio, Reviews, Analytics
    Group 7 — Account: Subscription, Boost Profile, Referral Programme

    Each nav item: icon (Lucide), label, href. Active state via usePathname() comparison.
    User profile section at bottom: avatar, business_name, "View public profile" link.

    Use Plus Jakarta Sans for the "BRITESTATE" logo text (font-display class or direct className="font-[\'Plus_Jakarta_Sans\']").
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <done>Layout.tsx exists with auth guard; ProviderSidebar renders all 25 nav items in 7 groups; pnpm build passes</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Home (11.1) + My Profile Edit (11.2)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/page.tsx,
    britv3.0/src/components/dashboard/provider/KPICard.tsx,
    britv3.0/src/components/dashboard/provider/ActivityFeed.tsx,
    britv3.0/src/components/dashboard/provider/UpcomingJobsList.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/profile/page.tsx,
    britv3.0/src/components/dashboard/provider/ProfileEditForm.tsx
  </files>
  <action>
    **Dashboard Home page.tsx** (Server Component):
    Reference stitch/dashboard-overview.html for exact layout. Implement:

    1. Fetch in parallel: getProviderDashboardStats, getRecentActivity(limit=8), getUpcomingJobs(limit=5)
    2. Verification banner: Show if verification_complete_pct < 100. Full-width banner with brand-primary background, progress bar (65% in Stitch), "Complete Verification" CTA button. Map Stitch dark card style to Britestate — use `bg-brand-primary text-white rounded-xl p-8`.
    3. KPI row: 4 KPICard components. Each card: icon, title, value, change indicator (△ or ▽ with color). Card values: New Leads (new_leads_count), Active Jobs (active_jobs_count), Pending Reviews (pending_reviews_count), Monthly Earnings (£{monthly_earnings_pence/100 formatted}).
    4. Two-column grid (lg:grid-cols-2): Recent Activity (left) + Upcoming Jobs (right)
    5. Quick action buttons row: "New Quote", "Log Payment", "Add Portfolio Item", "Invite to Review"

    **KPICard.tsx** (Server Component — no interactivity):
    Props: { title: string, value: string, icon: LucideIcon, trend?: { value: number, direction: 'up'|'down' } }
    Style: white card, rounded-xl, shadow-sm, border border-neutral-200, p-6. Icon in brand-primary-lighter rounded-lg background.

    **ActivityFeed.tsx** (Server Component):
    Props: { items: RecentActivityItem[] }
    List of activity items with icon per type, timestamp in relative time ("2h ago"), clickable links.

    **UpcomingJobsList.tsx** (Server Component):
    Props: { jobs: UpcomingJob[] }
    Table/list of upcoming jobs with: job title, client name, date, status badge, amount. Link to /dashboard/provider/jobs/{id}.

    **Profile page.tsx** (Server Component shell + client form):
    - Server: fetch getProviderProfile, pass to ProfileEditForm
    - ProfileEditForm.tsx ("use client"): react-hook-form + zod validation
      - Avatar upload: react-dropzone circular upload zone (same as Stitch design)
      - Bio textarea: character count (max 2000), displays remaining
      - Business name, phone, website fields
      - Qualifications: multi-entry (add/remove text fields)
      - Service categories: multi-select checkboxes from ServiceCategory enum
      - Save button: calls updateProviderProfile Server Action, shows sonner toast on success/error
      - Live preview panel (right column on desktop): shows how profile appears publicly. Implement as a simplified card preview of name, bio truncated, categories.

    Colour remapping for ALL components (per britestatestyle.txt):
    - Primary green: bg-[#1B4D3E] (brand-primary), text-[#1B4D3E]
    - Light green bg: bg-[#E8F5EE] (brand-primary-lighter)
    - Success: text-[#16A34A] (success token)
    - All text uses neutral-900 / neutral-700 / neutral-500 for hierarchy
    - Fonts: Plus Jakarta Sans for headings (className includes font-sans for headings), Inter for body
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "error|Error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Dashboard home renders 4 KPI cards + verification banner + activity feed + upcoming jobs; Profile edit form has avatar upload + bio + qualifications; pnpm build passes</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 build errors
2. `cd britv3.0 && pnpm lint` — 0 lint errors
3. Navigate to /dashboard/provider as unauthenticated user → redirects to /login
4. Navigate to /dashboard/provider as non-provider user → redirects to /dashboard
5. Visual: KPI cards row is present with all 4 metrics

human-verify: Sidebar nav groups visible with all 25 items; Britestate brand-primary (#1B4D3E) used consistently (NOT Stitch placeholder green)
</verification>

<success_criteria>
- layout.tsx auth-guards ALL 25 provider pages
- ProviderSidebar has all 25 nav items grouped correctly
- Dashboard home shows real data from Supabase (not mock arrays)
- Profile edit form persists changes with success/error feedback
- Zero Stitch placeholder colours (#11d432, #17cf97) in any component — all remapped to #1B4D3E
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md`
</output>

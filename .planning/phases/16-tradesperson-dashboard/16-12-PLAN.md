---
phase: 16-tradesperson-dashboard
plan: "12"
type: execute
wave: 5
depends_on:
  - "16-04"
  - "16-05"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/analytics/page.tsx
  - britv3.0/src/components/dashboard/provider/AnalyticsCharts.tsx
  - britv3.0/src/components/dashboard/provider/ConversionFunnel.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/boost/page.tsx
  - britv3.0/src/components/dashboard/provider/BoostSelector.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/referrals/page.tsx
  - britv3.0/src/components/dashboard/provider/ReferralCard.tsx
  - britv3.0/src/app/api/provider/boost/route.ts
  - britv3.0/src/app/api/provider/referrals/route.ts
autonomous: false
requirements:
  - TPD-22
  - TPD-24
  - TPD-25

must_haves:
  truths:
    - "Analytics page shows earnings area chart with period selector (7d/30d/90d)"
    - "Conversion funnel shows Profile Views → Enquiries → Quotes Sent → Bookings from real data"
    - "Boost/Promote page shows active promotions and allows purchasing new boosts"
    - "Referral Programme page shows referral link with copy button and referred providers list"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/analytics/page.tsx"
      provides: "Analytics page (11.22) — charts, funnel, top categories"
    - path: "britv3.0/src/components/dashboard/provider/AnalyticsCharts.tsx"
      provides: "Recharts AreaChart for earnings trend + BarChart for top categories"
    - path: "britv3.0/src/components/dashboard/provider/ConversionFunnel.tsx"
      provides: "4-stage conversion funnel visualization"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/boost/page.tsx"
      provides: "Promote/Boost Profile page (11.24)"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/referrals/page.tsx"
      provides: "Referral Programme page (11.25)"
  key_links:
    - from: "britv3.0/src/components/dashboard/provider/AnalyticsCharts.tsx"
      to: "britv3.0/src/services/provider/provider-analytics-service.ts"
      via: "Page fetches analytics data server-side; charts receive typed data props"
      pattern: "getProviderAnalytics"
    - from: "britv3.0/src/app/(protected)/dashboard/provider/referrals/page.tsx"
      to: "britv3.0/src/services/provider/provider-referral-service.ts"
      via: "Server Component calls getReferralCode + getReferralStats"
      pattern: "getReferralCode"
---

<objective>
Build the Analytics, Boost/Promote, and Referral Programme pages — the three growth tools.

Purpose: Analytics closes the feedback loop for tradespeople on what's working; boosts drive more lead visibility; referrals grow the provider base organically.
Output: 3 pages + 4 components + 2 API routes covering TPD-22, TPD-24, and TPD-25.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-04-SUMMARY.md
@.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch reference (read for analytics layout and charts):
@.planning/phases/16-tradesperson-dashboard/stitch/analytics-payouts.html

<interfaces>
From provider-analytics-service.ts (Plan 04):
- getProviderAnalytics(supabase, providerId, period: '7d'|'30d'|'90d'): returns { profile_views_total, profile_views_by_day, enquiry_rate_pct, conversion_by_stage: ConversionFunnel, earnings_by_month, top_categories }
- ConversionFunnel: { viewed: number, enquired: number, quoted: number, booked: number }

From provider-boost-service.ts (Plan 04):
- getActiveBoosts(supabase, providerId): ProviderBoost[]
- createBoostCheckout(providerId, config): { checkout_url }

ProviderBoost: { id, boost_type, coverage_area, starts_at, ends_at, is_active }

From provider-referral-service.ts (Plan 04):
- getReferralCode(supabase, providerId): string (nanoid code, created if not exists)
- getReferralStats(supabase, providerId): { total_referred, by_status: Record<string, number>, total_rewards_pence }
- getReferrals(supabase, providerId): ProviderReferral[]

Recharts (already installed, ^2.15.4):
- AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer
- BarChart, Bar
- All charts are "use client" (browser rendering)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics (11.22) with Recharts</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/analytics/page.tsx,
    britv3.0/src/components/dashboard/provider/AnalyticsCharts.tsx,
    britv3.0/src/components/dashboard/provider/ConversionFunnel.tsx
  </files>
  <action>
    Reference stitch/analytics-payouts.html analytics section for layout.

    **Analytics page.tsx** (Server Component):
    - Accept URL search param ?period= (default '30d')
    - Fetch getProviderAnalytics(supabase, providerId, period) server-side
    - Render: period selector tabs, then AnalyticsCharts + ConversionFunnel + top_categories section

    **AnalyticsCharts.tsx** ("use client" — Recharts requires client):
    Props: { earningsByMonth: Array<{month: string, total_pence: number}>, viewsByDay: Array<{date: string, count: number}>, period: '7d'|'30d'|'90d' }

    Two charts:
    1. **Earnings Trend** (AreaChart, top half):
       - X-axis: month labels (abbreviated: "Jan", "Feb")
       - Y-axis: £ amounts (format as "£{N}")
       - Area fill: gradient from brand-primary (#1B4D3E) at top to transparent
       - Tooltip: shows £{amount} for hovered month
       - Use ResponsiveContainer width="100%" height={280}
       - Line stroke: #1B4D3E, strokeWidth 2

    2. **Profile Views** (AreaChart below earnings if 30d+ period):
       - Same styling but filled with brand-secondary-light (#F5ECD7) and stroke #D4A853 (gold)
       - X-axis: date labels (abbreviated)

    3. **Top Service Categories** (BarChart, bottom section):
       - Horizontal bars: category name (Y-axis), job count (X-axis)
       - Bar fill: #1B4D3E
       - Shows top 5 categories by job count

    Period selector (tab row above charts):
    - "7d" | "30d" | "90d" tabs; active tab = brand-primary bg white text
    - On click → update URL ?period= param (client-side navigation using useRouter)

    **ConversionFunnel.tsx** (Server Component — pure visualization):
    Props: { funnel: ConversionFunnel }
    - 4-stage horizontal funnel visualization (not Recharts — custom CSS/SVG)
    - Stages: "Profile Views" → "Enquiries" → "Quotes Sent" → "Bookings"
    - Each stage: count in large text, stage label, conversion rate % below
    - Funnel shape: each stage slightly narrower than previous (CSS clip-path or border trick)
    - Colours: stage 1 = neutral-200, stage 2 = brand-primary-lighter, stage 3 = brand-primary/60, stage 4 = brand-primary
    - Conversion rate between stages: small arrow with "X% conversion"
    - If funnel data is all 0 (new provider): show placeholder "Build your history to see conversion data"
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Analytics page renders Recharts AreaChart for earnings, ConversionFunnel, and top categories bar chart; period selector updates URL param; pnpm build passes</done>
</task>

<task type="auto">
  <name>Task 2: Boost/Promote (11.24) + Referral Programme (11.25)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/boost/page.tsx,
    britv3.0/src/components/dashboard/provider/BoostSelector.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/referrals/page.tsx,
    britv3.0/src/components/dashboard/provider/ReferralCard.tsx,
    britv3.0/src/app/api/provider/boost/route.ts,
    britv3.0/src/app/api/provider/referrals/route.ts
  </files>
  <action>
    **Boost page.tsx** (Server Component):
    - Fetch getActiveBoosts(supabase, providerId)
    - Render: active promotions section (if any) + BoostSelector for purchasing new boosts

    **BoostSelector.tsx** ("use client"):
    Props: { activeBoosts: ProviderBoost[], providerId: string }

    Layout (2 sections):
    1. Active Promotions (if any):
       - Card per active boost: boost_type label, coverage_area, "Expires in N days" progress bar
       - Expired boosts: greyed out with "Expired" badge

    2. Purchase Boost section — 3 option cards:
       - **Featured Profile** (£29/week): Appear at top of search results in your area. Features: pinned placement, "Featured" badge on profile, priority in email alerts.
       - **Area Spotlight** (£49/week): Highlighted in specific location or service category spotlight. Features: banner in search results, 3x visibility boost in target area.
       - **Category Top** (£79/week): Top position in a specific trade category nationally. Features: category homepage banner, newsletter feature, premium badge.

       Each card: price prominent, feature list with tick marks, duration selector (1 week / 2 weeks / 1 month), "Boost Now" button
       "Boost Now" → calls POST /api/provider/boost → receives { checkout_url } → redirect to Stripe Checkout
       Show "Active" badge if same type boost is already active

    **API route boost/route.ts** (POST):
    - Auth, validate { boost_type, coverage_area?, duration_days }
    - Call createBoostCheckout(providerId, config)
    - Return { checkout_url }
    - Also: GET handler returning active boosts list

    ---

    **Referrals page.tsx** (Server Component):
    - Fetch getReferralCode(supabase, providerId) — creates code if none exists
    - Fetch getReferralStats(supabase, providerId)
    - Fetch getReferrals(supabase, providerId) — list of referred providers
    - Pass all data to ReferralCard

    **ReferralCard.tsx** ("use client" for copy button):
    Props: { referralCode: string, stats: ReferralStats, referrals: ProviderReferral[] }

    Layout sections:
    1. **Referral Link panel** (prominent, top):
       - "Refer a tradesperson, earn £50 per verified provider" heading
       - Referral URL: `https://britestate.co.uk/join?ref={code}` displayed in an input
       - "Copy Link" button: navigator.clipboard.writeText() → changes to "Copied!" for 2s with check icon
       - Share buttons: WhatsApp (`https://wa.me/?text=...`), Email (`mailto:?subject=...`)

    2. **Stats row** (3 stat cards):
       - Total Invited: count of all referrals
       - Signed Up: count with status in ['signed_up','verified','rewarded']
       - Total Earned: £{total_rewards_pence/100} (sum of rewarded referrals)

    3. **Referred Providers table**:
       - Columns: Name/Email, Date Invited, Status, Reward
       - Status badges: pending = neutral, signed_up = info blue, verified = brand-primary, rewarded = success green
       - Reward column: "£50 pending" (verified but not rewarded), "£50 earned" (rewarded), "-" (pending/signed_up)
       - Empty state: "No referrals yet — share your link to get started"

    **API route referrals/route.ts** (GET):
    - Auth, return { referral_code, stats, referrals } from service functions

    Referral code generation: nanoid(8) uppercase alphanumeric (e.g., "K8JM2NQP")
    Referral reward: display £50 per verified provider (per CONTEXT.md discretion: £50 flat amount)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Boost page shows 3 purchasable boost options with duration selector; Referral page shows copyable link with stats and referred providers table; pnpm build passes</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 errors
2. `cd britv3.0 && pnpm lint` — 0 errors
3. Analytics Recharts charts render without SSR errors (charts are "use client")
4. Referral link copy button copies to clipboard
5. Boost page shows 3 purchase options with pricing
6. Visual checkpoint: all pages use brand-primary (#1B4D3E) not Stitch placeholder greens
</verification>

<success_criteria>
- Recharts charts are client-side only (no SSR render errors)
- Referral code is auto-generated with nanoid if provider has no code yet
- Boost options show correct pricing and duration options
- All 25 provider dashboard routes now exist and render without build errors
- pnpm build passes with 0 errors
- Complete Phase 16 checklist: 25 pages, 9 DB tables, 8 service files, 12 plans
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-12-SUMMARY.md`
</output>

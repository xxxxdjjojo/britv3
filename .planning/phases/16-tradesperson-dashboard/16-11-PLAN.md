---
phase: 16-tradesperson-dashboard
plan: "11"
type: execute
wave: 5
depends_on:
  - "16-04"
  - "16-05"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/portfolio/page.tsx
  - britv3.0/src/components/dashboard/provider/PortfolioGrid.tsx
  - britv3.0/src/components/dashboard/provider/PortfolioItemCard.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/reviews/page.tsx
  - britv3.0/src/components/dashboard/provider/ReviewsBreakdown.tsx
  - britv3.0/src/components/dashboard/provider/ReviewCard.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/reviews/[id]/respond/page.tsx
  - britv3.0/src/components/dashboard/provider/ReviewResponseForm.tsx
  - britv3.0/src/app/api/provider/portfolio/route.ts
  - britv3.0/src/app/api/provider/reviews/[id]/respond/route.ts
autonomous: false
requirements:
  - TPD-19
  - TPD-20
  - TPD-21

must_haves:
  truths:
    - "Portfolio/Gallery allows uploading before/after photo pairs with drag-and-drop reorder"
    - "Reordering updates display_order values in provider_portfolio_items"
    - "Reviews Dashboard shows star rating breakdown chart with aggregate stats"
    - "Provider can submit a public response to a review, visible on their public profile"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/portfolio/page.tsx"
      provides: "Portfolio/Gallery Manage page (11.19)"
    - path: "britv3.0/src/components/dashboard/provider/PortfolioGrid.tsx"
      provides: "dnd-kit sortable grid of portfolio items with before/after photo pairs"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/reviews/page.tsx"
      provides: "Reviews Dashboard (11.20) — star breakdown, all reviews, filter"
    - path: "britv3.0/src/components/dashboard/provider/ReviewsBreakdown.tsx"
      provides: "Star rating bar chart breakdown (5-star to 1-star)"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/reviews/[id]/respond/page.tsx"
      provides: "Reviews Respond page (11.21)"
  key_links:
    - from: "britv3.0/src/components/dashboard/provider/PortfolioGrid.tsx"
      to: "britv3.0/src/services/provider/provider-portfolio-service.ts"
      via: "reorderPortfolioItems called after drag-and-drop completes"
      pattern: "reorderPortfolioItems"
    - from: "britv3.0/src/app/(protected)/dashboard/provider/reviews/[id]/respond/page.tsx"
      to: "britv3.0/src/app/api/provider/reviews/[id]/respond/route.ts"
      via: "ReviewResponseForm submits to API route"
      pattern: "fetch.*reviews.*respond"
---

<objective>
Build the Portfolio/Gallery management, Reviews Dashboard, and Review Response pages.

Purpose: Portfolio and reviews are the two primary trust signals on a tradesperson's public profile. Clean management tools drive completeness.
Output: 3 pages + 5 components + 2 API routes covering TPD-19 through TPD-21.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-04-SUMMARY.md
@.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch reference (read for portfolio and reviews layout):
@.planning/phases/16-tradesperson-dashboard/stitch/portfolio-reviews.html

<interfaces>
From provider-portfolio-service.ts (Plan 04):
- getPortfolioItems(supabase, providerId): ProviderPortfolioItem[]
- addPortfolioItem(supabase, providerId, input): ProviderPortfolioItem
- updatePortfolioItem(supabase, providerId, itemId, updates): ProviderPortfolioItem
- deletePortfolioItem(supabase, providerId, itemId): void
- reorderPortfolioItems(supabase, providerId, orderedIds: string[]): void

ProviderPortfolioItem: { id, title, description, category, before_image_path, after_image_path, is_featured, display_order }

From Phase 4 reviews table:
- reviews: id, booking_id, reviewer_id, reviewee_id, overall_rating, communication_rating, quality_rating, value_rating, comment, provider_response, created_at
- review-service.ts: getReviews(supabase, revieweeId): Review[]
- respondToReview(supabase, revieweeId, reviewId, responseText): Review (updates provider_response field)

@dnd-kit packages (already installed):
- @dnd-kit/core: DndContext, DragEndEvent
- @dnd-kit/sortable: SortableContext, useSortable, arrayMove, verticalListSortingStrategy
- @dnd-kit/modifiers: restrictToParentElement
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portfolio/Gallery Manage (11.19)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/portfolio/page.tsx,
    britv3.0/src/components/dashboard/provider/PortfolioGrid.tsx,
    britv3.0/src/components/dashboard/provider/PortfolioItemCard.tsx,
    britv3.0/src/app/api/provider/portfolio/route.ts
  </files>
  <action>
    Reference stitch/portfolio-reviews.html portfolio section for layout.

    **Portfolio page.tsx** (Server Component shell):
    - Fetch getPortfolioItems(supabase, providerId)
    - Pass to PortfolioGrid client component
    - "Add Project" button renders a dialog for new item entry

    **PortfolioGrid.tsx** ("use client" — dnd-kit requires browser events):
    Props: { items: ProviderPortfolioItem[], providerId: string }

    dnd-kit sortable grid implementation:
    ```typescript
    const [items, setItems] = useState(initialItems);
    const sensors = useSensors(useSensor(PointerSensor), useSensor(KeyboardSensor));

    function handleDragEnd(event: DragEndEvent) {
      const { active, over } = event;
      if (active.id !== over?.id) {
        const newOrder = arrayMove(items, oldIndex, newIndex);
        setItems(newOrder);
        // Persist: call reorderPortfolioItems with newOrder.map(i => i.id)
        reorderPortfolioItemsAction(providerId, newOrder.map(i => i.id));
      }
    }

    <DndContext sensors={sensors} onDragEnd={handleDragEnd}>
      <SortableContext items={items} strategy={rectSortingStrategy}>
        {items.map(item => <PortfolioItemCard key={item.id} item={item} />)}
      </SortableContext>
    </DndContext>
    ```

    Grid layout: 3 columns on desktop (lg:grid-cols-3), 2 on tablet, 1 on mobile.

    "Add Project" dialog form:
    - Title (required, max 100 chars)
    - Description (textarea, max 500 chars)
    - Category (select from ServiceCategory)
    - Before photo upload: react-dropzone, accept image/*, max 5MB, shows preview
    - After photo upload: same
    - Uploads images to Supabase Storage 'portfolio' bucket at path `{providerId}/{itemId}/{before|after}`
    - Submits to POST /api/provider/portfolio on save

    **PortfolioItemCard.tsx** ("use client" — useSortable):
    Props: { item: ProviderPortfolioItem }
    - useSortable hook for drag handle
    - Before/after image comparison (side-by-side or slider):
      - Show "Before" and "After" labels on photos
      - If no before photo, show single "After" photo full width
    - Item title, category pill, description (truncated)
    - Drag handle icon (GripVertical from Lucide) on left edge
    - Edit button: opens pre-filled dialog
    - Delete button: confirmation → deletePortfolioItem → removes from list
    - "Set as Featured" toggle: updates is_featured field

    **API route portfolio/route.ts** (POST, GET):
    - POST: auth, zod validation ({ title, description?, category?, before_image_path?, after_image_path? }), call addPortfolioItem, return 201
    - GET: auth, call getPortfolioItems, return array
    - PATCH /api/provider/portfolio/reorder: accept { orderedIds: string[] }, call reorderPortfolioItems
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Portfolio grid renders with dnd-kit drag handles; dragging and dropping reorders items; add project dialog uploads photos and creates portfolio_items row</done>
</task>

<task type="auto">
  <name>Task 2: Reviews Dashboard (11.20) + Review Response (11.21)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/reviews/page.tsx,
    britv3.0/src/components/dashboard/provider/ReviewsBreakdown.tsx,
    britv3.0/src/components/dashboard/provider/ReviewCard.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/reviews/[id]/respond/page.tsx,
    britv3.0/src/components/dashboard/provider/ReviewResponseForm.tsx,
    britv3.0/src/app/api/provider/reviews/[id]/respond/route.ts
  </files>
  <action>
    Reference stitch/portfolio-reviews.html reviews section for layout.

    **Reviews page.tsx** (Server Component):
    - Fetch getReviews(supabase, revieweeId=providerId) from review-service.ts
    - Compute aggregate stats: total count, average overall_rating, count per star level
    - Filter param: URL ?rating=1|2|3|4|5 for filter by star
    - Pass data to ReviewsBreakdown + ReviewCard list

    **ReviewsBreakdown.tsx** (Server Component):
    Props: { totalCount, averageRating, countByStars: { 5: N, 4: N, 3: N, 2: N, 1: N } }
    - Large average rating number (e.g., "4.7") + star icons
    - Horizontal bar chart per star level: 5★ through 1★
    - Bar width proportional to percentage (calculated from counts)
    - Bar colour: brand-primary (#1B4D3E) filled, neutral-100 track
    - "Based on {N} reviews" subtitle
    - Also show sub-dimension averages: Communication, Quality, Value (from respective rating columns)

    **ReviewCard.tsx** (Server Component):
    Props: { review: Review, onRespond?: string } // onRespond = URL to respond page
    - Reviewer avatar (placeholder if no avatar), name, date
    - Star rating display (filled/empty star icons)
    - Review comment text
    - Sub-dimension pills: "Communication ★4", "Quality ★5", "Value ★3"
    - If provider_response exists: response section with grey background, "Response from provider" label, response text
    - If no provider_response and onRespond provided: "Reply to review" button → link to reviews/{id}/respond

    **Reviews Respond page.tsx** (Server Component):
    - Fetch the specific review by ID (must belong to this provider's reviewee_id)
    - Pass review + providerId to ReviewResponseForm

    **ReviewResponseForm.tsx** ("use client"):
    Props: { review: Review, providerId: string }
    - Shows original review (read-only) at top
    - Response textarea: max 500 chars, character counter
    - Guidelines box (amber bg): "Your response is public and cannot be edited after saving. Be professional."
    - Preview section: shows how response will appear on profile
    - Submit button: calls POST /api/provider/reviews/{id}/respond
    - On success: redirect to /reviews with sonner toast "Response submitted"
    - Cancel button: back to /reviews

    **API route reviews/[id]/respond/route.ts** (POST):
    - Auth + ownership check (review.reviewee_id must match provider's ID)
    - Validate response_text: required, max 500 chars, min 10 chars
    - If review already has provider_response: return 409 (cannot edit existing response)
    - Call respondToReview(supabase, revieweeId, reviewId, response_text) from review-service
    - Return 200 with updated review
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Reviews breakdown chart shows star distribution bars; review cards show existing responses; review respond form enforces 500 char limit and one-time submission</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 errors
2. `cd britv3.0 && pnpm lint` — 0 errors
3. Portfolio grid renders with drag handles visible
4. Reviews page shows aggregate star breakdown chart
5. Review response form shows character counter and guidelines box
</verification>

<success_criteria>
- Portfolio drag-and-drop reorders items AND persists display_order to database
- Review responses enforce one-time submission (409 if already responded)
- Star breakdown bar chart widths are proportional to actual review counts
- No mock data arrays — all from Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-11-SUMMARY.md`
</output>

---
phase: 16-tradesperson-dashboard
plan: "10"
type: execute
wave: 4
depends_on:
  - "16-04"
  - "16-05"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/payments/page.tsx
  - britv3.0/src/components/dashboard/provider/PaymentsOverview.tsx
  - britv3.0/src/components/dashboard/provider/StripeConnectOnboarding.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/payments/[id]/page.tsx
  - britv3.0/src/components/dashboard/provider/TransactionDetail.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/billing/page.tsx
  - britv3.0/src/components/dashboard/provider/SubscriptionBilling.tsx
autonomous: false
requirements:
  - TPD-17
  - TPD-18
  - TPD-23

must_haves:
  truths:
    - "Payments Overview shows Stripe Connect balance (available + pending) from real Stripe API"
    - "Stripe Connect Express onboarding starts OAuth flow when provider clicks Connect"
    - "Payout history table shows real stripe.payouts.list() results"
    - "Individual Transaction page shows charge details from Stripe with related invoice"
    - "Billing page shows current subscription plan with upgrade table"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/payments/page.tsx"
      provides: "Payments Overview page (11.17) — Stripe balance, payouts, quick stats"
    - path: "britv3.0/src/components/dashboard/provider/PaymentsOverview.tsx"
      provides: "Balance display, payout history table, earnings stats"
    - path: "britv3.0/src/components/dashboard/provider/StripeConnectOnboarding.tsx"
      provides: "Connect Stripe banner + OAuth redirect initiation"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/payments/[id]/page.tsx"
      provides: "Individual Transaction page (11.18)"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/billing/page.tsx"
      provides: "Subscription & Billing page (11.23)"
  key_links:
    - from: "britv3.0/src/components/dashboard/provider/StripeConnectOnboarding.tsx"
      to: "britv3.0/src/app/api/stripe/connect/create-account/route.ts"
      via: "Client component fetches onboarding URL then redirects"
      pattern: "fetch.*stripe/connect"
---

<objective>
Build the Payments Overview, Individual Transaction, and Subscription & Billing pages — connecting to real Stripe APIs.

Purpose: Tradespeople need to see their earnings and manage payouts. Stripe Connect is the payment backbone.
Output: 3 pages + 3 components covering TPD-17, TPD-18, and TPD-23.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-04-SUMMARY.md
@.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch reference (read for analytics/payouts layout):
@.planning/phases/16-tradesperson-dashboard/stitch/analytics-payouts.html

<interfaces>
From provider-payment-service.ts (Plan 04):
- getStripeConnectAccount(supabase, providerId): StripeConnectAccount | null
- getStripeBalance(stripeAccountId): { available_pence, pending_pence }
- getPayoutHistory(stripeAccountId, limit): StripePayout[]
- getTransactionDetail(stripeAccountId, chargeId): StripeCharge with metadata

StripeConnectAccount: { stripe_account_id, onboarding_complete, charges_enabled, payouts_enabled }

Stripe API types (from stripe SDK):
- Stripe.Balance: available[].amount (in pence), pending[].amount
- Stripe.Payout: id, amount, arrival_date, status, failure_message?
- Stripe.Charge: id, amount, description, created, status, metadata { booking_id? }

Existing API routes (Plan 04):
- POST /api/stripe/connect/create-account → returns { onboarding_url }
- POST /api/stripe/connect/onboarding-link → returns { url }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payments Overview (11.17) + Individual Transaction (11.18)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/payments/page.tsx,
    britv3.0/src/components/dashboard/provider/PaymentsOverview.tsx,
    britv3.0/src/components/dashboard/provider/StripeConnectOnboarding.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/payments/[id]/page.tsx,
    britv3.0/src/components/dashboard/provider/TransactionDetail.tsx
  </files>
  <action>
    Reference stitch/analytics-payouts.html for payout history table and balance card layout.

    **Payments page.tsx** (Server Component):
    - Fetch getStripeConnectAccount(supabase, providerId)
    - If account is null OR onboarding_complete is false: render StripeConnectOnboarding banner (full-width prompt to connect)
    - If connected + onboarded: fetch balance and payout history in parallel; pass to PaymentsOverview

    **StripeConnectOnboarding.tsx** ("use client"):
    - Full-width banner with brand-primary bg
    - "Connect your bank account to receive payments" heading, description text
    - "Connect Stripe" button: onClick → fetch POST /api/stripe/connect/create-account → receive { onboarding_url } → window.location.href = onboarding_url
    - Loading state during fetch
    - Note below button: "Powered by Stripe — your details are secure"
    - Add Stripe logo SVG (inline, small, grey)

    **PaymentsOverview.tsx** ("use client" for period selector interaction):
    Props: { balance: { available_pence, pending_pence }, payouts: StripePayout[], earnings: { this_month_pence, last_month_pence } }

    Layout (reference stitch/analytics-payouts.html):
    1. Balance cards row (2 cards): "Available Balance £{available}" + "Pending £{pending}"
       - Available: bg-[#1B4D3E] text-white card, large amount
       - Pending: white card, neutral text
    2. Earnings summary row: This Month, Last Month (from provider_invoices sums)
    3. Payout History table:
       - Columns: Date, Amount, Status, Arrival Date, Actions
       - Status badges: 'paid' = success green, 'pending' = amber, 'failed' = error red
       - Failed payouts show failure_message in tooltip
       - Each row links to /payments/{payout.id}
    4. "Request Payout" button (if available_pence > 0): calls POST /api/stripe/connect/payout Server Action

    **Individual Transaction page.tsx** (Server Component):
    - Extract `id` from route params (Stripe charge ID or payout ID)
    - Call getTransactionDetail(stripeAccountId, chargeId) to get charge details
    - Also fetch linked provider_invoices row by metadata.booking_id (if present)

    **TransactionDetail.tsx** (Server Component):
    Props: { transaction: StripeCharge, invoice?: ProviderInvoice }
    - Header: transaction ID, date, status badge
    - Amount breakdown:
      - Gross amount: £{amount/100}
      - Stripe fee: (if available in balance_transaction)
      - Platform fee (Britestate 2.5%): computed
      - Net payout: gross - fees
    - Related booking: link to /jobs/{booking_id} if metadata contains booking_id
    - Related invoice: if invoice exists, show invoice_number + link to download PDF
    - Status history (Stripe charge status timeline)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Payments overview shows onboarding banner if not connected; shows balance + payout table when connected; transaction detail shows fee breakdown</done>
</task>

<task type="auto">
  <name>Task 2: Subscription & Billing (11.23)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/billing/page.tsx,
    britv3.0/src/components/dashboard/provider/SubscriptionBilling.tsx
  </files>
  <action>
    **Billing page.tsx** (Server Component):
    - Fetch current subscription from provider_subscriptions (if table exists from Plan 01) or stripe customer API
    - Pass subscription data and billing history to SubscriptionBilling

    **SubscriptionBilling.tsx** ("use client"):
    Props: { currentPlan: 'free'|'starter'|'professional'|'premium', billingHistory: BillingRecord[] }

    Layout sections:
    1. **Current Plan Banner**: Plan name badge, monthly/annual toggle, renewal date
       - Free plan: neutral bg, "Upgrade to unlock more leads"
       - Paid plans: brand-primary-lighter bg, plan features checklist
    2. **Plan Comparison Table**: 3 plan columns (Starter £29/mo, Professional £79/mo, Premium £149/mo)
       Feature rows: max leads/month, badge visibility, analytics depth, priority support, featured listing boost credits
       Current plan column: highlighted with bg-[#E8F5EE] border-[#1B4D3E]
       Upgrade CTA per column: "Upgrade to {plan}" button → creates Stripe Checkout session
    3. **Payment Method**: shows masked card (last 4 digits), expiry, "Update Payment Method" button
       Update button: opens Stripe Customer Portal via Stripe.js (use @stripe/stripe-js)
    4. **Billing History table**: invoice date, amount, status (paid/unpaid), "Download" link for each
       - Data from Stripe invoices API or provider_invoices table

    Plan data (hardcoded — no DB lookup needed for plan features table):
    - Free: 5 leads/month, basic profile, no analytics
    - Starter (£29/mo): 25 leads, standard profile, basic analytics
    - Professional (£79/mo): 100 leads, verified badge priority, full analytics, 2 boost credits
    - Premium (£149/mo): unlimited leads, top placement, full analytics, 5 boost credits, priority support

    Server Action for Stripe Checkout:
    - createSubscriptionCheckout(providerId, planId): Creates Stripe Checkout Session with mode='subscription'
    - Returns { checkout_url } — client redirects to it
    - Add TODO comment: "Connect stripe.subscriptions API to provider_subscriptions table on webhook receipt"
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Billing page shows current plan with feature comparison table; Stripe portal integration for payment method update; billing history table visible</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 errors
2. `cd britv3.0 && pnpm lint` — 0 errors
3. Provider without Stripe account sees onboarding banner on Payments page
4. Plan comparison table shows 3 columns with current plan highlighted
5. Stripe secret key is only used in server-side code (not imported in client components)
</verification>

<success_criteria>
- Payments overview conditionally shows onboarding vs real balance based on stripe_connect_accounts table
- Balance fetched from real Stripe API (not mocked) when account is connected
- Subscription plan comparison table is visually clear with current plan highlighted
- No Stripe secret key usage in client-side code
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-10-SUMMARY.md`
</output>

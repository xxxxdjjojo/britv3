---
phase: 16-tradesperson-dashboard
plan: "08"
type: execute
wave: 4
depends_on:
  - "16-03"
  - "16-05"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/jobs/leads/page.tsx
  - britv3.0/src/components/dashboard/provider/JobLeadCard.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/jobs/active/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/jobs/completed/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/jobs/[id]/page.tsx
  - britv3.0/src/components/dashboard/provider/JobDetailView.tsx
  - britv3.0/src/components/dashboard/provider/JobTimeline.tsx
autonomous: false
requirements:
  - TPD-11
  - TPD-12
  - TPD-13
  - TPD-14

must_haves:
  truths:
    - "New Leads page shows incoming service_requests matching provider's categories with accept/decline/message actions"
    - "Supabase Realtime subscription on service_requests notifies provider of new leads without page refresh"
    - "Active Jobs page shows bookings with status=in_progress and days_running badge"
    - "Completed Jobs page supports text search and shows review prompt for unreviewed jobs"
    - "Job Detail shows full scope, message thread, timeline, quote summary, invoice status, and review"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/jobs/leads/page.tsx"
      provides: "New Enquiries/Leads page (11.11) with real-time updates"
    - path: "britv3.0/src/components/dashboard/provider/JobLeadCard.tsx"
      provides: "Lead card with accept/decline/message actions and urgency badge"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/jobs/[id]/page.tsx"
      provides: "Job Detail page (11.14) — full job view"
    - path: "britv3.0/src/components/dashboard/provider/JobDetailView.tsx"
      provides: "Job detail panel: timeline, quote, invoice, review sections"
  key_links:
    - from: "britv3.0/src/app/(protected)/dashboard/provider/jobs/leads/page.tsx"
      to: "britv3.0/src/services/provider/provider-job-service.ts"
      via: "Server Component calls getProviderLeads; client sets up Realtime subscription"
      pattern: "getProviderLeads"
    - from: "britv3.0/src/app/(protected)/dashboard/provider/jobs/[id]/page.tsx"
      to: "britv3.0/src/services/provider/provider-job-service.ts"
      via: "Server Component calls getJobDetail with bookingId"
      pattern: "getJobDetail"
---

<objective>
Build the 4 Job Management pages — New Leads, Active Jobs, Completed Jobs, and Job Detail.

Purpose: Jobs are the core daily workflow. New leads with real-time Realtime subscription ensures tradespeople never miss an opportunity.
Output: 4 pages + 3 components covering TPD-11 through TPD-14.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-03-SUMMARY.md
@.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch references (read for exact layout):
@.planning/phases/16-tradesperson-dashboard/stitch/job-leads-enquiries.html
@.planning/phases/16-tradesperson-dashboard/stitch/job-board.html

<interfaces>
From provider-job-service.ts (Plan 03):
- getProviderLeads(supabase, providerId, filters?): ServiceRequest[] with client info
- getActiveJobs(supabase, providerId): Booking[] with days_running, service_request title
- getCompletedJobs(supabase, providerId, search?, page?, pageSize?): paginated Booking[]
- getJobDetail(supabase, providerId, bookingId): full job object with booking, service_request, client, quote, invoice?, review?
- acceptLead(supabase, providerId, requestId): updates status to 'accepted'
- declineLead(supabase, providerId, requestId): updates status to 'cancelled'

ServiceRequest shape: { id, title, description, category, budget_range_min, budget_range_max, postcode, urgency_level, status, created_at, client: { name, avatar_url } }
Booking shape with days_running: { id, status, scheduled_date, total_amount, days_running, service_request: { title, description }, client: { name } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New Leads (11.11) + Active Jobs (11.12) + Completed Jobs (11.13)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/jobs/leads/page.tsx,
    britv3.0/src/components/dashboard/provider/JobLeadCard.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/jobs/active/page.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/jobs/completed/page.tsx
  </files>
  <action>
    Reference stitch/job-leads-enquiries.html and stitch/job-board.html for exact layout.

    **New Leads page.tsx** (Server Component shell + client for Realtime):
    - Server: fetch getProviderLeads for initial data, pass to JobLeadsClient
    - JobLeadsClient ("use client"):
      - Renders filter bar (All | category tabs — matching Stitch filter row with category icons)
      - Maps leads to JobLeadCard components
      - Sets up Supabase Realtime subscription:
        ```typescript
        const channel = supabase.channel('provider-leads')
          .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'service_requests' },
            (payload) => {
              // Check if matches provider's categories; if yes, prepend to leads list
              // Show sonner toast: "New lead: {title}"
            })
          .subscribe();
        ```
      - Cleanup: `return () => supabase.removeChannel(channel)` in useEffect

    **JobLeadCard.tsx** ("use client"):
    Props: { lead: ServiceRequest, onAccept: () => Promise<void>, onDecline: () => Promise<void>, onMessage: () => void }
    Reference stitch/job-leads-enquiries.html card structure:
    - Card layout: client avatar (left), job title + description excerpt (middle), budget + location + category icon (right)
    - Urgency badge: 'urgent' = red bg-[#FEE2E2] text-[#DC2626], 'high' = amber, 'medium' = neutral, 'low' = green
    - Action row: "Accept Lead" (primary button, bg-[#1B4D3E] text-white), "Decline" (outline), "Message" (ghost)
    - Accept → calls acceptLead Server Action → shows success toast → removes card from list
    - Decline → confirmation dialog → calls declineLead → removes card from list
    - Budget display: "£{min}–£{max}" or "Budget: £{budget}" format
    - Posted time: relative ("2h ago") using Date diff calculation

    **Active Jobs page.tsx** (Server Component):
    - Calls getActiveJobs(supabase, providerId)
    - Table layout on desktop, card stack on mobile
    - Columns: Job Title (links to /jobs/{id}), Client, Started, Days Running (badge — amber if > 7 days), Status badge, Total Value
    - Days Running badge: green if ≤ 3d, amber if 4-7d, red if > 7d
    - Status badges: 'confirmed' = blue, 'in_progress' = amber
    - Empty state: "No active jobs" with link to leads page

    **Completed Jobs page.tsx** (Server Component with search):
    - URL search param: `?q=` for text search, `?page=` for pagination
    - Calls getCompletedJobs with search and page params
    - Table: Job Title, Client, Completed Date, Total Amount, Review status (star rating or "No review yet")
    - Review prompt: if no review, show "Request Review" button → sends review request email
    - Pagination: prev/next buttons
    - Export button: downloads CSV of completed jobs (client-side, format as `completedJobs.csv`)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>3 job list pages render with real data; new leads has filter tabs and Realtime subscription; active jobs shows days_running badge; completed jobs has search and pagination</done>
</task>

<task type="auto">
  <name>Task 2: Job Detail (11.14)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/jobs/[id]/page.tsx,
    britv3.0/src/components/dashboard/provider/JobDetailView.tsx,
    britv3.0/src/components/dashboard/provider/JobTimeline.tsx
  </files>
  <action>
    **Job Detail page.tsx** (Server Component):
    - Extract `id` param from route params (this is the booking id)
    - Call getJobDetail(supabase, providerId, bookingId) — throws 404 if booking not found or doesn't belong to provider
    - Pass full job object to JobDetailView client component

    **JobDetailView.tsx** ("use client"):
    Props: { job: JobDetail } where JobDetail = { booking, serviceRequest, client, quote?, invoice?, review?, messages: Message[] }
    Layout: two-column on desktop (main left, sidebar right)

    Left column sections:
    1. **Header**: Job title, status badge (colored per status), client name + avatar, posted date
    2. **Scope of Work**: service_request description, budget, location (postcode), category, urgency
    3. **Message Thread**: scrollable message history, compose box at bottom. Use existing conversation/messages tables from Phase 3/4. New messages call the existing messaging API endpoint.
    4. **JobTimeline**: visual step-by-step timeline of booking status changes

    Right column (sidebar):
    1. **Job Status Panel**: current status + allowed next status transitions. Status change button (e.g., "Mark In Progress", "Mark Complete")
    2. **Quote Summary**: if quote exists, show total, line item count, "View Full Quote" link. If no quote, "Create Quote" link to /quotes/builder?request_id={id}.
    3. **Invoice**: if invoice exists, show invoice_number, total, status (paid/sent/overdue). "View Invoice" link. If complete booking and no invoice, "Generate Invoice" link.
    4. **Review**: if review exists, show star rating + comment + response (if any). If no review and booking complete, "Request Review" button.
    5. **Payment Status**: integration with invoice status — shows "Awaiting Payment", "Paid £{amount}", or "Overdue"

    **JobTimeline.tsx** (Server Component — no interactivity):
    Props: { booking: Booking }
    - Vertical timeline showing status transitions
    - Each step: circle indicator (filled for completed, outline for future), label, timestamp
    - Status sequence: Enquiry → Quote Sent → Booking Confirmed → In Progress → Completed
    - Completed steps: bg-[#1B4D3E] circle, solid line connector
    - Future steps: neutral-300 circle, dashed line connector

    Status transition logic in JobDetailView:
    - 'pending' → allowed: 'confirmed', 'cancelled'
    - 'confirmed' → allowed: 'in_progress', 'cancelled'
    - 'in_progress' → allowed: 'completed'
    - 'completed' → no transitions
    - Transition calls POST /api/bookings/{id}/status with new status (existing booking API from Phase 4)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Job detail page shows scope, messages, timeline, quote/invoice/review panels; status transitions work; 404 returned for jobs not belonging to provider</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 errors
2. `cd britv3.0 && pnpm lint` — 0 errors
3. New leads page shows card-based lead inbox matching Stitch layout
4. Accepting a lead updates the service_request status
5. Job detail page loads all sections for a known booking ID
</verification>

<success_criteria>
- Supabase Realtime subscription set up on service_requests table — new leads appear without page refresh
- Accept/decline actions update database and remove card from list
- Job detail shows all 5 right-sidebar panels (status, quote, invoice, review, payment)
- Status transitions are restricted to allowed next states only
- No mock data arrays remain — all data from Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-08-SUMMARY.md`
</output>

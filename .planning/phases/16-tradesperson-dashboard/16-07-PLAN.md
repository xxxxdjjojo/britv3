---
phase: 16-tradesperson-dashboard
plan: "07"
type: execute
wave: 3
depends_on:
  - "16-02"
  - "16-05"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/services/page.tsx
  - britv3.0/src/components/dashboard/provider/ServiceCard.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/services/areas/page.tsx
  - britv3.0/src/components/dashboard/provider/ServiceAreaMapEditor.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/availability/page.tsx
  - britv3.0/src/components/dashboard/provider/AvailabilityCalendar.tsx
autonomous: false
requirements:
  - TPD-08
  - TPD-09
  - TPD-10

must_haves:
  truths:
    - "Services page shows all provider_services rows with add/edit/delete capability"
    - "Service Areas page renders a MapLibre map with terra-draw polygon and circle drawing modes"
    - "Drawing a zone on the map and saving persists GeoJSON to provider_service_areas in Supabase"
    - "Availability Calendar shows booked dates from bookings table and allows marking blocked dates"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/services/page.tsx"
      provides: "Manage Services page — list, add, edit, delete services (11.8)"
    - path: "britv3.0/src/components/dashboard/provider/ServiceCard.tsx"
      provides: "Editable service card with pricing type and inline edit"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/services/areas/page.tsx"
      provides: "Service Areas Map Editor (11.9)"
    - path: "britv3.0/src/components/dashboard/provider/ServiceAreaMapEditor.tsx"
      provides: "MapLibre + terra-draw canvas for drawing service coverage zones"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/availability/page.tsx"
      provides: "Availability Calendar (11.10) — monthly view with bookings and blocked dates"
  key_links:
    - from: "britv3.0/src/components/dashboard/provider/ServiceAreaMapEditor.tsx"
      to: "britv3.0/src/app/api/provider/service-areas/route.ts"
      via: "Client component POSTs GeoJSON to API route which saves to provider_service_areas"
      pattern: "fetch.*service-areas"
---

<objective>
Build the Services & Availability section — services management, interactive service area map editor, and availability calendar.

Purpose: These pages define the provider's offering. Without service areas, the lead matching system cannot match providers to RFQs.
Output: 3 pages + 3 components covering TPD-08 through TPD-10.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-02-SUMMARY.md
@.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch reference (read for layout and map UI):
@.planning/phases/16-tradesperson-dashboard/stitch/availability-service-area.html

<interfaces>
From provider-dashboard-service.ts area (Plan 02):
- getProviderServices(supabase, providerId): returns ProviderService[]
- createProviderService(supabase, providerId, input): inserts provider_services row
- updateProviderService(supabase, providerId, serviceId, updates): updates row
- deleteProviderService(supabase, providerId, serviceId): deletes row
- getServiceAreas(supabase, providerId): returns ProviderServiceArea[]
- saveServiceArea(supabase, providerId, area: { area_type, label, geometry, radius_km? }): upsert

From Phase 4 existing:
- provider_availability table: id, provider_id, date, is_available, reason
- bookings table: provider_id, scheduled_date, status

Terra-draw integration (from research):
- import { TerraDraw, TerraDrawMapLibreGLAdapter } from "terra-draw"
- import { TerraDrawCircleMode, TerraDrawPolygonMode, TerraDrawSelectMode } from "terra-draw"
- Always "use client" — map interaction requires browser
- Access map via useMap() from @vis.gl/react-maplibre

ServiceCategory enum values (from Phase 3/4):
'plumbing', 'electrical', 'gas', 'carpentry', 'plastering', 'painting', 'roofing', 'flooring', 'landscaping', 'general_maintenance', 'cleaning', 'moving', 'conveyancing', 'surveying', 'mortgage_advice'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Services Manage (11.8) + Service Areas Map Editor (11.9)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/services/page.tsx,
    britv3.0/src/components/dashboard/provider/ServiceCard.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/services/areas/page.tsx,
    britv3.0/src/components/dashboard/provider/ServiceAreaMapEditor.tsx,
    britv3.0/src/app/api/provider/service-areas/route.ts
  </files>
  <action>
    **Services page.tsx** (Server Component shell):
    - Fetch getProviderServices (returns ProviderService[]) server-side
    - Pass to ServicesClient ("use client" wrapper for add/edit/delete interactions)
    - "Add Service" button opens a dialog form: name, category (select from ServiceCategory), description (textarea), pricing_type (radio: hourly/fixed/quote_on_request), price_amount (number, shown only for hourly/fixed)
    - Service list renders ServiceCard per service

    **ServiceCard.tsx** ("use client"):
    Props: { service: ProviderService, onEdit: () => void, onDelete: () => void }
    - Card layout: service name (heading), category pill badge, pricing display (e.g., "£45/hr", "£200 fixed", "Quote on Request")
    - Edit button opens pre-filled dialog. Delete button shows confirmation.
    - Server Actions: createProviderService, updateProviderService, deleteProviderService (invalidate React Query cache on success)

    **Service Areas page.tsx** (Server Component shell + client map):
    - Fetch existing service areas via getServiceAreas
    - Render: page title, instructions text, ServiceAreaMapEditor below, saved areas list on the right

    **ServiceAreaMapEditor.tsx** ("use client" — MANDATORY, browser-only):
    - Wrap in dynamic import with ssr: false in the page (to avoid SSR hydration issues with MapLibre)
    - Map: use `<Map>` from @vis.gl/react-maplibre with MapTiler API key (NEXT_PUBLIC_MAPTILER_API_KEY)
    - Map style: `https://api.maptiler.com/maps/streets/style.json?key=${key}`
    - Initial center: UK centroid [51.5074, -0.1278], zoom: 8
    - Terra-draw setup:
      ```typescript
      const draw = new TerraDraw({
        adapter: new TerraDrawMapLibreGLAdapter({ map }),
        modes: [
          new TerraDrawPolygonMode(),
          new TerraDrawCircleMode(),
          new TerraDrawSelectMode({ flags: { polygon: { feature: { draggable: true } } } })
        ]
      });
      draw.start();
      ```
    - Toolbar above map: "Draw Area" (polygon mode), "Draw Radius" (circle mode), "Select" (select mode), "Delete Selected" button
    - Active mode indicator: active button gets brand-primary bg
    - On complete drawing: draw.on("finish", (id, context) => { setDraftZone(draw.getFeatureById(id)) })
    - Save zone button: posts GeoJSON feature to /api/provider/service-areas, shows sonner toast, re-fetches areas list
    - Existing zones rendered as filled GeoJSON layers on the map in brand-primary-lighter with brand-primary stroke

    **API route service-areas/route.ts** (POST handler):
    - Auth guard + provider role check
    - Accept: { area_type, label, geometry, radius_km? }
    - Validate geometry is valid GeoJSON (check type = 'Feature' or 'FeatureCollection')
    - Insert via saveServiceArea service function
    - Returns 201 with created area

    Colour mapping:
    - Map drawn zones: fill `rgba(27, 77, 62, 0.15)`, stroke `#1B4D3E`, strokeWidth 2
    - Toolbar buttons: bg-white border-neutral-200, active bg-[#1B4D3E] text-white
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Services page lists provider_services with add/edit/delete; service areas page renders MapLibre map with terra-draw toolbar; pnpm build passes</done>
</task>

<task type="auto">
  <name>Task 2: Availability Calendar (11.10)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/availability/page.tsx,
    britv3.0/src/components/dashboard/provider/AvailabilityCalendar.tsx,
    britv3.0/src/app/api/provider/availability/route.ts
  </files>
  <action>
    Reference stitch/availability-service-area.html for the calendar layout section.

    **Availability page.tsx** (Server Component):
    - Fetch bookings for provider (confirmed + in_progress) from bookings table for current + next 2 months
    - Fetch provider_availability rows for same period
    - Pass both to AvailabilityCalendar client component

    **AvailabilityCalendar.tsx** ("use client"):
    Props: { bookings: UpcomingJob[], availability: ProviderAvailability[], providerId: string }
    - Month/week toggle buttons (default: month view)
    - Month view: CSS grid (7 columns), date cells for the current month
    - Date cell states (apply appropriate bg colors):
      - Available: white bg, subtle border
      - Blocked by provider: bg-neutral-100, diagonal stripe texture (CSS), tooltip "Marked unavailable"
      - Booked job: bg-[#E8F5EE] border-[#1B4D3E], job title truncated, links to job detail
      - Today: ring-2 ring-[#1B4D3E]
    - Click empty available date: toggle to blocked (calls POST /api/provider/availability with is_available=false)
    - Click blocked date: toggle back to available (calls PATCH)
    - Booked dates NOT toggleable (prevents accidental unblock of confirmed jobs)
    - Week view: 7-column grid with time slots (8am-6pm, 1-hour rows)
    - Navigation: prev/next month buttons, "Today" reset button
    - Legend: colour key for Available/Blocked/Booked states

    **API route availability/route.ts**:
    - POST: auth guard, upsert provider_availability row ({ provider_id, date, is_available, reason? })
    - GET: auth guard, return provider_availability for given month (query param: year, month)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "^.*error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Availability calendar renders month grid with correct date cell states; clicking available dates marks them blocked; booked job dates are read-only; pnpm build passes</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 errors
2. `cd britv3.0 && pnpm lint` — 0 errors
3. Services page renders list of provider services with add button
4. Service areas page loads MapLibre map (visually verified)
5. Availability calendar shows month grid with today highlighted
</verification>

<success_criteria>
- Services CRUD operations work end-to-end (add → appears in list, delete → removed)
- ServiceAreaMapEditor has polygon and circle drawing modes from terra-draw
- Drawing a zone and saving creates a provider_service_areas row
- Availability calendar distinguishes between booked (from bookings table) and blocked (from provider_availability)
- No SSR errors from MapLibre (dynamic import with ssr:false)
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-07-SUMMARY.md`
</output>

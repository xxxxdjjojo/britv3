---
phase: 16-tradesperson-dashboard
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - britv3.0/src/services/provider/provider-dashboard-service.ts
  - britv3.0/src/services/provider/provider-verification-service.ts
  - britv3.0/src/services/provider/provider-profile-service.ts
autonomous: true
requirements:
  - TPD-01
  - TPD-02
  - TPD-03
  - TPD-04
  - TPD-05
  - TPD-06
  - TPD-07

must_haves:
  truths:
    - "getProviderDashboardStats returns real KPI counts from Supabase"
    - "getVerificationSteps returns ordered steps with completion status"
    - "sendReferenceRequest saves a pending provider_reference row and queues email via Inngest"
    - "updateProviderProfile persists bio, qualifications, and avatar path changes"
  artifacts:
    - path: "britv3.0/src/services/provider/provider-dashboard-service.ts"
      provides: "getProviderDashboardStats, getRecentActivity, getUpcomingJobs"
      exports:
        - getProviderDashboardStats
        - getRecentActivity
        - getUpcomingJobs
    - path: "britv3.0/src/services/provider/provider-verification-service.ts"
      provides: "getVerificationSteps, sendReferenceRequest, getProviderBadges"
      exports:
        - getVerificationSteps
        - sendReferenceRequest
        - getProviderBadges
        - getProviderReferences
    - path: "britv3.0/src/services/provider/provider-profile-service.ts"
      provides: "updateProviderProfile, getProviderProfile, uploadAvatar"
      exports:
        - getProviderProfile
        - updateProviderProfile
        - uploadAvatar
  key_links:
    - from: "britv3.0/src/services/provider/provider-dashboard-service.ts"
      to: "britv3.0/src/types/provider-dashboard.ts"
      via: "return types use ProviderDashboardStats, RecentActivityItem, UpcomingJob"
      pattern: "ProviderDashboardStats"
---

<objective>
Build the service layer for the dashboard home, profile edit, and verification centre pages — the three most foundational UI pages.

Purpose: Wave 3 UI plans cannot render real data without these service functions. Services use Phase 3+4 tables and the new Phase 16 tables from Plan 01.
Output: 3 service files covering TPD-01 through TPD-07.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-RESEARCH.md
@.planning/phases/16-tradesperson-dashboard/16-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 output -->
From britv3.0/src/types/provider-dashboard.ts:
- ProviderDashboardStats { new_leads_count, active_jobs_count, pending_reviews_count, monthly_earnings_pence, verification_complete_pct }
- RecentActivityItem { id, type, title, description, created_at, link? }
- UpcomingJob { id, title, client_name, scheduled_date, status, total_amount_pence }
- ProviderReference { id, provider_id, reference_type, referee_name, referee_email, status, requested_at, ... }
- ProviderBadge { id, provider_id, badge_type, badge_label, description, earned_at, expires_at, is_active }

From britv3.0/src/types/database.ts (Phase 3+4):
- ServiceProviderDetails { id, user_id, business_name, services, description, hourly_rate, ... }
- VerificationDocumentType enum
- BookingStatus enum

Existing services to extend:
- britv3.0/src/services/provider-service.ts: createProviderProfile, updateProviderProfile, uploadProviderDocument
- britv3.0/src/services/booking-service.ts: getBookings (filter by provider_id + status)
- britv3.0/src/services/review-service.ts: getReviews (filter by reviewee_id)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: provider-dashboard-service — KPIs, activity feed, upcoming jobs</name>
  <files>
    britv3.0/src/services/provider/provider-dashboard-service.ts
  </files>
  <behavior>
    - getProviderDashboardStats(supabase, providerId): queries service_requests (new leads matching provider), bookings (active/completed), reviews (pending response), and provider_invoices (this month earnings) — returns ProviderDashboardStats shape
    - getRecentActivity(supabase, providerId, limit=10): queries latest 10 events from service_requests, bookings, and reviews relevant to this provider, ordered by created_at DESC — returns RecentActivityItem[]
    - getUpcomingJobs(supabase, providerId, limit=5): queries bookings WHERE provider_id = providerId AND status IN ('confirmed', 'in_progress') AND scheduled_date >= now() ORDER BY scheduled_date ASC — returns UpcomingJob[]
    - All functions accept a Supabase server client and providerId string
    - All functions return typed objects (no `any`)
    - Earnings are calculated in pence (integer) to avoid floating point issues
  </behavior>
  <action>
    Create britv3.0/src/services/provider/provider-dashboard-service.ts:

    1. Import SupabaseClient from @supabase/supabase-js and types from @/types/provider-dashboard
    2. getProviderDashboardStats(supabase, providerId): Use Promise.all to run 4 parallel queries:
       - Count service_requests WHERE status = 'open' and category matches provider's services (join via service_provider_details)
       - Count bookings WHERE provider_id = providerId AND status = 'in_progress'
       - Count reviews WHERE reviewee_id = providerId AND provider_response IS NULL
       - Sum provider_invoices WHERE provider_id = providerId AND status = 'paid' AND paid_at >= start of current month
       - Calculate verification_complete_pct by counting completed verification steps
    3. getRecentActivity(supabase, providerId, limit): Query bookings, service_requests, and reviews in parallel, merge results by created_at, return most recent `limit` items as RecentActivityItem[]
    4. getUpcomingJobs(supabase, providerId, limit): Single query on bookings JOIN service_requests for job title, JOIN auth.users for client display name
    5. Handle empty/null results gracefully — return zero values, not errors
    6. Export all 3 functions as named exports

    Update the test stubs in __tests__/provider-dashboard-service.test.ts to GREEN (make them pass).
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm tsc --noEmit 2>&1 | grep provider-dashboard-service | wc -l</automated>
  </verify>
  <done>provider-dashboard-service.ts exports getProviderDashboardStats, getRecentActivity, getUpcomingJobs with correct TypeScript return types; tsc --noEmit passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provider-verification-service + provider-profile-service</name>
  <files>
    britv3.0/src/services/provider/provider-verification-service.ts,
    britv3.0/src/services/provider/provider-profile-service.ts
  </files>
  <behavior>
    provider-verification-service:
    - getVerificationSteps(supabase, providerId): Returns ordered array of 5 verification step objects: { id, label, description, status: 'complete'|'in_progress'|'pending', documents?, references? }. Derives status by checking provider_documents (for identity/insurance/qualifications steps) and provider_references (for client_refs/peer_refs steps)
    - getProviderReferences(supabase, providerId, type: 'client'|'peer'): Returns ProviderReference[] filtered by reference_type
    - sendReferenceRequest(supabase, providerId, input: { referee_name, referee_email, reference_type }): Inserts a provider_references row with status='pending', then triggers Inngest event 'provider/reference.requested' (Inngest event only — email template handled by Inngest function)
    - getProviderBadges(supabase, providerId): Returns ProviderBadge[] with is_active=true, ordered by earned_at DESC
    - uploadVerificationDocument(supabase, providerId, documentType, file): Calls Supabase Storage upload to 'provider-documents' bucket path `{providerId}/{documentType}/{filename}`, then upserts a provider_documents row

    provider-profile-service:
    - getProviderProfile(supabase, userId): Fetches full service_provider_details row JOIN user_roles for the given user_id
    - updateProviderProfile(supabase, providerId, updates: Partial<ServiceProviderDetails>): Updates service_provider_details row, validates business_name length 2-100 chars, description max 2000 chars
    - uploadAvatar(supabase, userId, file: File): Uploads to 'avatars' bucket at path `providers/{userId}/avatar`, returns public URL
  </behavior>
  <action>
    Create britv3.0/src/services/provider/provider-verification-service.ts and britv3.0/src/services/provider/provider-profile-service.ts following the patterns above.

    For provider-verification-service.ts:
    - VERIFICATION_STEPS constant defines the 5 steps: identity, insurance, qualifications, client_references, peer_references
    - Each step maps to VerificationDocumentType values from the existing enum
    - getVerificationSteps derives overall completion percentage: (completed_steps / 5) * 100
    - sendReferenceRequest validates referee_email format with zod before insert
    - uploadVerificationDocument validates file type (PDF, JPEG, PNG only) and size (max 10MB) using the existing file-validator utility from Phase 4

    For provider-profile-service.ts:
    - Reuses the existing uploadProviderDocument pattern from provider-service.ts
    - updateProviderProfile uses zod schema for validation before DB update
    - uploadAvatar uses supabase.storage.from('avatars').upload() then .getPublicUrl()

    Update test stubs to GREEN for both services.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm tsc --noEmit 2>&1 | grep -E "provider-verification|provider-profile" | wc -l</automated>
  </verify>
  <done>Both service files export all required functions; TypeScript types are correct; test stubs pass; pnpm tsc --noEmit reports 0 errors for these files</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd britv3.0 && pnpm tsc --noEmit` — 0 TypeScript errors
2. `cd britv3.0 && pnpm lint` — 0 lint errors
3. `cd britv3.0 && pnpm test --run src/services/provider` — test stubs pass (GREEN)
</verification>

<success_criteria>
- 3 service files created: provider-dashboard-service.ts, provider-verification-service.ts, provider-profile-service.ts
- All exported functions have correct TypeScript signatures matching types from Plan 01
- Test stubs in __tests__/ directory turn GREEN
- pnpm build produces 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-02-SUMMARY.md`
</output>

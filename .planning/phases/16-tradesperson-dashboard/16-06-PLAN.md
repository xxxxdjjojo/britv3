---
phase: 16-tradesperson-dashboard
plan: "06"
type: execute
wave: 3
depends_on:
  - "16-02"
  - "16-05"
files_modified:
  - britv3.0/src/app/(protected)/dashboard/provider/verification/page.tsx
  - britv3.0/src/components/dashboard/provider/VerificationStepper.tsx
  - britv3.0/src/components/dashboard/provider/TrustScoreGauge.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/verification/credentials/page.tsx
  - britv3.0/src/components/dashboard/provider/CredentialUploadCard.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/verification/client-references/page.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/verification/peer-references/page.tsx
  - britv3.0/src/components/dashboard/provider/ReferenceTracker.tsx
  - britv3.0/src/app/(protected)/dashboard/provider/verification/badges/page.tsx
  - britv3.0/src/components/dashboard/provider/BadgeGallery.tsx
autonomous: false
requirements:
  - TPD-03
  - TPD-04
  - TPD-05
  - TPD-06
  - TPD-07

must_haves:
  truths:
    - "Verification Overview shows 5-step progress stepper with correct completion status per step"
    - "Trust score circular gauge shows verification_complete_pct"
    - "Credentials page allows document upload with file type validation (PDF, JPEG, PNG, max 10MB)"
    - "Client References Tracker shows 3 references with send/remind/view actions"
    - "Badge Status page shows all earned badges with expiry warnings"
  artifacts:
    - path: "britv3.0/src/app/(protected)/dashboard/provider/verification/page.tsx"
      provides: "Verification Centre Overview with stepper and trust score (11.3)"
    - path: "britv3.0/src/components/dashboard/provider/VerificationStepper.tsx"
      provides: "5-step vertical stepper component with progress rings"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/verification/credentials/page.tsx"
      provides: "Upload Credentials page with per-document-type upload cards (11.4)"
    - path: "britv3.0/src/components/dashboard/provider/ReferenceTracker.tsx"
      provides: "Client + Peer reference list with send/remind/status display"
    - path: "britv3.0/src/app/(protected)/dashboard/provider/verification/badges/page.tsx"
      provides: "Badge Status page with earned badges grid and explanations (11.7)"
  key_links:
    - from: "britv3.0/src/app/(protected)/dashboard/provider/verification/page.tsx"
      to: "britv3.0/src/services/provider/provider-verification-service.ts"
      via: "Server Component calls getVerificationSteps"
      pattern: "getVerificationSteps"
---

<objective>
Implement the complete Verification Centre — 5 pages covering identity, credentials, references, and badges.

Purpose: Verification is a core trust-building flow. Tradespeople cannot access premium leads without completing it.
Output: 5 verification pages + 4 components covering TPD-03 through TPD-07.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-tradesperson-dashboard/16-CONTEXT.md
@.planning/phases/16-tradesperson-dashboard/16-02-SUMMARY.md
@.planning/phases/16-tradesperson-dashboard/16-05-SUMMARY.md
@/Users/joanflerinbig/Documents/britv3.0/britestatestyle.txt

Stitch reference (read for exact layout):
@.planning/phases/16-tradesperson-dashboard/stitch/verification-trust-center.html

<interfaces>
From provider-verification-service.ts (Plan 02):
- getVerificationSteps(supabase, providerId): Returns VerificationStep[] — { id, label, description, status: 'complete'|'in_progress'|'pending', step_number, documents?: ProviderDocument[], references?: ProviderReference[] }
- getProviderReferences(supabase, providerId, type: 'client'|'peer'): Returns ProviderReference[]
- sendReferenceRequest(supabase, providerId, { referee_name, referee_email, reference_type }): Inserts row + queues email
- uploadVerificationDocument(supabase, providerId, documentType, file): Returns updated ProviderDocument
- getProviderBadges(supabase, providerId): Returns ProviderBadge[]

VerificationDocumentType enum values (from Phase 4):
- 'public_liability_insurance', 'gas_safe_certificate', 'niceic_registration', 'napit_certificate', 'cscs_card', 'part_p_certificate', 'acs_qualification', 'identity_document'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification Overview (11.3) + Upload Credentials (11.4)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/verification/page.tsx,
    britv3.0/src/components/dashboard/provider/VerificationStepper.tsx,
    britv3.0/src/components/dashboard/provider/TrustScoreGauge.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/verification/credentials/page.tsx,
    britv3.0/src/components/dashboard/provider/CredentialUploadCard.tsx
  </files>
  <action>
    Reference stitch/verification-trust-center.html for exact layout.

    **Verification Overview page.tsx** (Server Component):
    - Calls getVerificationSteps(supabase, providerId)
    - Renders: page heading "Verification & Trust Centre", trust score gauge (right column), vertical stepper (left column), badge gallery (bottom section)
    - Page layout: two-column on desktop, single column on mobile. Left = stepper, right = trust score + action prompt.

    **VerificationStepper.tsx** (Server Component):
    Props: { steps: VerificationStep[] }
    - Vertical list of 5 steps with connecting line between them
    - Each step: circular progress ring (filled for complete, half for in_progress, empty for pending), step number, label, description, status badge
    - Complete step: ring bg-[#1B4D3E], checkmark icon inside, text-neutral-900
    - In-progress step: ring border-[#1B4D3E], dashed animation, text-[#1B4D3E]
    - Pending step: ring border-neutral-300, text-neutral-500
    - Each step has CTA link to relevant sub-page (credentials, client-references, peer-references)

    **TrustScoreGauge.tsx** ("use client" for animation):
    Props: { score: number, label: string }
    - Circular SVG gauge showing score percentage (0-100)
    - Track: neutral-200, filled arc: brand-primary (#1B4D3E)
    - Center text: score% in large bold, "Trust Score" label below
    - Animated fill using CSS stroke-dasharray/stroke-dashoffset
    - Score ranges: 0-40 = "Building", 40-70 = "Verified", 70-100 = "Trusted"

    **Upload Credentials page.tsx** (Server Component shell):
    - Fetch provider documents grouped by document_type
    - Render grid of CredentialUploadCard per document type

    **CredentialUploadCard.tsx** ("use client"):
    Props: { documentType: VerificationDocumentType, existingDocument?: ProviderDocument, onUpload: (file: File) => Promise<void> }
    - Card with document type label (human-readable), description, status badge (uploaded/pending/verified)
    - react-dropzone upload zone (accepts PDF, JPEG, PNG, max 10MB)
    - Upload state: loading spinner, error toast via sonner, success state
    - Calls uploadVerificationDocument Server Action on file selection
    - Shows existing document filename and upload date if already uploaded

    Colour tokens throughout:
    - Verified badges: bg-[#DCFCE7] text-[#16A34A] (success tokens)
    - Pending review: bg-[#FEF9C3] text-[#CA8A04] (warning tokens)
    - Upload zone hover: bg-[#E8F5EE] border-[#1B4D3E] dashed border
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Verification overview page renders VerificationStepper with 5 steps; TrustScoreGauge shows SVG arc; credentials page shows upload cards per document type</done>
</task>

<task type="auto">
  <name>Task 2: Client References (11.5) + Peer References (11.6) + Badge Status (11.7)</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/provider/verification/client-references/page.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/verification/peer-references/page.tsx,
    britv3.0/src/components/dashboard/provider/ReferenceTracker.tsx,
    britv3.0/src/app/(protected)/dashboard/provider/verification/badges/page.tsx,
    britv3.0/src/components/dashboard/provider/BadgeGallery.tsx
  </files>
  <action>
    **Client References page.tsx** + **Peer References page.tsx** (Server Component shells, both use ReferenceTracker):
    - Each page fetches getProviderReferences(supabase, providerId, 'client') or 'peer'
    - Renders ReferenceTracker with the references list
    - Shows "3 references required" progress indicator (N/3 submitted, M/3 verified)
    - Add Reference button opens a dialog to enter referee name + email

    **ReferenceTracker.tsx** ("use client"):
    Props: { references: ProviderReference[], referenceType: 'client'|'peer', providerId: string }
    - Renders list of up to 3 reference cards (or empty slots if fewer than 3)
    - Each reference card shows: referee name, email (partially masked), status badge (pending/submitted/verified), date requested
    - Pending status: "Send Request" button → calls sendReferenceRequest Server Action
    - Submitted status: "Send Reminder" button (re-triggers Inngest event), "View Reference" button (shows reference_text in dialog)
    - Verified status: green badge with verified date, text excerpt
    - Empty slot card: dashed border, "Add Reference" CTA
    - Add reference dialog: react-hook-form with referee_name (required), referee_email (required, validate format), relationship field. Submit calls sendReferenceRequest.

    **Badge Status page.tsx** (Server Component):
    - Calls getProviderBadges(supabase, providerId) — includes all badges (active and expired)
    - Groups by active vs expired
    - Renders BadgeGallery component

    **BadgeGallery.tsx** (Server Component):
    Props: { badges: ProviderBadge[], showAll?: boolean }
    - Grid of badge cards (3 columns on desktop, 2 on tablet, 1 on mobile)
    - Each badge: icon (appropriate Lucide icon per badge_type), label, description, earned date, expiry date (if set)
    - Expiry warning: if expires_at is within 30 days, show amber warning banner on the card
    - Expired badges: grayscale filter + "Expired" overlay
    - "What does this mean?" accordion/tooltip per badge with description text
    - Empty state: "Complete verification steps to earn badges" with illustration

    Badge types and icons mapping:
    - 'identity_verified': ShieldCheck icon, "Identity Verified" label
    - 'insurance_verified': Award icon, "Insured Professional"
    - 'gas_safe': Flame icon, "Gas Safe Registered"
    - 'electrical_certified': Zap icon, "Electrically Certified"
    - 'cscs_holder': HardHat icon, "CSCS Cardholder"
    - 'top_rated': Star icon, "Top Rated Provider"
    - 'references_complete': Users icon, "References Complete"
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | grep -E "error" | grep -v "warning" | wc -l</automated>
  </verify>
  <done>Client references page shows 3 reference slots with send/remind actions; peer references identical structure; badges page shows earned badges with expiry warnings; pnpm build passes</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` — 0 errors
2. `cd britv3.0 && pnpm lint` — 0 errors
3. Visit /dashboard/provider/verification → see 5-step stepper with correct status per step
4. Visit /dashboard/provider/verification/credentials → see upload cards for each document type
5. Visit /dashboard/provider/verification/client-references → see 3 reference slot cards
</verification>

<success_criteria>
- 5 verification pages render without errors
- Trust score gauge animates correctly as SVG arc
- File upload validates type (PDF/JPEG/PNG) and size (10MB max) before uploading
- References show correct status badges and actions per status
- Badge cards show expiry warnings for badges expiring within 30 days
</success_criteria>

<output>
After completion, create `.planning/phases/16-tradesperson-dashboard/16-06-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - britv3.0/package.json
  - britv3.0/src/app/globals.css
  - britv3.0/src/lib/utils.ts
  - britv3.0/src/lib/supabase/client.ts
  - britv3.0/src/lib/supabase/server.ts
  - britv3.0/src/lib/supabase/admin.ts
  - britv3.0/src/env.ts
  - britv3.0/components.json
  - britv3.0/vitest.config.mts
  - britv3.0/src/__tests__/setup.ts
  - britv3.0/src/__tests__/mocks/supabase.ts
  - britv3.0/src/__tests__/mocks/next.ts
  - britv3.0/src/components/ui/*.tsx
autonomous: true
requirements:
  - AUTH-19

must_haves:
  truths:
    - "Design system tokens (colors, fonts, spacing, shadows) are applied via Tailwind v4 CSS-first config"
    - "Shadcn UI components are installed and functional with Britestate theme"
    - "Supabase client factories work for browser, server, and admin contexts"
    - "Environment variables are type-safe validated at build time"
    - "Test infrastructure runs and passes with mock setup"
  artifacts:
    - path: "britv3.0/src/app/globals.css"
      provides: "Britestate design tokens via @theme directive"
      contains: "@theme"
    - path: "britv3.0/src/lib/supabase/client.ts"
      provides: "Browser Supabase client factory"
      exports: ["createClient"]
    - path: "britv3.0/src/lib/supabase/server.ts"
      provides: "Server Supabase client factory with async cookies"
      exports: ["createClient"]
    - path: "britv3.0/src/lib/supabase/admin.ts"
      provides: "Service role Supabase client"
      exports: ["createAdminClient"]
    - path: "britv3.0/src/env.ts"
      provides: "Type-safe env validation"
      exports: ["env"]
    - path: "britv3.0/vitest.config.mts"
      provides: "Test configuration"
    - path: "britv3.0/src/__tests__/mocks/supabase.ts"
      provides: "Supabase client mock"
  key_links:
    - from: "britv3.0/src/lib/supabase/server.ts"
      to: "britv3.0/src/env.ts"
      via: "env vars import"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE"
---

<objective>
Set up the complete project foundation: install all Phase 1 dependencies, configure the Britestate design system in Tailwind v4, initialize Shadcn UI with themed components, create Supabase client factories, set up type-safe environment validation, and establish the test infrastructure.

Purpose: Everything in Phase 1 depends on this -- design system for UI, Supabase clients for auth, test infra for verification. This must be done first and correctly.
Output: Working dev environment with themed UI components, Supabase connectivity, and test runner.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@britv3.0/docs/britestatestyle.txt
@britv3.0/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, init Shadcn, configure design system</name>
  <files>
    britv3.0/package.json,
    britv3.0/src/app/globals.css,
    britv3.0/src/lib/utils.ts,
    britv3.0/components.json
  </files>
  <action>
1. From `britv3.0/`, install all Phase 1 production dependencies:
   ```bash
   pnpm add @supabase/supabase-js @supabase/ssr zod react-hook-form @hookform/resolvers @t3-oss/env-nextjs lucide-react class-variance-authority clsx tailwind-merge
   ```

2. Initialize Shadcn UI (use `pnpm dlx shadcn@latest init`). When prompted:
   - Style: New York
   - Base color: Neutral
   - CSS variables: yes
   - Tailwind CSS: globals.css path
   - Use `--defaults` flag if available, or answer prompts

3. Install Shadcn components needed for Phase 1:
   ```bash
   pnpm dlx shadcn@latest add button input label card alert dialog dropdown-menu separator checkbox radio-group select toast tabs badge avatar sheet form sonner
   ```

4. Create `src/lib/utils.ts` with the `cn()` utility (clsx + tailwind-merge).

5. Replace `src/app/globals.css` with the complete Britestate design system using Tailwind v4 CSS-first configuration. Use `@theme` directive (NOT tailwind.config.ts). Include:
   - Google Fonts import: Plus Jakarta Sans (400,500,600,700) and Inter (400,500,600)
   - All brand color tokens from the style guide: `--color-brand-primary: #1B4D3E`, `--color-brand-primary-light: #2D7A5F`, `--color-brand-primary-lighter: #E8F5EE`, `--color-brand-secondary: #D4A853`, `--color-brand-secondary-light: #F5ECD7`, `--color-brand-accent: #2563EB`, `--color-brand-accent-light: #EFF6FF`
   - Neutral scale: 50 (#F8F8FA) through 950 (#0A0A0B)
   - Semantic colors: success (#16A34A), warning (#CA8A04), error (#DC2626), info (#2563EB) + light variants
   - Font families: `--font-heading: 'Plus Jakarta Sans', sans-serif` and `--font-body: 'Inter', sans-serif`
   - Border radius: sm (6px), md (8px), lg (12px), xl (16px), 2xl (24px)
   - Shadow scale: xs, sm, md, lg, xl per style guide values
   - Shadcn CSS variable overrides mapped to Britestate tokens (--background, --foreground, --primary, --secondary, --accent, --destructive, --card, --popover, --muted, --border, --input, --ring, --radius)
   - Base styles: antialiased text, smooth scroll, neutral-50 body background
   - Focus-visible ring style: 2px brand-accent with offset-2
   - Dark mode variants using `prefers-color-scheme` mapped to same Britestate palette

IMPORTANT: Tailwind v4 uses CSS-first config. Do NOT create a tailwind.config.ts file. All theme customization goes in globals.css via `@theme {}`.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build</automated>
  </verify>
  <done>All dependencies installed, Shadcn UI initialized with Britestate-themed components in src/components/ui/, globals.css has full design system tokens, build passes cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Supabase clients, env validation, and test infrastructure</name>
  <files>
    britv3.0/src/lib/supabase/client.ts,
    britv3.0/src/lib/supabase/server.ts,
    britv3.0/src/lib/supabase/admin.ts,
    britv3.0/src/env.ts,
    britv3.0/vitest.config.mts,
    britv3.0/src/__tests__/setup.ts,
    britv3.0/src/__tests__/mocks/supabase.ts,
    britv3.0/src/__tests__/mocks/next.ts,
    britv3.0/package.json
  </files>
  <action>
1. Create `src/env.ts` using `@t3-oss/env-nextjs` with Zod validation:
   - Client vars: `NEXT_PUBLIC_SUPABASE_URL` (url), `NEXT_PUBLIC_SUPABASE_ANON_KEY` (string min 1)
   - Server vars: `SUPABASE_SERVICE_ROLE_KEY` (string min 1)
   - Use `skipValidation` when `process.env.SKIP_ENV_VALIDATION === "true"` (for CI/test)

2. Create `src/lib/supabase/client.ts` -- browser Supabase client factory:
   - Uses `createBrowserClient` from `@supabase/ssr`
   - References `process.env.NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - Export named `createClient` function

3. Create `src/lib/supabase/server.ts` -- server Supabase client factory:
   - Uses `createServerClient` from `@supabase/ssr`
   - `async function createClient()` that `await cookies()` (Next.js 16 async cookie API)
   - Uses `getAll()`/`setAll()` cookie pattern (NOT deprecated get/set/remove)
   - Wraps `setAll` in try/catch for Server Component context where cookies can't be set
   - Export named `createClient` function

4. Create `src/lib/supabase/admin.ts` -- service role client:
   - Uses `createClient` from `@supabase/supabase-js` (NOT ssr)
   - Uses `SUPABASE_SERVICE_ROLE_KEY` -- server-only, bypasses RLS
   - Export named `createAdminClient` function

5. Install test dependencies:
   ```bash
   pnpm add -D vitest @vitejs/plugin-react @testing-library/react @testing-library/dom @testing-library/jest-dom happy-dom vite-tsconfig-paths
   ```

6. Create `vitest.config.mts`:
   - Plugin: `@vitejs/plugin-react`
   - Plugin: `vite-tsconfig-paths` (for `@/*` path alias)
   - Environment: `happy-dom`
   - Setup files: `src/__tests__/setup.ts`
   - Include: `src/**/*.test.{ts,tsx}`

7. Create `src/__tests__/setup.ts`:
   - Import `@testing-library/jest-dom/vitest`
   - Set `SKIP_ENV_VALIDATION=true` in process.env

8. Create `src/__tests__/mocks/supabase.ts`:
   - Mock factory for Supabase client with chainable query builder
   - Mock `auth.signUp`, `auth.signInWithPassword`, `auth.signInWithOAuth`, `auth.signOut`, `auth.getUser`, `auth.resetPasswordForEmail`, `auth.updateUser`, `auth.exchangeCodeForSession`
   - Mock `from().select().eq().single()` chain pattern
   - Export `createMockSupabaseClient` and `mockSupabaseAuth`

9. Create `src/__tests__/mocks/next.ts`:
   - Mock `next/navigation` (useRouter, usePathname, useSearchParams, redirect)
   - Mock `next/headers` (cookies, headers)
   - Export individual mocks

10. Add test scripts to `package.json`:
    - `"test": "vitest"`
    - `"test:run": "vitest run"`
    - `"test:coverage": "vitest run --coverage"`
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && SKIP_ENV_VALIDATION=true pnpm vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Supabase clients created for all three contexts (browser, server, admin), env validation works with @t3-oss/env-nextjs, Vitest runs with mock setup, `pnpm build` still passes</done>
</task>

</tasks>

<verification>
1. `cd britv3.0 && pnpm build` passes without errors
2. `cd britv3.0 && SKIP_ENV_VALIDATION=true pnpm vitest run --passWithNoTests` exits 0
3. `src/components/ui/button.tsx` exists (Shadcn installed)
4. `src/app/globals.css` contains `@theme` with Britestate color tokens
5. `src/lib/supabase/server.ts` uses `await cookies()` pattern
</verification>

<success_criteria>
- Build passes with all dependencies installed
- Shadcn UI components are themed with Britestate design tokens
- Three Supabase client factories (browser, server, admin) compile correctly
- Test runner executes without errors
- Design system CSS variables are accessible in Tailwind classes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

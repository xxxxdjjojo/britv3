---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - britv3.0/src/app/(auth)/register/role-select/page.tsx
  - britv3.0/src/app/(auth)/register/onboarding/[role]/page.tsx
  - britv3.0/src/app/(protected)/dashboard/page.tsx
  - britv3.0/src/app/(protected)/dashboard/[role]/page.tsx
  - britv3.0/src/app/(protected)/dashboard/[role]/layout.tsx
  - britv3.0/src/app/(protected)/dashboard/layout.tsx
  - britv3.0/src/components/auth/RoleSelector.tsx
  - britv3.0/src/components/auth/OnboardingFlow.tsx
  - britv3.0/src/components/layout/Sidebar.tsx
  - britv3.0/src/components/layout/RoleSwitcher.tsx
  - britv3.0/src/hooks/useRole.ts
  - britv3.0/src/services/auth/role-service.ts
  - britv3.0/src/__tests__/auth/role-select.test.ts
  - britv3.0/src/__tests__/auth/role-switch.test.ts
  - britv3.0/src/__tests__/auth/verification-levels.test.ts
  - britv3.0/src/__tests__/dashboard/shell.test.ts
autonomous: true
requirements:
  - AUTH-07
  - AUTH-08
  - AUTH-09
  - AUTH-10

must_haves:
  truths:
    - "User can select one or more roles after registration"
    - "User can switch between their active roles"
    - "Dashboard shell loads with role-specific sidebar and content"
    - "Verification level is computed based on completed verification stages"
    - "Dashboard redirects to role-specific view based on active_role"
  artifacts:
    - path: "britv3.0/src/app/(auth)/register/role-select/page.tsx"
      provides: "Post-registration role selection"
      min_lines: 20
    - path: "britv3.0/src/components/auth/RoleSelector.tsx"
      provides: "Multi-role selection component"
    - path: "britv3.0/src/components/layout/RoleSwitcher.tsx"
      provides: "Role switching dropdown"
    - path: "britv3.0/src/components/layout/Sidebar.tsx"
      provides: "Role-specific dashboard sidebar"
    - path: "britv3.0/src/services/auth/role-service.ts"
      provides: "Role CRUD operations"
      exports: ["selectRoles", "switchRole", "getUserRoles", "getActiveRole"]
    - path: "britv3.0/src/hooks/useRole.ts"
      provides: "Client-side role state hook"
      exports: ["useRole"]
  key_links:
    - from: "britv3.0/src/components/layout/RoleSwitcher.tsx"
      to: "britv3.0/src/services/auth/role-service.ts"
      via: "switchRole call"
      pattern: "switchRole"
    - from: "britv3.0/src/app/(protected)/dashboard/page.tsx"
      to: "britv3.0/src/services/auth/role-service.ts"
      via: "getActiveRole for redirect"
      pattern: "getActiveRole|active_role"
---

<objective>
Build the multi-role system: post-registration role selection, role switching, role-specific dashboard shells with sidebars, and verification level computation. This connects auth to the dashboard experience.

Purpose: Multi-role is central to Britestate -- a user can be a homebuyer AND a landlord. The dashboard must adapt to the active role.
Output: Working role selection flow, role switcher, 6 dashboard shells with role-specific sidebars.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@britv3.0/docs/britestatestyle.txt

<interfaces>
From src/types/auth.ts:
export type UserRole = "homebuyer" | "renter" | "seller" | "landlord" | "agent" | "service_provider"
export type VerificationLevel = "basic" | "standard" | "enhanced" | "professional"
export type Profile (id, display_name, active_role, verification_level, ...)
export type UserRoleRecord (id, user_id, role, granted_at)

From src/lib/constants.ts:
export const ROLES: { value: UserRole; label: string; description: string; icon: string }[]
export const VERIFICATION_LEVELS: { ... }[]

From src/services/auth/auth-service.ts:
export function getUser(): Promise<{ user, error }>

From src/hooks/useAuth.ts:
export function useAuth(): { user, loading, signIn, signUp, signOut, signInWithOAuth }

From src/lib/supabase/client.ts:
export function createClient(): SupabaseClient

From src/lib/supabase/server.ts:
export async function createClient(): Promise<SupabaseClient>
</interfaces>

Screen designs:
- Sign Up - Role Selector (#99, screen ID: 1d4ccee322934eefb0e965cc9e644be4)
- Buyer Onboarding - Location Selection (#8, screen ID: f22a3f617e8c46d88b0ba167e91d60ab)
- Landlord Onboarding - Compliance Check (#30, screen ID: 5b44537748ef494388bef8e5cf90d3f2)
- Agent Onboarding - Agency Profile (#76, screen ID: 256a48c67bb545258069ba942a3eeec8)
- Tradesperson Onboarding - Trade Categories (#47, screen ID: 5a1bc7cf97894b0b9f58725dac53f392)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Role service, role hook, and role selection UI</name>
  <files>
    britv3.0/src/services/auth/role-service.ts,
    britv3.0/src/hooks/useRole.ts,
    britv3.0/src/app/(auth)/register/role-select/page.tsx,
    britv3.0/src/app/(auth)/register/onboarding/[role]/page.tsx,
    britv3.0/src/components/auth/RoleSelector.tsx,
    britv3.0/src/components/auth/OnboardingFlow.tsx,
    britv3.0/src/__tests__/auth/role-select.test.ts,
    britv3.0/src/__tests__/auth/role-switch.test.ts,
    britv3.0/src/__tests__/auth/verification-levels.test.ts
  </files>
  <behavior>
    - selectRoles(userId, roles[]) inserts into user_roles table and sets first role as active_role on profile
    - switchRole(userId, role) updates profiles.active_role if role exists in user_roles
    - switchRole rejects if user doesn't have the requested role
    - getUserRoles(userId) returns all roles from user_roles table
    - getActiveRole(userId) returns current active_role from profile
    - computeVerificationLevel(stages[]) returns correct level based on completed stages
    - RoleSelector allows selecting 1+ roles with visual card selection
  </behavior>
  <action>
1. Write tests:
   - `role-select.test.ts`: selectRoles inserts roles, sets active_role; rejects empty selection; handles duplicate roles
   - `role-switch.test.ts`: switchRole updates active_role; rejects unauthorized role; getUserRoles returns correct set
   - `verification-levels.test.ts`: computeVerificationLevel returns 'basic' with no stages, 'standard' with email+phone, 'enhanced' with ID, 'professional' with all stages

2. Create `src/services/auth/role-service.ts`:
   - `selectRoles(userId: string, roles: UserRole[])` -- batch insert into user_roles, update profiles.active_role to first role. Uses server Supabase client.
   - `switchRole(userId: string, role: UserRole)` -- verify role exists in user_roles, update profiles.active_role
   - `getUserRoles(userId: string)` -- select from user_roles WHERE user_id
   - `getActiveRole(userId: string)` -- select active_role from profiles WHERE id
   - `computeVerificationLevel(completedStages: VerificationStage[]): VerificationLevel` -- pure function: no stages = basic, email+phone = standard, +identity = enhanced, all = professional

3. Create `src/hooks/useRole.ts` ("use client"):
   - `useRole()` hook returning `{ roles, activeRole, loading, switchRole, refetch }`
   - Fetches roles on mount via Supabase client
   - `switchRole` updates active_role on server then refetches

4. Create `src/components/auth/RoleSelector.tsx` ("use client"):
   - Grid of 6 role cards (2x3 on desktop, 1 col on mobile)
   - Each card: icon (from ROLES constant Lucide icon name), role label, description
   - Cards are toggleable (checkbox-like) -- user can select multiple
   - Selected state: brand-primary border, brand-primary-lighter background, checkmark badge
   - Unselected: neutral-200 border, white background
   - "Continue" button at bottom (disabled until at least 1 role selected)
   - Minimum 1 role required

5. Create `src/app/(auth)/register/role-select/page.tsx`:
   - Title: "How will you use Britestate?"
   - Subtitle: "Select all that apply. You can add more roles later."
   - Renders RoleSelector
   - On submit: calls selectRoles, then redirects to onboarding for first selected role

6. Create `src/components/auth/OnboardingFlow.tsx` ("use client"):
   - Role-specific onboarding step component
   - For homebuyer: location preferences (town/city input, search radius)
   - For renter: similar to homebuyer + budget range
   - For seller: property to list prompt
   - For landlord: portfolio size, compliance awareness
   - For agent: agency name, license number, coverage area
   - For service_provider: trade categories (multi-select from predefined list), coverage area
   - Each is a simple form that saves preferences (can be skeletal -- full implementation comes in later phases)
   - "Skip for now" option on every step
   - "Complete Setup" button that redirects to /dashboard

7. Create `src/app/(auth)/register/onboarding/[role]/page.tsx`:
   - Dynamic route that renders OnboardingFlow for the given role
   - Validates role param against UserRole type
   - Redirects to /dashboard if role is invalid
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/auth/role-select.test.ts src/__tests__/auth/role-switch.test.ts src/__tests__/auth/verification-levels.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Role selection allows multi-role, switchRole works, verification levels compute correctly, onboarding flow renders per role. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard shells with role-specific sidebars and role switcher</name>
  <files>
    britv3.0/src/app/(protected)/dashboard/page.tsx,
    britv3.0/src/app/(protected)/dashboard/layout.tsx,
    britv3.0/src/app/(protected)/dashboard/[role]/page.tsx,
    britv3.0/src/app/(protected)/dashboard/[role]/layout.tsx,
    britv3.0/src/components/layout/Sidebar.tsx,
    britv3.0/src/components/layout/RoleSwitcher.tsx,
    britv3.0/src/__tests__/dashboard/shell.test.ts
  </files>
  <action>
1. Create `src/components/layout/RoleSwitcher.tsx` ("use client"):
   - DropdownMenu (Shadcn) showing current active role with icon
   - Dropdown lists all user's roles with icons from ROLES constant
   - Active role has a checkmark
   - Clicking a role calls switchRole from useRole hook
   - Shows loading indicator during switch

2. Create `src/components/layout/Sidebar.tsx` ("use client"):
   - Vertical sidebar (w-64 on lg, hidden on mobile via Sheet)
   - Role-specific navigation items per role:
     * homebuyer: Overview, Saved Properties, Searches, Viewings, Documents
     * renter: Overview, Saved Rentals, Applications, Tenancy, Documents
     * seller: Overview, My Listings, Viewings, Offers, Documents
     * landlord: Overview, Portfolio, Tenants, Maintenance, Finances, Compliance
     * agent: Overview, Listings, Leads, Viewings, Revenue, Team
     * service_provider: Overview, Jobs, Quotes, Reviews, Verification, Earnings
   - Each item: Lucide icon + label, active state (brand-primary bg)
   - RoleSwitcher component at top of sidebar
   - User info (avatar + name) at bottom with settings link
   - Collapsible on desktop (icon-only mode)

3. Create `src/app/(protected)/dashboard/layout.tsx`:
   - Flex layout: Sidebar on left + main content area on right
   - Mobile: no sidebar visible (accessed via mobile nav)
   - Header remains from root layout

4. Create `src/app/(protected)/dashboard/page.tsx`:
   - Server component
   - Fetches user's active_role from profile via Supabase server client
   - Redirects to `/dashboard/{active_role}` (e.g., `/dashboard/homebuyer`)
   - If no roles assigned, redirects to `/register/role-select`

5. Create `src/app/(protected)/dashboard/[role]/layout.tsx`:
   - Validates `role` param exists in user's roles (via server client)
   - If user doesn't have the role, show access denied or redirect to dashboard root
   - Passes role context down

6. Create `src/app/(protected)/dashboard/[role]/page.tsx`:
   - Dashboard shell page per role
   - Shows: "Welcome back, {display_name}" heading
   - Role-specific placeholder cards showing key metrics (empty state with icons)
   - homebuyer: "Saved Properties (0)", "Active Searches (0)", "Upcoming Viewings (0)"
   - renter: "Saved Rentals (0)", "Applications (0)", "Tenancy Status"
   - seller: "Active Listings (0)", "Total Views (0)", "Offers (0)"
   - landlord: "Properties (0)", "Occupancy Rate", "Monthly Income"
   - agent: "Active Listings (0)", "Leads (0)", "Revenue"
   - service_provider: "Active Jobs (0)", "Pending Quotes (0)", "Rating"
   - Cards use Shadcn Card with Britestate styling

7. Write test `shell.test.ts`:
   - Dashboard layout renders Sidebar with correct nav items per role
   - RoleSwitcher shows current role
   - Dashboard redirect works based on active_role
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard at /dashboard redirects to role-specific view, sidebar shows role-appropriate nav, RoleSwitcher allows changing roles, all 6 role shells render with placeholder cards. Build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with dashboard routes
2. Role tests pass: `SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/auth/role- src/__tests__/dashboard/`
3. /dashboard redirects to /dashboard/{active_role}
4. Sidebar shows different nav items per role
5. RoleSwitcher dropdown lists user's roles
6. Role selection page shows 6 role cards
</verification>

<success_criteria>
- User can select 1+ roles at /register/role-select
- User can switch between roles via RoleSwitcher dropdown
- Dashboard loads correct shell for active role with role-specific sidebar navigation
- Verification level computation works correctly
- Each role's dashboard shows appropriate placeholder metrics
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-03"]
files_modified:
  - britv3.0/src/app/(auth)/layout.tsx
  - britv3.0/src/app/(auth)/login/page.tsx
  - britv3.0/src/app/(auth)/register/page.tsx
  - britv3.0/src/app/(auth)/forgot-password/page.tsx
  - britv3.0/src/app/(auth)/reset-password/page.tsx
  - britv3.0/src/app/(auth)/verify-email/page.tsx
  - britv3.0/src/components/auth/LoginForm.tsx
  - britv3.0/src/components/auth/RegisterForm.tsx
  - britv3.0/src/components/auth/OAuthButtons.tsx
  - britv3.0/src/components/auth/PasswordStrengthMeter.tsx
  - britv3.0/src/components/auth/ForgotPasswordForm.tsx
  - britv3.0/src/components/auth/ResetPasswordForm.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "User can see login page with email/password form and OAuth buttons"
    - "User can see registration page with password strength meter"
    - "User can see forgot-password page with email input"
    - "User can see reset-password page with new password form"
    - "User can see verify-email page with resend option"
  artifacts:
    - path: "britv3.0/src/app/(auth)/login/page.tsx"
      provides: "Login page with email/password + OAuth"
      min_lines: 20
    - path: "britv3.0/src/app/(auth)/register/page.tsx"
      provides: "Registration page"
      min_lines: 20
    - path: "britv3.0/src/components/auth/LoginForm.tsx"
      provides: "Login form with validation"
    - path: "britv3.0/src/components/auth/RegisterForm.tsx"
      provides: "Register form with password strength"
    - path: "britv3.0/src/components/auth/OAuthButtons.tsx"
      provides: "Google and Apple OAuth buttons"
  key_links:
    - from: "britv3.0/src/components/auth/LoginForm.tsx"
      to: "britv3.0/src/services/auth/auth-service.ts"
      via: "signIn function call"
      pattern: "signIn"
    - from: "britv3.0/src/components/auth/RegisterForm.tsx"
      to: "britv3.0/src/services/auth/auth-service.ts"
      via: "signUp function call"
      pattern: "signUp"
    - from: "britv3.0/src/components/auth/OAuthButtons.tsx"
      to: "britv3.0/src/services/auth/auth-service.ts"
      via: "signInWithOAuth function call"
      pattern: "signInWithOAuth"
---

<objective>
Build all auth UI pages and form components using the Britestate design system. These pages consume the auth service layer from Plan 03.

Purpose: Auth pages are the user-facing gateway. Splitting UI from service logic keeps each plan focused and within scope limits.
Output: 5 auth pages (login, register, forgot-password, reset-password, verify-email) with 6 form components.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@britv3.0/docs/britestatestyle.txt

<interfaces>
<!-- From Plan 01 -->
From src/lib/utils.ts:
export function cn(...inputs: ClassValue[]): string

<!-- From Plan 03 -->
From src/services/auth/auth-service.ts:
export function signUp(email: string, password: string, displayName: string): Promise<{ user, error }>
export function signIn(email: string, password: string): Promise<{ user, session, error }>
export function signInWithOAuth(provider: 'google' | 'apple'): Promise<{ data, error }>
export function signOut(): Promise<{ error }>
export function resetPassword(email: string): Promise<{ data, error }>
export function updatePassword(password: string): Promise<{ data, error }>

From src/hooks/useAuth.ts:
export function useAuth(): { user, loading, signIn, signUp, signOut, signInWithOAuth }
</interfaces>

Screen designs to implement:
- Login Page (#44, screen ID: 0ef60920fd3342e49507479dd6cc689b)
- Sign Up - Account Details (#145, screen ID: 1bc0f4896fbc4b189aa4aadda4b680de)
- Forgot Password (#106, screen ID: c9557fb1b99547c88637aba9651e5b73)
- Reset Password Form (#53, screen ID: d732945d40374505a6d7e54fee6eb3c4)
- Email Verification Pending (#107, screen ID: d21cfbdf7ce749c89e5e3ddd1e7b3252)
- Email Verification Confirmed (#113, screen ID: 8d6b3d7ec4e74a0687d76ba7ca29a666)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth layout and login/register pages</name>
  <files>
    britv3.0/src/app/(auth)/layout.tsx,
    britv3.0/src/app/(auth)/login/page.tsx,
    britv3.0/src/app/(auth)/register/page.tsx,
    britv3.0/src/components/auth/LoginForm.tsx,
    britv3.0/src/components/auth/RegisterForm.tsx,
    britv3.0/src/components/auth/OAuthButtons.tsx,
    britv3.0/src/components/auth/PasswordStrengthMeter.tsx
  </files>
  <action>
1. Create `src/app/(auth)/layout.tsx`:
   - Centered card layout with Britestate logo at top
   - Background: neutral-50 with subtle pattern or gradient
   - Max width 480px card with shadow-lg, radius-xl, white bg
   - No main nav -- auth pages are standalone

2. Create `src/components/auth/LoginForm.tsx` ("use client"):
   - react-hook-form + Zod validation: email (valid email), password (min 8 chars)
   - Email input with envelope icon, password input with eye toggle
   - "Sign In" primary button (brand-primary), full width
   - "Forgot password?" link below password field
   - Error display via Shadcn Alert (destructive variant)
   - Loading state on submit button
   - Calls `signIn` from auth-service, redirects to `/dashboard` on success

3. Create `src/components/auth/OAuthButtons.tsx` ("use client"):
   - "Continue with Google" button with Google icon (secondary variant, full width)
   - "Continue with Apple" button with Apple icon (secondary variant, full width)
   - Separator with "or" text between OAuth and email form
   - Each calls `signInWithOAuth` from auth-service

4. Create `src/app/(auth)/login/page.tsx`:
   - Server component page
   - Renders LoginForm + OAuthButtons
   - "Don't have an account? Sign up" link at bottom
   - Title: "Welcome back" (Plus Jakarta Sans)

5. Create `src/components/auth/PasswordStrengthMeter.tsx` ("use client"):
   - Visual strength bar (4 segments): weak (red), fair (yellow), good (green), strong (brand-primary)
   - Checks: length >= 8, has uppercase, has number, has special char
   - Shows requirement checklist below

6. Create `src/components/auth/RegisterForm.tsx` ("use client"):
   - react-hook-form + Zod: display_name (min 2), email (valid), password (min 8, one uppercase, one number), confirm_password (must match)
   - PasswordStrengthMeter component below password field
   - "Create Account" primary button
   - Calls `signUp`, redirects to `/verify-email` on success

7. Create `src/app/(auth)/register/page.tsx`:
   - Renders RegisterForm + OAuthButtons
   - "Already have an account? Sign in" link
   - Title: "Create your account"

All form components use Shadcn UI primitives (Input, Button, Label, Card, Alert). All styling uses Tailwind classes with Britestate design tokens. Typography: headings in Plus Jakarta Sans (font-heading), body in Inter (font-body). Colors: brand-primary for primary actions, brand-accent for links, error for validation errors.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Auth layout renders centered card, login page has email/password form + OAuth buttons, register page has form with password strength meter. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Password reset and email verification pages</name>
  <files>
    britv3.0/src/app/(auth)/forgot-password/page.tsx,
    britv3.0/src/app/(auth)/reset-password/page.tsx,
    britv3.0/src/app/(auth)/verify-email/page.tsx,
    britv3.0/src/components/auth/ForgotPasswordForm.tsx,
    britv3.0/src/components/auth/ResetPasswordForm.tsx
  </files>
  <action>
1. Create `src/components/auth/ForgotPasswordForm.tsx` ("use client"):
   - Email input + "Send Reset Link" button
   - Success state: "Check your email" message with mail icon
   - Calls `resetPassword` from auth-service

2. Create `src/app/(auth)/forgot-password/page.tsx`:
   - Renders ForgotPasswordForm
   - "Back to login" link
   - Title: "Reset your password"

3. Create `src/components/auth/ResetPasswordForm.tsx` ("use client"):
   - New password + confirm password with PasswordStrengthMeter
   - "Update Password" button
   - Calls `updatePassword`, redirects to `/login` with success message

4. Create `src/app/(auth)/reset-password/page.tsx`:
   - Renders ResetPasswordForm
   - Title: "Set new password"

5. Create `src/app/(auth)/verify-email/page.tsx`:
   - Shows "Check your email" message with email icon illustration
   - "We sent a verification link to {email}" text
   - "Resend email" button (with cooldown timer)
   - "Back to login" link
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Forgot-password, reset-password, and verify-email pages render correctly with forms. Build passes. All 5 auth pages complete.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with all auth routes compiled
2. Login page has email/password form + Google/Apple OAuth buttons
3. Registration form validates password strength
4. Forgot-password page has email input
5. Reset-password page has new password form
6. Verify-email page shows confirmation message
7. All forms use react-hook-form + Zod validation
</verification>

<success_criteria>
- User can navigate to /login and see email/password form with OAuth buttons
- User can navigate to /register and see registration form with password strength meter
- User can navigate to /forgot-password and see email input for reset
- User can navigate to /reset-password and see new password form
- User can navigate to /verify-email and see confirmation message
- All pages use Britestate design system
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>

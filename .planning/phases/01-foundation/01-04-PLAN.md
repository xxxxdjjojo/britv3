---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - britv3.0/src/middleware.ts
  - britv3.0/src/app/(main)/layout.tsx
  - britv3.0/src/app/(main)/page.tsx
  - britv3.0/src/app/(main)/about/page.tsx
  - britv3.0/src/app/(main)/terms/page.tsx
  - britv3.0/src/app/(main)/privacy/page.tsx
  - britv3.0/src/app/(protected)/layout.tsx
  - britv3.0/src/app/layout.tsx
  - britv3.0/src/app/not-found.tsx
  - britv3.0/src/app/error.tsx
  - britv3.0/src/components/layout/Header.tsx
  - britv3.0/src/components/layout/Footer.tsx
  - britv3.0/src/components/layout/MobileNav.tsx
  - britv3.0/src/components/shared/Logo.tsx
  - britv3.0/src/components/shared/LoadingSpinner.tsx
  - britv3.0/src/__tests__/security/csp.test.ts
  - britv3.0/src/__tests__/security/middleware.test.ts
  - britv3.0/src/__tests__/pages/public.test.ts
  - britv3.0/src/__tests__/layout/shell.test.ts
autonomous: true
requirements:
  - AUTH-16
  - AUTH-17
  - AUTH-18
  - AUTH-19

must_haves:
  truths:
    - "CSP Level 3 headers are set on all responses with nonce"
    - "Unauthenticated users are redirected from protected routes to /login"
    - "Authenticated users are redirected from auth routes to /dashboard"
    - "Public pages (home, about, terms, privacy) load without authentication"
    - "Responsive layout shell with header, navigation, and footer works on mobile and desktop"
    - "Error pages (404, 500, access denied) render with Britestate design"
  artifacts:
    - path: "britv3.0/src/middleware.ts"
      provides: "Auth + RBAC + CSP middleware"
      exports: ["middleware", "config"]
      min_lines: 50
    - path: "britv3.0/src/app/(main)/page.tsx"
      provides: "Homepage"
      min_lines: 30
    - path: "britv3.0/src/components/layout/Header.tsx"
      provides: "Responsive header with nav"
    - path: "britv3.0/src/app/not-found.tsx"
      provides: "404 page"
    - path: "britv3.0/src/app/error.tsx"
      provides: "Error boundary page"
  key_links:
    - from: "britv3.0/src/middleware.ts"
      to: "britv3.0/src/lib/supabase/server.ts"
      via: "createServerClient for auth check"
      pattern: "createServerClient"
    - from: "britv3.0/src/middleware.ts"
      to: "britv3.0/src/lib/constants.ts"
      via: "PUBLIC_ROUTES and AUTH_ROUTES arrays"
      pattern: "PUBLIC_ROUTES|AUTH_ROUTES"
---

<objective>
Build the security middleware (CSP + RBAC), responsive layout shell (header, footer, mobile nav), public pages (home, about, terms, privacy), error pages, and protected route guard.

Purpose: Security hardening and the layout shell are the container for all other features. CSP and RBAC must be in place before auth flows are tested in the browser.
Output: Working middleware, responsive layout, public pages, and error pages.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@britv3.0/docs/britestatestyle.txt

<interfaces>
From src/lib/supabase/server.ts:
export async function createClient(): Promise<SupabaseClient>

From src/lib/constants.ts:
export const AUTH_ROUTES: string[]
export const PUBLIC_ROUTES: string[]
export const PROTECTED_ROUTES: string[]
export const ROLES: { value: UserRole; label: string; description: string; icon: string }[]

From src/types/auth.ts:
export type UserRole = "homebuyer" | "renter" | "seller" | "landlord" | "agent" | "service_provider"
</interfaces>

Screen designs to implement:
- Britestate Homepage variants (#4, #10, #27, #98)
- Error pages: Access Denied (#84), 404 (#114), 500 (#146), Session Expired (#40)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Middleware with auth guard, RBAC, and CSP headers</name>
  <files>
    britv3.0/src/middleware.ts,
    britv3.0/src/__tests__/security/csp.test.ts,
    britv3.0/src/__tests__/security/middleware.test.ts
  </files>
  <behavior>
    - CSP header includes nonce-based script-src with allowlisted domains (accounts.google.com, appleid.cdn-apple.com)
    - CSP connect-src allows *.supabase.co
    - CSP frame-src allows Google and Apple OAuth
    - Security headers present: X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy
    - Unauthenticated request to /dashboard redirects to /login
    - Authenticated request to /login redirects to /dashboard
    - Request to / (public) passes through regardless of auth state
    - Middleware matcher excludes static files and images
  </behavior>
  <action>
1. Write tests first:
   - `csp.test.ts`: Verify CSP header format, nonce presence, required domains in allowlist, security headers
   - `middleware.test.ts`: Verify redirect logic for unauthenticated to protected routes, authenticated to auth routes, public route passthrough

2. Create `src/middleware.ts` following the pattern from 01-RESEARCH.md:
   - Create Supabase server client using `createServerClient` from `@supabase/ssr` (NOT the helper from lib/supabase -- middleware needs direct cookie access via request.cookies)
   - Call `supabase.auth.getUser()` (NOT getSession) to verify auth
   - Route protection logic:
     * PUBLIC_ROUTES (/, /about, /terms, /privacy) -- always accessible
     * AUTH_ROUTES (/login, /register, /forgot-password, /reset-password, /verify-email) -- redirect to /dashboard if authenticated
     * Everything else -- redirect to /login if NOT authenticated
   - CSP Level 3 with nonce:
     * Generate nonce via `crypto.randomUUID()` base64 encoded
     * `default-src 'self'`
     * `script-src 'self' 'nonce-{nonce}' https://accounts.google.com https://appleid.cdn-apple.com`
     * `style-src 'self' 'unsafe-inline'` (Tailwind needs unsafe-inline)
     * `img-src 'self' data: blob: https://*.supabase.co`
     * `connect-src 'self' https://*.supabase.co wss://*.supabase.co`
     * `frame-src https://accounts.google.com https://appleid.apple.com`
     * `form-action 'self'`
     * `base-uri 'self'`
     * `frame-ancestors 'none'`
   - Set `x-nonce` response header for server components to read
   - Additional security headers: X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy camera=() microphone=() geolocation=()
   - Matcher config: exclude `_next/static`, `_next/image`, `favicon.ico`, static file extensions
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/security/ 2>&1 | tail -10</automated>
  </verify>
  <done>Middleware sets CSP with nonce, redirects unauthenticated users from protected routes, redirects authenticated users from auth routes, sets all security headers. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Layout shell, public pages, and error pages</name>
  <files>
    britv3.0/src/app/layout.tsx,
    britv3.0/src/app/(main)/layout.tsx,
    britv3.0/src/app/(main)/page.tsx,
    britv3.0/src/app/(main)/about/page.tsx,
    britv3.0/src/app/(main)/terms/page.tsx,
    britv3.0/src/app/(main)/privacy/page.tsx,
    britv3.0/src/app/(protected)/layout.tsx,
    britv3.0/src/app/not-found.tsx,
    britv3.0/src/app/error.tsx,
    britv3.0/src/components/layout/Header.tsx,
    britv3.0/src/components/layout/Footer.tsx,
    britv3.0/src/components/layout/MobileNav.tsx,
    britv3.0/src/components/shared/Logo.tsx,
    britv3.0/src/components/shared/LoadingSpinner.tsx,
    britv3.0/src/__tests__/pages/public.test.ts,
    britv3.0/src/__tests__/layout/shell.test.ts
  </files>
  <action>
1. Update `src/app/layout.tsx` (root layout):
   - Set metadata: title "Britestate | UK Property Portal", description
   - Font imports: Plus Jakarta Sans + Inter via next/font/google
   - Apply font CSS variables to html element
   - Body: `font-body antialiased` class
   - Wrap children in Sonner Toaster provider

2. Create `src/components/shared/Logo.tsx`:
   - SVG or text-based Britestate logo
   - Props: size (sm, md, lg), variant (default, white)
   - Brand-primary color with "Britestate" text in Plus Jakarta Sans

3. Create `src/components/shared/LoadingSpinner.tsx`:
   - Animated spinner in brand-primary
   - Props: size (sm, md, lg)

4. Create `src/components/layout/Header.tsx` ("use client"):
   - Sticky header with white bg, shadow-sm on scroll
   - Logo left, nav links center (Home, About, Search properties), auth buttons right
   - "Sign In" ghost button + "Get Started" primary button (when not authenticated)
   - User avatar + dropdown (when authenticated) showing role switcher, settings, sign out
   - Mobile: hamburger menu triggering Sheet (Shadcn) side panel
   - Responsive: nav links hidden on mobile, shown on md+

5. Create `src/components/layout/MobileNav.tsx` ("use client"):
   - Sheet component sliding from left
   - Full nav links, auth buttons, logo
   - Closes on link click

6. Create `src/components/layout/Footer.tsx`:
   - 4-column grid: Company (About, Careers, Press), Support (Help, Contact, Terms), Legal (Privacy, Cookies, GDPR), Social (placeholder icons)
   - Bottom bar: copyright, "Britestate Ltd"
   - Brand-primary background with white text OR neutral-50 bg with dark text

7. Create `src/app/(main)/layout.tsx`:
   - Renders Header + children + Footer
   - No auth required

8. Create `src/app/(main)/page.tsx` (Homepage):
   - Hero section: headline "Find Your Perfect Property", subtext about UK property portal, "Start Searching" primary CTA + "List Your Property" secondary CTA
   - Background: subtle brand-primary-lighter gradient or hero image placeholder
   - Trust badges section: "10,000+ Properties", "Verified Agents", "Secure Platform"
   - Features grid: Search & Compare, Service Marketplace, Transaction Tracking, Landlord Tools (each with Lucide icon)
   - How It Works: 3-step section (Search, Connect, Complete)
   - All styled with Britestate design tokens

9. Create `src/app/(main)/about/page.tsx`:
   - Company mission, team placeholder, values
   - Simple content page with Section/Container layout

10. Create `src/app/(main)/terms/page.tsx`:
    - Terms of service content (placeholder legal text clearly marked as placeholder)
    - Structured with headings and sections

11. Create `src/app/(main)/privacy/page.tsx`:
    - Privacy policy content (placeholder, marked as such)
    - GDPR-relevant sections: data collection, usage, rights, contact

12. Create `src/app/(protected)/layout.tsx`:
    - Server component that checks auth via Supabase server client
    - If no user, redirect to /login
    - Renders children within a dashboard-ready container (header present, sidebar area reserved)

13. Create `src/app/not-found.tsx`:
    - "Page Not Found" with illustration/icon
    - "Go back home" button
    - Britestate branding

14. Create `src/app/error.tsx` ("use client"):
    - Error boundary with "Something went wrong" message
    - "Try again" button calling reset()
    - Britestate branding

15. Write tests:
    - `public.test.ts`: Verify homepage renders with hero section, about/terms/privacy pages render
    - `shell.test.ts`: Verify Header renders with logo and nav, Footer renders with links

All pages use Britestate design: brand-primary for CTAs, Plus Jakarta Sans for headings, Inter for body, proper spacing and shadows from the token system.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Root layout has fonts and metadata, Header with responsive nav works, Footer renders, 4 public pages load correctly, error pages show branded messages, protected layout redirects unauthenticated users. Build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with all routes
2. Security tests pass: `SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/security/`
3. Layout tests pass: `SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/layout/ src/__tests__/pages/`
4. CSP header is present in middleware response
5. Navigation links work: /, /about, /terms, /privacy
6. Not-found page renders at /nonexistent-route
</verification>

<success_criteria>
- Middleware enforces CSP Level 3, RBAC redirects, and security headers on all routes
- Homepage displays with hero, features, and CTAs
- About, Terms, Privacy pages render with content
- 404 and 500 error pages show Britestate-branded messages
- Header is responsive with mobile hamburger menu
- Footer has navigation links
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>

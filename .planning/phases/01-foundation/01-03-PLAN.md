---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - britv3.0/src/services/auth/auth-service.ts
  - britv3.0/src/hooks/useAuth.ts
  - britv3.0/src/app/auth/callback/route.ts
  - britv3.0/src/__tests__/auth/signup.test.ts
  - britv3.0/src/__tests__/auth/oauth.test.ts
  - britv3.0/src/__tests__/auth/password-reset.test.ts
  - britv3.0/src/__tests__/auth/session.test.ts
  - britv3.0/src/__tests__/auth/verify-email.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "Auth service provides signUp, signIn, signInWithOAuth, signOut, resetPassword, updatePassword operations"
    - "useAuth hook manages user state with real-time auth state changes"
    - "PKCE callback route exchanges code for session and redirects"
    - "All auth operations are tested with mocks"
  artifacts:
    - path: "britv3.0/src/services/auth/auth-service.ts"
      provides: "Auth business logic"
      exports: ["signUp", "signIn", "signOut", "signInWithOAuth", "resetPassword", "updatePassword", "getUser"]
    - path: "britv3.0/src/hooks/useAuth.ts"
      provides: "Client-side auth hook"
      exports: ["useAuth"]
    - path: "britv3.0/src/app/auth/callback/route.ts"
      provides: "PKCE code exchange callback"
      exports: ["GET"]
  key_links:
    - from: "britv3.0/src/hooks/useAuth.ts"
      to: "britv3.0/src/services/auth/auth-service.ts"
      via: "auth operations"
      pattern: "signIn|signUp|signOut"
    - from: "britv3.0/src/app/auth/callback/route.ts"
      to: "britv3.0/src/lib/supabase/server.ts"
      via: "exchangeCodeForSession"
      pattern: "exchangeCodeForSession"
---

<objective>
Build the auth service layer, useAuth hook, and PKCE callback route with full test coverage. This is the logic layer that auth UI pages will consume.

Purpose: Separating auth logic from UI enables independent testing and keeps each plan within scope. The UI plan (01-04) depends on these exports.
Output: Tested auth service, useAuth hook, and callback route ready for UI consumption.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/lib/supabase/client.ts:
export function createClient(): SupabaseClient

From src/lib/supabase/server.ts:
export async function createClient(): Promise<SupabaseClient>

From src/lib/utils.ts:
export function cn(...inputs: ClassValue[]): string

From src/env.ts:
export const env (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY)

<!-- From Plan 02 -->
From src/types/auth.ts:
export type UserRole = "homebuyer" | "renter" | "seller" | "landlord" | "agent" | "service_provider"
export type VerificationLevel = "basic" | "standard" | "enhanced" | "professional"
export type Profile (id, display_name, active_role, verification_level, avatar_url, phone, ...)

From src/lib/constants.ts:
export const ROLES: { value: UserRole; label: string; description: string; icon: string }[]
export const AUTH_ROUTES: string[]
export const PUBLIC_ROUTES: string[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth service with all operations and tests</name>
  <files>
    britv3.0/src/services/auth/auth-service.ts,
    britv3.0/src/__tests__/auth/signup.test.ts,
    britv3.0/src/__tests__/auth/oauth.test.ts,
    britv3.0/src/__tests__/auth/password-reset.test.ts,
    britv3.0/src/__tests__/auth/verify-email.test.ts
  </files>
  <behavior>
    - signUp(email, password, displayName) calls supabase.auth.signUp with emailRedirectTo, returns { user, error }
    - signIn(email, password) calls supabase.auth.signInWithPassword, returns { user, session, error }
    - signInWithOAuth(provider) calls supabase.auth.signInWithOAuth with PKCE options, redirectTo callback URL
    - signOut() calls supabase.auth.signOut
    - resetPassword(email) calls supabase.auth.resetPasswordForEmail with redirect URL
    - updatePassword(newPassword) calls supabase.auth.updateUser
  </behavior>
  <action>
1. Write tests first using the Supabase mock from `__tests__/mocks/supabase.ts`:
   - `signup.test.ts`: Test signUp calls supabase correctly, handles errors (weak password, duplicate email)
   - `oauth.test.ts`: Test signInWithOAuth calls with correct provider ('google', 'apple'), PKCE options
   - `password-reset.test.ts`: Test resetPassword sends email, updatePassword changes password
   - `verify-email.test.ts`: Test callback route handles code exchange success and error

2. Create `src/services/auth/auth-service.ts`:
   - `signUp(email: string, password: string, displayName: string)` -- calls `supabase.auth.signUp` with `data: { display_name: displayName }` in options and `emailRedirectTo` pointing to `/auth/callback`
   - `signIn(email: string, password: string)` -- calls `supabase.auth.signInWithPassword`
   - `signInWithOAuth(provider: 'google' | 'apple')` -- calls `supabase.auth.signInWithOAuth` with `redirectTo: origin + '/auth/callback'`, `queryParams: { access_type: 'offline', prompt: 'consent' }` for Google
   - `signOut()` -- calls `supabase.auth.signOut`
   - `resetPassword(email: string)` -- calls `supabase.auth.resetPasswordForEmail` with `redirectTo: origin + '/reset-password'`
   - `updatePassword(password: string)` -- calls `supabase.auth.updateUser({ password })`
   - `getUser()` -- calls `supabase.auth.getUser()` (NOT getSession)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/auth/signup.test.ts src/__tests__/auth/oauth.test.ts src/__tests__/auth/password-reset.test.ts src/__tests__/auth/verify-email.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Auth service has all 7 operations (signUp, signIn, signInWithOAuth, signOut, resetPassword, updatePassword, getUser). All 4 test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAuth hook and PKCE callback route</name>
  <files>
    britv3.0/src/hooks/useAuth.ts,
    britv3.0/src/app/auth/callback/route.ts,
    britv3.0/src/__tests__/auth/session.test.ts
  </files>
  <behavior>
    - useAuth hook provides { user, loading, signIn, signUp, signOut, signInWithOAuth } state
    - Hook initializes with loading=true, resolves user from getUser()
    - Hook subscribes to onAuthStateChange for real-time updates
    - PKCE callback route exchanges code for session and redirects to /dashboard
    - Callback handles error by redirecting to /login?error=auth_callback_error
  </behavior>
  <action>
1. Write `session.test.ts`: Test useAuth hook initializes with loading state, resolves user, handles auth state changes

2. Create `src/hooks/useAuth.ts`:
   - Uses `useState` for `user` and `loading`
   - On mount, calls `supabase.auth.getUser()` to initialize
   - Subscribes to `supabase.auth.onAuthStateChange` for real-time updates
   - Exposes auth operations from auth-service
   - Cleans up subscription on unmount

3. Create `src/app/auth/callback/route.ts`:
   - GET handler that reads `code` from searchParams
   - Calls `supabase.auth.exchangeCodeForSession(code)` via server client
   - On success: redirect to `/dashboard` (or `next` searchParam)
   - On error: redirect to `/login?error=auth_callback_error`
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0/britv3.0 && SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/auth/session.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>useAuth hook manages auth state with real-time updates, callback route exchanges PKCE codes. Session test passes.</done>
</task>

</tasks>

<verification>
1. All auth tests pass: `SKIP_ENV_VALIDATION=true pnpm vitest run src/__tests__/auth/`
2. `pnpm build` passes
3. Auth service exports all 7 operations
4. useAuth hook exports correctly
5. Callback route handles code exchange
</verification>

<success_criteria>
- Auth service correctly calls Supabase Auth methods for all operations
- useAuth hook initializes user state and subscribes to changes
- PKCE callback exchanges code for session
- All auth tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

---
phase: 14-landlord-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/app/(protected)/dashboard/landlord/tenants/page.tsx
  - src/app/(protected)/dashboard/landlord/tenants/[applicationId]/page.tsx
  - src/app/(protected)/dashboard/landlord/tenants/[applicationId]/decision/page.tsx
  - src/app/(protected)/dashboard/landlord/tenants/[tenancyId]/agreement/page.tsx
  - src/components/landlord/ApplicationPipelineCard.tsx
  - src/components/landlord/TenancyAgreementPDF.tsx
autonomous: true
requirements:
  - LD-03
  - LD-04
  - LD-14
  - LD-21
  - LD-22

must_haves:
  truths:
    - "Tenant Screening page shows a Kanban pipeline: Received → Shortlisted → Referencing → Approved/Rejected"
    - "Application Detail shows full applicant data with credit check and references status"
    - "Accept/Reject action fires email notification to applicant via Resend"
    - "Tenancy Agreement page generates a PDF using @react-pdf/renderer (client-side only, ssr:false)"
    - "pnpm build passes"
  artifacts:
    - path: "src/app/(protected)/dashboard/landlord/tenants/page.tsx"
      provides: "9.6 Tenant Screening — Kanban pipeline with real tenant_applications data"
    - path: "src/app/(protected)/dashboard/landlord/tenants/[applicationId]/page.tsx"
      provides: "9.7 Application Detail — full applicant info, status history"
    - path: "src/app/(protected)/dashboard/landlord/tenants/[applicationId]/decision/page.tsx"
      provides: "9.8 Accept/Reject with reason input"
    - path: "src/app/(protected)/dashboard/landlord/tenants/[tenancyId]/agreement/page.tsx"
      provides: "9.9 Tenancy Agreement — PDF generation + upload"
    - path: "src/components/landlord/TenancyAgreementPDF.tsx"
      provides: "@react-pdf/renderer PDF component (use client, ssr:false)"
  key_links:
    - from: "src/app/(protected)/dashboard/landlord/tenants/page.tsx"
      to: "src/services/landlord/tenant-application-service.ts"
      via: "listApplications(supabase) called in Server Component"
      pattern: "listApplications"
    - from: "src/app/(protected)/dashboard/landlord/tenants/[applicationId]/decision/page.tsx"
      to: "src/services/landlord/tenant-application-service.ts"
      via: "acceptApplication / rejectApplication mutation"
      pattern: "acceptApplication|rejectApplication"
---

<objective>
Build the tenant screening pipeline (9.6–9.9): Kanban application view, application detail, accept/reject decision page, and tenancy agreement PDF generation.

Purpose: The tenant lifecycle from application to signed agreement. Uses the new tenant-application-service from 14-02 and @react-pdf/renderer for the agreement PDF.

Output: 4 pages + 2 components covering the full tenant onboarding workflow
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-landlord-dashboard/14-CONTEXT.md
@.planning/phases/14-landlord-dashboard/14-RESEARCH.md
@src/services/landlord/tenant-application-service.ts
@src/services/landlord/tenancy-service.ts
@src/types/landlord.ts
@src/app/(protected)/dashboard/landlord/tenants/page.tsx
@src/components/landlord/TenancyForm.tsx
@src/components/landlord/TenancyStatusBadge.tsx
@.planning/phases/14-landlord-dashboard/stitch/tenant-screening.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenant Screening pipeline (9.6), Application Detail (9.7), Accept/Reject (9.8)</name>
  <files>
    src/app/(protected)/dashboard/landlord/tenants/page.tsx,
    src/app/(protected)/dashboard/landlord/tenants/[applicationId]/page.tsx,
    src/app/(protected)/dashboard/landlord/tenants/[applicationId]/decision/page.tsx,
    src/components/landlord/ApplicationPipelineCard.tsx
  </files>
  <action>
**CRITICAL: Read stitch/tenant-screening.html before building.** Use `#1B4D3E` (not orange). Replace material-symbols with Lucide.

**ApplicationPipelineCard.tsx** — card component for one application in the Kanban:
Props: `{ application: TenantApplication; onMoveToNextStage: () => void }`
Shows: applicant name, email, monthly_income, employment_status, credit_check badge (pending/passed/failed), references badge, action button ("Review" → links to detail page)
Status badge colours: received=gray, shortlisted=blue, referencing=amber, approved=green, rejected=red

**tenants/page.tsx (9.6)** — Server Component with client Kanban:
1. Server: `const applications = await listApplications(supabase)` — real query
2. Pass to `TenantScreeningClient` (client wrapper with React Query for optimistic updates)
3. Kanban columns from Stitch design: Received | Shortlisted | Referencing | Approved | Rejected
4. Each column shows count badge + list of `ApplicationPipelineCard`s
5. Header: "Tenant Screening" title, total count, "Add Application" button (opens Sheet form for manual entry)
6. Manual application entry (Sheet): applicant_name, applicant_email, property_id (Select from landlord's properties), monthly_income, employment_status → calls Supabase insert to tenant_applications

**tenants/[applicationId]/page.tsx (9.7)** — Server Component:
1. Fetch `getApplicationById(supabase, applicationId)`
2. Also fetch the associated property for address display
3. Two-column layout:
   - Left: Applicant details (name, email, phone, income, employment, credit check status, references status)
   - Right: Application timeline / status history, actions panel
4. Actions: "Move to Shortlisted", "Move to Referencing", "Make Decision" (→ links to decision page), credit check status update dropdown
5. Notes textarea → saves to `application.notes` via Supabase update (optimistic)

**tenants/[applicationId]/decision/page.tsx (9.8)** — `"use client"` form:
Two tabs: Accept | Reject
Accept tab:
- Confirmation text: "Accept {applicantName} for {propertyAddress}?"
- "This will send an acceptance email to {applicantEmail}"
- Submit button → calls `acceptApplication(supabase, applicationId)` → redirect to `/dashboard/landlord/tenants` + toast "Application accepted — email sent to {name}"

Reject tab:
- Textarea for rejection reason (required, min 10 chars)
- "This will send a rejection email to {applicantEmail}"
- Submit button → calls `rejectApplication(supabase, applicationId, reason)` → redirect + toast

Use optimistic mutation pattern from RESEARCH.md. Handle loading state on submit button (disabled + spinner).
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>All 3 pages and ApplicationPipelineCard exist. Kanban renders real applications from Supabase. Accept/Reject mutation calls correct service functions. pnpm build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tenancy Agreement creation and PDF generation (9.9)</name>
  <files>
    src/app/(protected)/dashboard/landlord/tenants/[tenancyId]/agreement/page.tsx,
    src/components/landlord/TenancyAgreementPDF.tsx
  </files>
  <action>
**IMPORTANT anti-pattern:** `@react-pdf/renderer` cannot be server-rendered. All PDF code must be `"use client"`. Use `dynamic(() => import('./TenancyAgreementPDF'), { ssr: false })` when embedding inside any Server Component tree.

**TenancyAgreementPDF.tsx** — `"use client"` PDF component:
```typescript
"use client";
import { Document, Page, Text, View, StyleSheet, PDFDownloadLink } from "@react-pdf/renderer";

const styles = StyleSheet.create({
  page: { padding: 50, fontFamily: "Helvetica", fontSize: 11, lineHeight: 1.5 },
  title: { fontSize: 16, fontWeight: "bold", textAlign: "center", marginBottom: 20 },
  section: { marginBottom: 12 },
  heading: { fontSize: 12, fontWeight: "bold", marginBottom: 4 },
  body: { fontSize: 10 },
  clause: { marginBottom: 8, paddingLeft: 10 },
});
```
Props: `{ tenancy: Tenancy; landlordName: string; propertyAddress: string }`
PDF content (standard AST structure):
1. Title: "Assured Shorthold Tenancy Agreement"
2. Parties: Landlord details, Tenant details
3. Property address
4. Tenancy period: start date – end date
5. Rent amount, payment day, payment method
6. Deposit amount + scheme name
7. 12 standard clauses (tenant obligations, landlord obligations, deposit rules, repairs, access, termination)
8. Signature lines for landlord and tenant

Export: `TenancyAgreementPDFDownload` component that wraps in `PDFDownloadLink`.

**tenants/[tenancyId]/agreement/page.tsx (9.9)** — Server Component outer, client inner:
1. Server: fetch tenancy by `tenancyId` from `tenancy-service.ts getTenancyById(supabase, tenancyId)`
2. Also fetch landlord profile for name
3. Render page with two sections:
   - **Upload existing agreement:** Drag-drop file upload (react-dropzone) → uploads to `landlord-documents/${userId}/agreements/${tenancyId}.pdf` → updates `property_documents` table via `document-service.ts uploadDocument()`
   - **Generate new agreement:** Shows tenancy details form (pre-filled from tenancy record), then `const AgreementPDFDownload = dynamic(() => import("@/components/landlord/TenancyAgreementPDF"), { ssr: false })` — renders PDFDownloadLink button
4. After generating PDF, option to save storage path to property_documents: `{ category: 'lease_agreement', property_id: tenancy.property_id, document_url: storedPath }`
5. Breadcrumb: Tenants / {tenantName} / Agreement
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>TenancyAgreementPDF.tsx uses "use client" directive. Agreement page dynamically imports PDF component with ssr:false. pnpm build passes without @react-pdf/renderer SSR error.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes (no SSR errors from @react-pdf/renderer)
2. All 4 page files exist at correct paths
3. Kanban pipeline groups applications by status
4. Accept/Reject calls tenant-application-service functions (not direct Supabase calls)
5. PDF component is "use client" only
</verification>

<success_criteria>
- Tenant Screening shows Kanban pipeline from real tenant_applications table
- Application Detail shows full applicant data
- Accept/Reject fires Resend email notification via service
- Tenancy Agreement generates PDF with @react-pdf/renderer (client-side only)
- No SSR errors in build
</success_criteria>

<output>
After completion, create `.planning/phases/14-landlord-dashboard/14-04-SUMMARY.md`
</output>

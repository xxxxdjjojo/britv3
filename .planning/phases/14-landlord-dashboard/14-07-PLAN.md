---
phase: 14-landlord-dashboard
plan: 07
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/app/(protected)/dashboard/landlord/maintenance/page.tsx
  - src/app/(protected)/dashboard/landlord/maintenance/[id]/page.tsx
  - src/app/(protected)/dashboard/landlord/maintenance/[id]/assign/page.tsx
  - src/components/landlord/MaintenancePriorityBadge.tsx
  - src/components/landlord/TradesPersonAssignModal.tsx
  - src/app/api/landlord/maintenance/route.ts
  - src/app/api/landlord/maintenance/[id]/status/route.ts
  - src/app/api/landlord/maintenance/[id]/notes/route.ts
  - src/app/api/landlord/maintenance/[id]/assign/route.ts
autonomous: true
requirements:
  - LD-17
  - LD-18
  - LD-28
  - LD-29

must_haves:
  truths:
    - "Maintenance Inbox shows all maintenance_requests across portfolio, sorted by priority (Urgent first)"
    - "Individual Request shows status, photos, notes, and assigned tradesperson"
    - "Assign Tradesperson searches existing marketplace providers and assigns to request"
    - "Status updates (open → in_progress → resolved) happen via optimistic mutations"
    - "pnpm build passes"
  artifacts:
    - path: "src/app/(protected)/dashboard/landlord/maintenance/page.tsx"
      provides: "9.15 Maintenance Inbox — list with priority/status filters, real data"
    - path: "src/app/(protected)/dashboard/landlord/maintenance/[id]/page.tsx"
      provides: "9.16 Individual Request — details, photo gallery, status timeline"
    - path: "src/app/(protected)/dashboard/landlord/maintenance/[id]/assign/page.tsx"
      provides: "9.17 Assign Tradesperson — marketplace provider search + assignment"
    - path: "src/components/landlord/TradesPersonAssignModal.tsx"
      provides: "Modal for searching and assigning a tradesperson from marketplace"
  key_links:
    - from: "src/app/(protected)/dashboard/landlord/maintenance/page.tsx"
      to: "src/services/landlord/maintenance-service.ts"
      via: "getMaintenanceRequests(supabase) — portfolio-wide query"
      pattern: "getMaintenanceRequests"
    - from: "src/app/(protected)/dashboard/landlord/maintenance/[id]/assign/page.tsx"
      to: "marketplace providers table"
      via: "search providers by category (tradesperson), filtered by location"
      pattern: "providers.*category"
---

<objective>
Build the maintenance management pages (9.15–9.17): portfolio-wide inbox, individual request detail, and tradesperson assignment from the marketplace.

Purpose: Landlords need a cross-portfolio view of all maintenance issues. The assign tradesperson flow reuses the existing Epic 4 marketplace provider data.

Output: 3 pages + 2 components. Maintenance inbox replaces the mock-data Phase 6 page with real queries.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-landlord-dashboard/14-CONTEXT.md
@.planning/phases/14-landlord-dashboard/14-RESEARCH.md
@src/services/landlord/maintenance-service.ts
@src/types/landlord.ts
@src/app/(protected)/dashboard/landlord/maintenance/page.tsx
@src/components/landlord/MaintenanceForm.tsx
@src/components/landlord/MaintenanceList.tsx
@src/components/landlord/MaintenanceStatusBadge.tsx
@src/components/landlord/ProviderAssignment.tsx
@.planning/phases/14-landlord-dashboard/stitch/maintenance-requests.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maintenance Inbox (9.15) and Individual Request (9.16)</name>
  <files>
    src/app/(protected)/dashboard/landlord/maintenance/page.tsx,
    src/app/(protected)/dashboard/landlord/maintenance/[id]/page.tsx,
    src/components/landlord/MaintenancePriorityBadge.tsx
  </files>
  <action>
**CRITICAL: Read stitch/maintenance-requests.html before building.** Use `#1B4D3E` as primary. Replace orange. Replace material-symbols with Lucide.

**MaintenancePriorityBadge.tsx** — badge component:
Props: `{ priority: "urgent" | "high" | "routine" | "low" }`
Colours: urgent=red filled, high=orange, routine=blue, low=gray
Includes priority icon: urgent=AlertTriangle, high=ArrowUp, routine=Clock, low=ArrowDown

**maintenance/page.tsx (9.15 — Maintenance Inbox)** — Server Component + client wrapper:
1. Server: `const requests = await getMaintenanceRequests(supabase)` — portfolio-wide, no property filter
2. Pass `initialData` to `MaintenanceInboxClient`
3. Header from Stitch: "Maintenance Requests" + count badge + "New Request" button
4. Filter row: Priority (All | Urgent | Routine) | Status (All | Open | In Progress | Resolved) | Property (All | dropdown)
5. List view: each item shows:
   - `MaintenancePriorityBadge` (top-right)
   - `MaintenanceStatusBadge` from existing component
   - Category icon (Pipe for plumbing, Zap for electrical, Thermometer for heating, Wrench for general)
   - Title + description preview (50 chars)
   - Property address + tenant name
   - Created date (relative: "2 days ago")
   - Assigned tradesperson (if any) or "Unassigned" in amber
   - Click: navigates to `/dashboard/landlord/maintenance/${id}`
6. "New Request" opens Sheet form with `MaintenanceForm` component (existing) — save inserts to `maintenance_requests`

**maintenance/[id]/page.tsx (9.16)** — Server Component:
1. Fetch: `getMaintenanceRequestById(supabase, id)` — add this function to maintenance-service if not exists
2. Also fetch assigned provider if `assigned_provider_id` exists
3. Layout (2-col on lg+):
   - Left: title, description, priority badge, status badge, category, property, tenant contact, created date
   - Right: photo gallery (if `photo_urls` exist — render as `<img>` grid with Supabase signed URLs), notes textarea
4. Status timeline: visual stepper showing open → in_progress → resolved with timestamps
5. Assign Tradesperson button → navigates to `/dashboard/landlord/maintenance/${id}/assign`
6. Status update dropdown (Open / In Progress / Resolved) → fires PATCH to `/api/landlord/maintenance/${id}/status`
7. Notes: textarea bound to `maintenance_requests.notes` — auto-save on blur via PATCH API
8. Photo upload: react-dropzone → uploads to `maintenance-photos` bucket (existing)

**API routes:**
- `src/app/api/landlord/maintenance/route.ts` — GET (list), POST (create new request)
- `src/app/api/landlord/maintenance/[id]/status/route.ts` — PATCH (update status)
- `src/app/api/landlord/maintenance/[id]/notes/route.ts` — PATCH (update notes)

All routes: `supabase.auth.getUser()` first.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10 && grep -r "getMaintenanceRequests" src/app/\(protected\)/dashboard/landlord/maintenance/page.tsx</automated>
  </verify>
  <done>Maintenance Inbox shows all requests from maintenance_requests table (not mocks). Priority and status filters work. Individual request shows photo gallery with signed URLs. pnpm build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Assign Tradesperson (9.17)</name>
  <files>
    src/app/(protected)/dashboard/landlord/maintenance/[id]/assign/page.tsx,
    src/components/landlord/TradesPersonAssignModal.tsx
  </files>
  <action>
**Context from RESEARCH.md:** This page reuses the Epic 4 marketplace provider data. Query the `providers` table (or `service_provider_profiles`) filtered by category (plumber, electrician, builder, etc.) and the property's location/postcode for proximity matching.

**TradesPersonAssignModal.tsx** — `"use client"` component:
Not a modal — a searchable list component:
Props: `{ maintenanceCategory: string; propertyPostcode: string; currentAssignedId?: string; onAssign: (providerId: string) => void }`
1. Search input: searches providers by name
2. Category filter chips: same as maintenance_requests.category (plumbing, electrical, heating, general)
3. Provider list: name, rating (stars), specialty, distance (from postcode — show "~{N} miles" or "Area: {city}")
4. Each provider card: "Assign" button → fires `onAssign(providerId)`
5. Currently assigned: shown with green checkmark + "Reassign" option

**maintenance/[id]/assign/page.tsx (9.17)** — Server Component:
1. Fetch maintenance request: `getMaintenanceRequestById(supabase, id)`
2. Fetch property for postcode
3. Fetch providers filtered by `category = maintenanceRequest.category`:
   ```typescript
   const { data: providers } = await supabase
     .from("service_provider_profiles")  // or providers table from Epic 4
     .select("id, business_name, category, average_rating, city")
     .eq("category", mapMaintenanceCategoryToProviderCategory(request.category))
     .order("average_rating", { ascending: false })
     .limit(20);
   ```
   Note: map maintenance category to marketplace category (e.g. 'plumbing' → 'plumber', 'electrical' → 'electrician')
4. Pass providers to `TradesPersonAssignModal` component
5. On assign: PATCH to `/api/landlord/maintenance/${id}/assign` with `{ provider_id }`
   - Updates `maintenance_requests.assigned_provider_id` and `status = 'in_progress'`
   - Optional: create a Supabase message to the provider (if messaging infrastructure exists)
6. After assign: toast "Tradesperson assigned" + redirect to `/dashboard/landlord/maintenance/${id}`

**API route** `src/app/api/landlord/maintenance/[id]/assign/route.ts` — PATCH:
- `supabase.auth.getUser()`
- Verify maintenance request belongs to landlord
- Update `assigned_provider_id` and `status = 'in_progress'`
- Return updated request

Note: If `service_provider_profiles` table doesn't exist (Epic 4 may use different table name), use `providers` table. Check actual table name in existing codebase. Add a TODO comment if unsure, and fall back to a simple text-input for provider name with a note: "Marketplace integration pending Phase 4 data."
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10 && grep -r "service_provider_profiles\|providers" "src/app/(protected)/dashboard/landlord/maintenance/[id]/assign/page.tsx"</automated>
  </verify>
  <done>Assign Tradesperson page lists providers from marketplace data. Assignment updates maintenance request in Supabase. pnpm build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. Maintenance inbox queries `maintenance_requests` (no mock data)
3. Photo gallery uses signed URLs (grep `getPublicUrl` in maintenance pages — should be none)
4. Status update API route has `supabase.auth.getUser()` guard
5. Assign tradesperson queries provider data from existing marketplace tables
</verification>

<success_criteria>
- Maintenance inbox shows real requests sorted by priority (urgent first)
- Individual request shows photo gallery via signed URLs
- Status updates propagate to Supabase via API route
- Assign tradesperson pulls from marketplace provider data
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-landlord-dashboard/14-07-SUMMARY.md`
</output>

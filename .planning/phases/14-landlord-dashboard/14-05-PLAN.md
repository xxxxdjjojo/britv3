---
phase: 14-landlord-dashboard
plan: 05
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/app/(protected)/dashboard/landlord/rent/page.tsx
  - src/app/(protected)/dashboard/landlord/rent/[propertyId]/page.tsx
  - src/app/(protected)/dashboard/landlord/deposits/page.tsx
  - src/components/landlord/RentPaymentRow.tsx
  - src/components/landlord/DepositCard.tsx
autonomous: true
requirements:
  - LD-05
  - LD-15
  - LD-23
  - LD-24
  - LD-25

must_haves:
  truths:
    - "Rent Collection Overview groups real financial_entries by paid/partial/overdue status"
    - "Per-property rent page shows payment history for that property"
    - "Log Payment form inserts to financial_entries and updates UI optimistically"
    - "Deposit Management lists all deposit_registrations with scheme, amount, status"
    - "pnpm build passes"
  artifacts:
    - path: "src/app/(protected)/dashboard/landlord/rent/page.tsx"
      provides: "9.10 Rent Collection Overview — summary cards + property list"
    - path: "src/app/(protected)/dashboard/landlord/rent/[propertyId]/page.tsx"
      provides: "9.11 Individual Property Rent — payment history + log payment form"
    - path: "src/app/(protected)/dashboard/landlord/deposits/page.tsx"
      provides: "9.25 Deposit Management — list + register/update deposit forms"
    - path: "src/components/landlord/RentPaymentRow.tsx"
      provides: "Table row for one rent payment with status badge and actions"
    - path: "src/components/landlord/DepositCard.tsx"
      provides: "Card showing deposit registration with scheme, status, dates"
  key_links:
    - from: "src/app/(protected)/dashboard/landlord/rent/page.tsx"
      to: "src/services/landlord/financial-service.ts"
      via: "getRentCollection(supabase) — queries financial_entries WHERE category='rent'"
      pattern: "getRentCollection"
    - from: "src/app/(protected)/dashboard/landlord/deposits/page.tsx"
      to: "src/services/landlord/deposit-service.ts"
      via: "listDeposits(supabase)"
      pattern: "listDeposits"
---

<objective>
Build rent collection pages (9.10, 9.11) and deposit management (9.25). These pages surface the financial health of the landlord's portfolio using real payment data from financial_entries and deposit_registrations.

Purpose: Core financial workflow. Landlords must be able to see who has paid, log new payments, and track deposit registrations — all backed by real Supabase data.

Output: 3 page files + 2 components, all querying real data with optimistic UI for mutations
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-landlord-dashboard/14-CONTEXT.md
@.planning/phases/14-landlord-dashboard/14-RESEARCH.md
@src/services/landlord/financial-service.ts
@src/services/landlord/deposit-service.ts
@src/types/landlord.ts
@src/app/(protected)/dashboard/landlord/rent-collection/page.tsx
@src/components/landlord/RentStatusIndicator.tsx
@src/components/landlord/FinancialEntryForm.tsx
@src/components/landlord/FinancialSummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rent Collection Overview (9.10) and Per-Property Rent (9.11)</name>
  <files>
    src/app/(protected)/dashboard/landlord/rent/page.tsx,
    src/app/(protected)/dashboard/landlord/rent/[propertyId]/page.tsx,
    src/components/landlord/RentPaymentRow.tsx
  </files>
  <action>
**IMPORTANT pitfall (from RESEARCH.md):** Rent Collection must query `financial_entries WHERE category = 'rent'`, joined to `tenancies` for tenant name. Do NOT query `tenancies` alone for payment status. The `financial_entries.payment_status` field holds the payment state.

**RentPaymentRow.tsx** — table row component:
Props: `{ entry: FinancialEntry & { tenant_name: string; property_address: string }; onMarkPaid: (id: string) => void }`
Columns: Property address | Tenant name | Due date | Amount (£X,XXX) | Status badge | Actions
Status badge colours: paid=green, partial=amber, overdue=red (use `RentStatusIndicator` component from Phase 6)
Actions: "Mark Paid" button (shown only for partial/overdue), amount paid input for partial

**rent/page.tsx (9.10)** — Server Component + client wrapper:
1. Server: `const rentGroups = await getRentCollection(supabase)` — real query from financial-service
2. Pass `initialData={rentGroups}` to `RentCollectionClient`
3. Summary cards (4): Total Expected This Month | Total Collected | Outstanding | Overdue Count
4. Three tabs using Shadcn Tabs: All | Overdue | Upcoming
5. Table per tab using `RentPaymentRow` component
6. Each property row links to `/dashboard/landlord/rent/${propertyId}`
7. "Log Payment" button opens Sheet form (uses existing `FinancialEntryForm` component, pre-filled with category='rent')

Client wrapper (`RentCollectionClient`) uses React Query:
```typescript
const { data } = useQuery({
  queryKey: ["rent-collection"],
  queryFn: () => fetch("/api/landlord/rent").then(r => r.json()),
  initialData,
});
const markPaidMutation = useMutation({
  mutationFn: (entryId: string) => fetch(`/api/landlord/rent/${entryId}/mark-paid`, { method: "PATCH" }),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["rent-collection"] });
    toast.success("Payment marked as paid");
  },
});
```

**API route** `src/app/api/landlord/rent/route.ts` — GET handler:
- `supabase.auth.getUser()` defense-in-depth
- Calls `getRentCollection(supabase)` and returns JSON

**API route** `src/app/api/landlord/rent/[entryId]/mark-paid/route.ts` — PATCH handler:
- Updates `financial_entries SET payment_status = 'paid', paid_at = NOW() WHERE id = entryId AND landlord_id = auth.uid()`

**rent/[propertyId]/page.tsx (9.11)** — Server Component:
1. Fetch property details + rent payments for this property: `getRentCollection(supabase, propertyId)`
2. Also fetch current tenancy: `getTenanciesByProperty(supabase, propertyId)` (latest active)
3. Header: property address + current tenant name + current rent amount
4. Payment history table (last 12 months) with status badges using `RentPaymentRow`
5. "Log Payment" button → opens Sheet with `FinancialEntryForm` pre-filled with `property_id` and `category = 'rent'`
6. Summary stats: Total collected (year), Total owed (outstanding), Last payment date
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Rent collection pages query financial_entries (not tenancies) for payment status. Mark Paid mutation updates optimistically. pnpm build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Deposit Management (9.25)</name>
  <files>
    src/app/(protected)/dashboard/landlord/deposits/page.tsx,
    src/components/landlord/DepositCard.tsx
  </files>
  <action>
**DepositCard.tsx** — card component for one deposit registration:
Props: `{ deposit: DepositRegistration & { tenancy: { tenant_name: string; property_address: string } } }`
Card shows:
- Property address (top)
- Tenant name
- Deposit amount: `£{deposit.amount.toLocaleString()}`
- Scheme: TDS / DPS / mydeposits / other (with scheme logo placeholder badge)
- Registration date: formatted UK date (e.g. "1 Jan 2026") or "Not registered" in amber
- Prescribed info sent: date or "Not sent" in amber
- Status badge: pending=amber, registered=green, returned=gray, disputed=red
- Scheme reference (if registered)
- Actions: "Edit" button, "Mark Registered" if pending

**deposits/page.tsx (9.25)** — Server Component + client wrapper:
1. Server: `const deposits = await listDeposits(supabase)` — queries deposit_registrations with tenancy join
2. Summary stats: Total Deposits Held (£sum), Registered Count, Pending Count, Disputed Count
3. Compliance warning banner: if any deposits are unregistered >30 days (registration_date is null OR registration_date > tenancy.lease_start_date + 30 days), show amber alert
4. Grid of `DepositCard` components (2-col on md+, 1-col on mobile)
5. "Register New Deposit" button → Sheet form:
   - Select tenancy (dropdown from landlord's active tenancies)
   - Amount (number input)
   - Scheme (Select: TDS | DPS | mydeposits | other)
   - Scheme reference (text)
   - Registration date (date input)
   - Prescribed info sent date (date input)
   - Submit → calls `createDepositRegistration(supabase, data)` → optimistic update + toast

**Edit deposit** — same Sheet form pre-filled, calls `updateDeposit(supabase, depositId, updates)`

**API routes** needed:
- `src/app/api/landlord/deposits/route.ts` — GET (list) + POST (create)
- `src/app/api/landlord/deposits/[id]/route.ts` — PATCH (update)

Each API route must call `supabase.auth.getUser()` before any DB operation.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Deposit Management page lists all deposit_registrations with correct status. Register/edit forms call deposit-service. Compliance warning banner appears for unregistered deposits. pnpm build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. Rent collection pages query `financial_entries WHERE category = 'rent'` (verify via grep)
3. Deposit management page exists at `/dashboard/landlord/deposits`
4. All API routes have `supabase.auth.getUser()` defense-in-depth
5. Mark Paid and Register Deposit mutations update UI optimistically
</verification>

<success_criteria>
- Rent collection shows real payments grouped by paid/partial/overdue from financial_entries
- Mark Paid action updates payment status in Supabase and reflects optimistically in UI
- Deposit Management lists all deposit_registrations with compliance warning for unregistered deposits
- All API routes authenticated with server-side getUser() check
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-landlord-dashboard/14-05-SUMMARY.md`
</output>

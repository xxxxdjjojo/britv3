---
phase: 14-landlord-dashboard
plan: 09
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/app/(protected)/dashboard/landlord/find-agent/page.tsx
  - src/app/(protected)/dashboard/landlord/find-tradespeople/page.tsx
  - src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-in/page.tsx
  - src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-out/page.tsx
  - src/components/landlord/InventoryRoomForm.tsx
autonomous: true
requirements:
  - LD-16
  - LD-17
  - LD-18
  - LD-19

must_haves:
  truths:
    - "Find Letting Agent lists real agents from marketplace data filtered by category=letting_agent"
    - "Find Tradespeople lists real providers from marketplace filtered by tradesperson categories"
    - "Inventory Check-In form creates an inventory_report record and allows room-by-room entry"
    - "Inventory Check-Out form loads the matching check-in report for comparison"
    - "Photo uploads store in landlord-documents bucket (private, signed URL access)"
    - "pnpm build passes"
  artifacts:
    - path: "src/app/(protected)/dashboard/landlord/find-agent/page.tsx"
      provides: "9.21 Find Letting Agent — marketplace browse filtered to letting agents"
    - path: "src/app/(protected)/dashboard/landlord/find-tradespeople/page.tsx"
      provides: "9.22 Find Tradespeople — marketplace browse for landlord maintenance context"
    - path: "src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-in/page.tsx"
      provides: "9.23 Inventory Check-In — room-by-room report with photo uploads"
    - path: "src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-out/page.tsx"
      provides: "9.24 Inventory Check-Out — comparison against check-in report"
    - path: "src/components/landlord/InventoryRoomForm.tsx"
      provides: "Reusable room entry form with condition select, notes, photo upload"
  key_links:
    - from: "src/app/(protected)/dashboard/landlord/find-agent/page.tsx"
      to: "marketplace providers table (Epic 4)"
      via: "query providers WHERE category = 'letting_agent'"
      pattern: "letting_agent"
    - from: "src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-in/page.tsx"
      to: "src/services/landlord/inventory-service.ts"
      via: "createInventoryReport(supabase, data)"
      pattern: "createInventoryReport"
---

<objective>
Build the find-agent/find-tradespeople browse pages (reusing marketplace data) and the inventory check-in/check-out report pages.

Purpose: These 4 pages complete the "tools" section of the landlord dashboard. The marketplace browse pages reuse Epic 4 provider data with landlord-specific context. Inventory reports use the new inventory_reports table from Plan 14-01.

Output: 4 page files + 1 component. These pages can run in parallel with plans 14-04 through 14-08.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-landlord-dashboard/14-CONTEXT.md
@.planning/phases/14-landlord-dashboard/14-RESEARCH.md
@src/services/landlord/inventory-service.ts
@src/types/landlord.ts
@src/services/landlord/portfolio-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find Letting Agent (9.21) and Find Tradespeople (9.22)</name>
  <files>
    src/app/(protected)/dashboard/landlord/find-agent/page.tsx,
    src/app/(protected)/dashboard/landlord/find-tradespeople/page.tsx
  </files>
  <action>
Both pages follow the same browse pattern — they are landlord-context wrappers over the Epic 4 marketplace provider data.

**find-agent/page.tsx (9.21)** — Server Component:
1. Query letting agents from the marketplace providers table (use the same table/view as Epic 4 agent search — likely `service_provider_profiles` or `agents` — check existing codebase):
   ```typescript
   const { data: agents } = await supabase
     .from("service_provider_profiles")  // adjust table name to match Epic 4
     .select("id, business_name, tagline, city, average_rating, review_count, fee_percentage, fee_type, photo_url")
     .eq("category", "letting_agent")
     .order("average_rating", { ascending: false })
     .limit(20);
   ```
2. If the table doesn't exist yet (Epic 4 not fully built), gracefully show empty state with message "No letting agents registered yet — check back soon" rather than throwing.
3. Page header: "Find a Letting Agent" + subtitle: "Browse verified letting agents who can manage your properties"
4. Search input: filter by city/area (client-side filter on loaded data)
5. Provider cards (2-col md, 3-col lg): business name, city, rating (star display), review count, fee type (% of rent / fixed), "View Profile" button
6. "View Profile" links to `/marketplace/providers/{id}` (existing marketplace route from Epic 4) — opens in new tab if route exists
7. CTA section: "List with an agent" — explains that landlords can invite agents to manage properties via the messaging system

**find-tradespeople/page.tsx (9.22)** — Server Component + client filter:
1. Query tradesperson providers from marketplace:
   ```typescript
   const categories = ["plumber", "electrician", "builder", "decorator", "gas_engineer", "roofer", "cleaner", "locksmith"];
   const { data: tradespeople } = await supabase
     .from("service_provider_profiles")
     .select("id, business_name, category, city, average_rating, review_count, photo_url, bio")
     .in("category", categories)
     .order("average_rating", { ascending: false })
     .limit(50);
   ```
2. Client-side category filter chips: All | Plumber | Electrician | Builder | Gas Engineer | Other
3. Provider cards same design as find-agent
4. Context difference: header "Find Tradespeople" + subtitle "Find verified tradespeople for maintenance and repairs"
5. Quick link: "Report a maintenance issue" → `/dashboard/landlord/maintenance` (new request)
6. If no providers loaded: empty state with "Coming soon — tradespeople will be listed here once they register"

Both pages: if `service_provider_profiles` table doesn't exist (Epic 4 table may differ), add a graceful fallback. Check the actual table name in existing migrations before hardcoding.
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Both browse pages compile without errors. They query marketplace providers with category filter. Graceful empty state shown if no providers exist. pnpm build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory Check-In (9.23) and Check-Out (9.24)</name>
  <files>
    src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-in/page.tsx,
    src/app/(protected)/dashboard/landlord/inventory/[propertyId]/check-out/page.tsx,
    src/components/landlord/InventoryRoomForm.tsx
  </files>
  <action>
**InventoryRoomForm.tsx** — `"use client"` component for one room:
Props: `{ roomName: string; initialCondition?: string; initialNotes?: string; onUpdate: (data: RoomEntry) => void }`
Fields:
- Room name (text, pre-filled e.g. "Living Room")
- Condition (Select): Excellent | Good | Fair | Poor | Damaged
- Notes (Textarea): description of condition, fixtures, furnishings
- Photo upload: react-dropzone (accepts jpg/png, max 5 photos per room), shows thumbnail preview
- On photo upload: call `supabase.storage.from("landlord-documents").upload()` immediately, store signed URL in component state

**check-in/page.tsx (9.23)** — `"use client"` page (needs file state management):
1. Load property details via Server Component wrapper, pass to client
2. Default rooms list: ["Entrance Hall", "Living Room", "Kitchen", "Bathroom", "Bedroom 1", "Bedroom 2", "Garden/Outdoor", "Other"]
3. "Add Room" button to add custom rooms
4. For each room: renders `InventoryRoomForm`
5. Overall notes textarea
6. Save draft: saves to `inventory_reports` table with `status = 'draft'` via `createInventoryReport(supabase, { type: 'check_in', rooms: [...], status: 'draft' })`
7. Mark Complete: updates `status = 'complete'` + sets `completed_at = NOW()`
8. Download Report button: client-side PDF generation using @react-pdf/renderer showing all rooms with condition/notes (dynamic import, ssr:false)
9. Breadcrumb: Properties / {propertyAddress} / Check-In Report

**check-out/page.tsx (9.24)** — Server Component + client:
1. Server: fetch existing check-in report for this property: `getInventoryReports(supabase, propertyId)` filtered to `type = 'check_in'` and most recent `status = 'complete'`
2. Also fetch existing check-out report if draft exists
3. Side-by-side comparison layout (lg+ screens): Check-In | Check-Out columns
4. For each room: shows check-in condition/notes on left, check-out `InventoryRoomForm` on right
5. Difference highlighting: if check-out condition is worse than check-in, show amber/red indicator on that room
6. "Log Deduction" button per room (for deposit deduction claims): opens inline form with deduction amount + reason
7. Mark Complete: saves `status = 'complete'` on check-out report
8. PDF export: side-by-side PDF showing both reports (dynamic import, ssr:false)

**API routes:**
- `src/app/api/landlord/inventory/route.ts` — POST (create report)
- `src/app/api/landlord/inventory/[reportId]/route.ts` — PATCH (update), GET (fetch)
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Inventory check-in creates inventory_reports record with room data. Check-out loads matching check-in for comparison. Photo uploads to landlord-documents bucket. PDF export is ssr:false. pnpm build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. Find Agent and Find Tradespeople pages have graceful empty states (no throw if table missing)
3. Inventory check-in creates inventory_reports record via inventory-service
4. Photo uploads use private bucket (grep `getPublicUrl` in inventory pages — should be none)
5. PDF components in inventory pages use dynamic import with ssr:false
</verification>

<success_criteria>
- Find Agent/Tradespeople query marketplace provider data with category filter
- Graceful empty state shown when no providers exist
- Inventory Check-In creates real inventory_reports record
- Inventory Check-Out loads check-in data for side-by-side comparison
- Photos upload to private landlord-documents bucket
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-landlord-dashboard/14-09-SUMMARY.md`
</output>

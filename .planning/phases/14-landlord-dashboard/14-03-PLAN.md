---
phase: 14-landlord-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/app/(protected)/dashboard/landlord/layout.tsx
  - src/app/(protected)/dashboard/landlord/page.tsx
  - src/app/(protected)/dashboard/landlord/properties/page.tsx
  - src/app/(protected)/dashboard/landlord/properties/[id]/page.tsx
  - src/app/(protected)/dashboard/landlord/properties/add/page.tsx
  - src/app/(protected)/dashboard/landlord/properties/[id]/listing/page.tsx
  - src/components/landlord/LandlordSidebar.tsx
  - src/components/landlord/KpiCard.tsx
  - src/components/landlord/ComplianceAlertBanner.tsx
  - next.config.ts
autonomous: true
requirements:
  - LD-01
  - LD-02
  - LD-12
  - LD-13
  - LD-19
  - LD-20

must_haves:
  truths:
    - "Landlord navigates to /dashboard/landlord and sees dashboard home with real KPI cards"
    - "KPI cards show total properties, occupancy rate, monthly income, compliance alerts from Supabase"
    - "Portfolio view /dashboard/landlord/properties lists all properties with status badges, yield, rent"
    - "Sidebar navigation is present on all landlord pages (layout.tsx wraps all routes)"
    - "Add Property form at /dashboard/landlord/properties/add submits to Supabase"
    - "pnpm build passes"
  artifacts:
    - path: "src/app/(protected)/dashboard/landlord/layout.tsx"
      provides: "Sidebar shell wrapping all 29 landlord pages"
    - path: "src/app/(protected)/dashboard/landlord/page.tsx"
      provides: "9.1 Dashboard Home — KPI cards, compliance alerts, activity feed"
    - path: "src/app/(protected)/dashboard/landlord/properties/page.tsx"
      provides: "9.2 Portfolio View — property grid with sorting/filtering"
    - path: "src/app/(protected)/dashboard/landlord/properties/[id]/page.tsx"
      provides: "9.3 Individual Property — tenancy, income, documents, maintenance"
    - path: "src/app/(protected)/dashboard/landlord/properties/add/page.tsx"
      provides: "9.4 Add Property to Portfolio"
    - path: "src/app/(protected)/dashboard/landlord/properties/[id]/listing/page.tsx"
      provides: "9.5 Create Rental Listing"
    - path: "src/components/landlord/LandlordSidebar.tsx"
      provides: "Sidebar navigation component (desktop permanent, mobile Sheet drawer)"
    - path: "src/components/landlord/KpiCard.tsx"
      provides: "Reusable KPI card with icon, value, trend indicator"
  key_links:
    - from: "src/app/(protected)/dashboard/landlord/page.tsx"
      to: "src/services/landlord/portfolio-service.ts"
      via: "getPortfolioKPIs(supabase) called in Server Component"
      pattern: "getPortfolioKPIs"
    - from: "src/app/(protected)/dashboard/landlord/properties/page.tsx"
      to: "src/services/landlord/portfolio-service.ts"
      via: "getPortfolioProperties(supabase)"
      pattern: "getPortfolioProperties"
---

<objective>
Build the landlord dashboard shell (sidebar layout), dashboard home (9.1), portfolio view (9.2), individual property (9.3), add property (9.4), and create rental listing (9.5).

Purpose: These 5 pages plus the sidebar layout are the core of the landlord dashboard. All other plans reference this sidebar. The dashboard home provides the first FAANG-quality view with real data.

Output: layout.tsx + 5 page files + 3 shared components, all displaying real Supabase data
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-landlord-dashboard/14-CONTEXT.md
@.planning/phases/14-landlord-dashboard/14-RESEARCH.md
@src/services/landlord/portfolio-service.ts
@src/types/landlord.ts
@src/components/landlord/PortfolioGrid.tsx
@src/components/landlord/PropertyCard.tsx
@src/components/landlord/PropertyOverview.tsx
@src/app/(protected)/dashboard/landlord/portfolio/page.tsx
@src/app/(protected)/dashboard/landlord/properties/[id]/overview/page.tsx
@.planning/phases/14-landlord-dashboard/stitch/dashboard-home.html
@.planning/phases/14-landlord-dashboard/stitch/my-properties.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar layout + Dashboard Home (9.1)</name>
  <files>
    src/app/(protected)/dashboard/landlord/layout.tsx,
    src/app/(protected)/dashboard/landlord/page.tsx,
    src/components/landlord/LandlordSidebar.tsx,
    src/components/landlord/KpiCard.tsx,
    src/components/landlord/ComplianceAlertBanner.tsx,
    next.config.ts
  </files>
  <action>
**CRITICAL: Read stitch/dashboard-home.html before building.** Adapt the HTML layout to Next.js components. Use `#1B4D3E` as primary colour. Replace any orange from other Stitch files. Replace `material-symbols-outlined` with Lucide React equivalents.

**LandlordSidebar.tsx** — `"use client"` component:
Nav items (with Lucide icons):
- Dashboard (LayoutDashboard) → `/dashboard/landlord`
- Properties (Building2) → `/dashboard/landlord/properties`
- Tenants (Users) → `/dashboard/landlord/tenants`
- Rent (Banknote) → `/dashboard/landlord/rent`
- Compliance (ShieldCheck) → `/dashboard/landlord/compliance`
- Maintenance (Wrench) → `/dashboard/landlord/maintenance`
- Finance (TrendingUp) → `/dashboard/landlord/finance/expenses`
- Documents (FolderOpen) → `/dashboard/landlord/deposits`
- Legal (Scale) → `/dashboard/landlord/legal/notices`
- Tools (Calculator) → `/dashboard/landlord/tools/yield-calculator`
- Analytics (BarChart3) → `/dashboard/landlord/analytics`
- Find Agent (Handshake) → `/dashboard/landlord/find-agent`

Active state from Stitch: `border-l-4 border-[#1B4D3E] bg-[#1B4D3E]/10 text-[#1B4D3E]`
Inactive: `text-muted-foreground hover:bg-muted hover:text-foreground pl-4`

Desktop: `w-64 h-screen border-r bg-background fixed` (permanent)
Mobile: Shadcn `Sheet` component triggered by hamburger button (use `Menu` Lucide icon)
Use `usePathname()` from `next/navigation` for active state detection.

**layout.tsx** — Server Component:
```typescript
import { LandlordSidebar } from "@/components/landlord/LandlordSidebar";
export default function LandlordLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen">
      <LandlordSidebar />
      <main className="flex-1 overflow-auto pl-64 max-lg:pl-0">
        {children}
      </main>
    </div>
  );
}
```

**KpiCard.tsx** — reusable component:
Props: `{ title: string; value: string | number; icon: LucideIcon; trend?: { value: number; label: string }; variant?: "default" | "warning" | "danger" }`
Design from Stitch: Card with icon top-right, large value, small title below, trend indicator (green up / red down arrow with percentage). Warning variant = amber background/border, danger = red.

**ComplianceAlertBanner.tsx** — amber/red banner:
Props: `{ type: string; propertyAddress: string; expiryDate: string; daysUntilExpiry: number }`
Design: amber background for expiring_soon (<30d), red for expired. Includes CTA link to `/dashboard/landlord/compliance`.

**page.tsx (Dashboard Home — 9.1)** — Server Component:
1. `const supabase = await createClient()` (from `@/lib/supabase/server`)
2. `const kpis = await getPortfolioKPIs(supabase)` — real RPC call
3. `const complianceAlerts = await getComplianceSummary(supabase)` — filter expired + expiring_soon
4. Render 4 KPI cards: Total Properties, Occupancy Rate (`${kpis.occupancy_rate}%`), Monthly Income (`£${kpis.total_monthly_rent.toLocaleString()}`), Open Maintenance
5. Render ComplianceAlertBanner for each expiring/expired document (max 3 shown, "View all" link)
6. Add Suspense boundary with Skeleton fallback

**next.config.ts** — add redirect for old portfolio path:
```typescript
async redirects() {
  return [
    { source: "/dashboard/landlord/portfolio", destination: "/dashboard/landlord/properties", permanent: true },
    { source: "/dashboard/landlord/rent-collection", destination: "/dashboard/landlord/rent", permanent: true },
    { source: "/dashboard/landlord/finances", destination: "/dashboard/landlord/finance/expenses", permanent: true },
  ];
}
```
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10 && grep -r "getPortfolioKPIs" src/app/\(protected\)/dashboard/landlord/page.tsx && grep -r "getComplianceSummary" src/app/\(protected\)/dashboard/landlord/page.tsx</automated>
  </verify>
  <done>layout.tsx exists wrapping all landlord routes. Dashboard home renders 4 KPI cards. ComplianceAlertBanner shows for expiring documents. pnpm build passes with no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio View (9.2), Individual Property (9.3), Add Property (9.4), Create Rental Listing (9.5)</name>
  <files>
    src/app/(protected)/dashboard/landlord/properties/page.tsx,
    src/app/(protected)/dashboard/landlord/properties/[id]/page.tsx,
    src/app/(protected)/dashboard/landlord/properties/add/page.tsx,
    src/app/(protected)/dashboard/landlord/properties/[id]/listing/page.tsx
  </files>
  <action>
**CRITICAL: Read stitch/my-properties.html before building.** Use `#1B4D3E` (not the orange from that file).

**properties/page.tsx (9.2 — Portfolio View)** — Server Component with client wrapper:
1. Server fetches `getPortfolioProperties(supabase)` — returns properties with active tenancy joined
2. Pass to `PortfolioViewClient` (client component) for sorting/filtering UI
3. Portfolio header: total count badge, "Add Property" button linking to `/dashboard/landlord/properties/add`
4. Use existing `PortfolioGrid` + `PropertyCard` components from `src/components/landlord/`
5. PropertyCard must show: property image (or placeholder), address, status badge (Occupied/Vacant/Expiring), monthly rent `£X,XXX/mo`, gross yield `X.X%`, occupancy indicator
6. Filter tabs from Stitch: All | Occupied | Vacant | Expiring Soon
7. Sort dropdown: Date Added | Rent (high-low) | Yield | Alphabetical
8. Cursor-based pagination: 20 properties per page, "Load more" button

**properties/[id]/page.tsx (9.3 — Individual Property)** — Server Component:
1. Fetch data from multiple services in parallel using `Promise.all`:
   - `getTenanciesByProperty(supabase, id)` from tenancy-service
   - `getFinancialEntriesByProperty(supabase, id)` from financial-service
   - `getDocumentsByProperty(supabase, id)` from document-service
   - `getMaintenanceRequests(supabase, { propertyId: id })` from maintenance-service
2. Tab layout (Shadcn Tabs): Overview | Tenancy | Financials | Documents | Maintenance
3. Overview tab: property details, current tenancy status (dates, rent amount, tenant name), next compliance action
4. Tenancy tab: uses existing `TenancyForm` + `TenancyStatusBadge` components, shows lease dates, rent, deposit
5. Financials tab: uses existing `FinancialSummary` component, income/expense totals, last 5 entries
6. Documents tab: uses existing `DocumentList` component
7. Maintenance tab: uses existing `MaintenanceList` component
8. Breadcrumb: Properties / {property address}

**properties/add/page.tsx (9.4 — Add Property)** — `"use client"` form:
Uses react-hook-form + zod:
```typescript
const addPropertySchema = z.object({
  address_line_1: z.string().min(5, "Address required"),
  address_line_2: z.string().optional(),
  city: z.string().min(2, "City required"),
  postcode: z.string().regex(/^[A-Z]{1,2}[0-9][0-9A-Z]?\s?[0-9][A-Z]{2}$/i, "Valid UK postcode required"),
  property_type: z.enum(["house", "flat", "bungalow", "studio", "room"]),
  bedrooms: z.number().min(0).max(20),
  bathrooms: z.number().min(1).max(10),
  purchase_price: z.number().positive().optional(),
  is_rental: z.literal(true).default(true),
});
```
On submit: call Supabase insert to `properties` table with `landlord_id = user.id, is_rental = true`. On success: redirect to `/dashboard/landlord/properties/{newId}` + toast "Property added".

**properties/[id]/listing/page.tsx (9.5 — Create Rental Listing)** — `"use client"` form:
Minimal wizard (3 steps) to create a rental listing from this portfolio property:
- Step 1: Listing details (title auto-filled from property, description, available from date, rent amount, deposit amount)
- Step 2: Photos (reuse existing image upload pattern — react-dropzone, uploads to existing storage bucket)
- Step 3: Review + publish
On submit: insert to `listings` table with `property_id`, `listing_type = 'rental'`, `landlord_id`. On success: redirect to properties/[id] + toast "Listing created".
  </action>
  <verify>
    <automated>cd /Users/joanflerinbig/Documents/britv3.0 && pnpm build 2>&1 | tail -10 && grep -r "getPortfolioProperties" src/app/\(protected\)/dashboard/landlord/properties/page.tsx</automated>
  </verify>
  <done>All 4 pages exist and render. Portfolio view fetches real data from getPortfolioProperties. Individual property shows 5 tabs with real data. Add property form validates with zod and inserts to Supabase. pnpm build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with zero TypeScript errors
2. `/dashboard/landlord` route resolves to dashboard home (no 404)
3. All new pages exist at correct file paths
4. No mock data arrays remain in any page file
5. Redirects configured in next.config.ts for old Phase 6 paths
</verification>

<success_criteria>
- Sidebar layout wraps all 29 landlord pages via layout.tsx
- Dashboard home shows real KPI cards with data from getPortfolioKPIs RPC
- Portfolio view lists properties from real Supabase query with filtering/sorting
- Individual property shows real tenancy/financial/document/maintenance data in tabs
- Add property and Create rental listing forms submit to real Supabase
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-landlord-dashboard/14-03-SUMMARY.md`
</output>

---
phase: 05-ai-financial-tools
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - britv3.0/src/types/calculators.ts
  - britv3.0/src/lib/calculators/mortgage.ts
  - britv3.0/src/lib/calculators/mortgage.test.ts
  - britv3.0/src/lib/calculators/sdlt-rates.ts
  - britv3.0/src/lib/calculators/sdlt-rates.test.ts
  - britv3.0/src/lib/calculators/sdlt.ts
  - britv3.0/src/lib/calculators/sdlt.test.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-05

must_haves:
  truths:
    - "Mortgage calculator produces correct monthly payment for known inputs (200K @ 5% / 25yr = 1169.18)"
    - "SDLT calculator produces correct tax for all 3 buyer types at current HMRC rates"
    - "SDLT rates use April 2025 thresholds (125K standard nil-rate, 5% additional surcharge, 300K FTB)"
    - "Edge cases handled: 0% rate, 0 deposit, price <= 0, deposit > price"
  artifacts:
    - path: "britv3.0/src/lib/calculators/mortgage.ts"
      provides: "Pure mortgage calculation functions"
      exports: ["calculateMonthlyPayment", "calculateTotalRepayable"]
    - path: "britv3.0/src/lib/calculators/sdlt-rates.ts"
      provides: "SDLT rate band configuration matching HMRC"
      exports: ["SDLT_STANDARD", "SDLT_FIRST_TIME_BUYER", "SDLT_ADDITIONAL_PROPERTY_SURCHARGE", "SDLT_FTB_PRICE_CAP"]
    - path: "britv3.0/src/lib/calculators/sdlt.ts"
      provides: "SDLT calculation logic"
      exports: ["calculateSdlt"]
    - path: "britv3.0/src/types/calculators.ts"
      provides: "Calculator-related TypeScript types"
  key_links:
    - from: "britv3.0/src/lib/calculators/sdlt.ts"
      to: "britv3.0/src/lib/calculators/sdlt-rates.ts"
      via: "imports rate band config"
      pattern: "import.*from.*sdlt-rates"
---

<objective>
Build and test the pure calculator logic libraries using TDD: mortgage amortization formula and SDLT (Stamp Duty Land Tax) calculations with current HMRC rates.

Purpose: These are pure functions with well-defined inputs/outputs -- perfect TDD candidates. The mortgage calculator powers personalized "Est. X/mo" displays. The SDLT calculator is tax-sensitive and must use correct April 2025 rates (the epic spec references outdated 3% surcharge -- actual is 5%).

Output: Fully tested calculator libraries with zero dependencies on UI or API.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ai-financial-tools/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mortgage Calculator Pure Functions</name>
  <files>britv3.0/src/types/calculators.ts, britv3.0/src/lib/calculators/mortgage.ts, britv3.0/src/lib/calculators/mortgage.test.ts</files>
  <behavior>
    - calculateMonthlyPayment(200_000, 5, 25) returns 1169.18 (within 0.01)
    - calculateMonthlyPayment(300_000, 4.5, 30) returns 1520.06 (within 0.01)
    - calculateMonthlyPayment(100_000, 0, 25) returns 333.33 (zero interest edge case: P / (n*12))
    - calculateMonthlyPayment(0, 5, 25) returns 0 (zero principal)
    - calculateMonthlyPayment(200_000, 5, 0) returns 0 (zero term)
    - calculateMonthlyPayment(-100_000, 5, 25) returns 0 (negative principal)
    - calculateTotalRepayable(200_000, 5, 25) returns totalRepayable ~350,754 and totalInterest ~150,754
    - calculateAffordability(350_000, 50_000, 4.5, 25) returns monthly payment for 300K principal
  </behavior>
  <action>
    1. Create `src/types/calculators.ts` with:
       - `MortgageParams` type: { deposit: number; interestRate: number; termYears: number }
       - `MortgageResult` type: { monthlyPayment: number; totalRepayable: number; totalInterest: number }
       - `SdltBand` type: { threshold: number; rate: number }
       - `BuyerType`: "standard" | "first_time" | "additional"
       - `SdltResult` type: { totalTax: number; effectiveRate: number; bands: Array<{ from: number; to: number; rate: number; tax: number }> }

    2. Create `src/lib/calculators/mortgage.ts` with:
       - `calculateMonthlyPayment(principal: number, annualRate: number, termYears: number): number`
         Standard amortization formula: M = P * [r(1+r)^n] / [(1+r)^n - 1]
         Handle r=0 case: return P / (n*12)
         Handle P<=0 or n<=0: return 0
       - `calculateTotalRepayable(principal, annualRate, termYears): { totalRepayable: number; totalInterest: number }`
       - `calculateAffordability(propertyPrice: number, deposit: number, annualRate: number, termYears: number): MortgageResult`
         Computes principal = price - deposit, then calls calculateMonthlyPayment

    3. RED: Write tests first, run (must fail), commit
    4. GREEN: Implement functions, run tests (must pass), commit
    5. REFACTOR: Clean up if needed, ensure tests still pass, commit
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm vitest run src/lib/calculators/mortgage.test.ts</automated>
  </verify>
  <done>All mortgage calculator tests pass with correct values for known inputs and edge cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SDLT Calculator with Current HMRC Rates</name>
  <files>britv3.0/src/lib/calculators/sdlt-rates.ts, britv3.0/src/lib/calculators/sdlt-rates.test.ts, britv3.0/src/lib/calculators/sdlt.ts, britv3.0/src/lib/calculators/sdlt.test.ts</files>
  <behavior>
    SDLT Rates config tests:
    - SDLT_STANDARD has 5 bands: 0-125K (0%), 125K-250K (2%), 250K-925K (5%), 925K-1.5M (10%), 1.5M+ (12%)
    - SDLT_FIRST_TIME_BUYER has 2 bands: 0-300K (0%), 300K-500K (5%)
    - SDLT_ADDITIONAL_PROPERTY_SURCHARGE is 0.05 (5%, NOT 3%)
    - SDLT_FTB_PRICE_CAP is 500_000

    SDLT Calculation tests (verified against GOV.UK calculator):
    - Standard buyer at 250K: tax = 2,500 (125K @ 0% + 125K @ 2%)
    - Standard buyer at 500K: tax = 15,000 (125K @ 0% + 125K @ 2% + 250K @ 5%)
    - Standard buyer at 1M: tax = 41,250
    - First-time buyer at 300K: tax = 0 (full relief)
    - First-time buyer at 450K: tax = 7,500 (300K @ 0% + 150K @ 5%)
    - First-time buyer at 600K: uses STANDARD rates (exceeds 500K cap), tax = 20,000
    - Additional property at 250K: tax = 15,000 (125K @ 5% + 125K @ 7%)
    - Additional property at 500K: tax = 40,000
    - Price = 0: tax = 0
    - Price = -100K: tax = 0
  </behavior>
  <action>
    1. Create `src/lib/calculators/sdlt-rates.ts` with HMRC rate bands as documented in research.
       CRITICAL: Use April 2025 rates. Additional property surcharge = 5% (NOT 3% from epic spec).
       Standard nil-rate band = 125K (NOT 250K).
       FTB nil-rate = 300K (NOT 425K).
       Include source URL and last-updated comment.

    2. Create `src/lib/calculators/sdlt.ts` with:
       - `calculateSdlt(price: number, buyerType: BuyerType): SdltResult`
       - Internal `calculateBands(price, bands, surcharge)` helper
       - First-time buyer: if price > FTB_PRICE_CAP, fall back to standard rates
       - Additional: add 5% surcharge to every band
       - Returns totalTax (rounded to nearest pound), effectiveRate, and per-band breakdown

    3. RED: Write all tests first (both sdlt-rates.test.ts and sdlt.test.ts), run (must fail), commit
    4. GREEN: Implement, run tests (must pass), commit
    5. REFACTOR: Clean up if needed, commit
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm vitest run src/lib/calculators/sdlt-rates.test.ts src/lib/calculators/sdlt.test.ts</automated>
  </verify>
  <done>All SDLT tests pass with correct values for all 3 buyer types using April 2025 HMRC rates</done>
</task>

</tasks>

<verification>
- All calculator tests pass: `cd britv3.0 && pnpm vitest run src/lib/calculators/`
- Build succeeds: `cd britv3.0 && pnpm build`
- Lint passes: `cd britv3.0 && pnpm lint`
</verification>

<success_criteria>
- Mortgage calculator produces correct values for known inputs including edge cases
- SDLT calculator uses current April 2025 HMRC rates (5% additional surcharge, 125K nil-rate)
- First-time buyer relief correctly falls back to standard rates above 500K
- All calculations are pure functions with no side effects or API dependencies
- All tests green with TDD commit history (RED -> GREEN -> REFACTOR)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-financial-tools/05-02-SUMMARY.md`
</output>

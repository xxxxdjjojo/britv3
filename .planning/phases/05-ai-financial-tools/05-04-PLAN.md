---
phase: 05-ai-financial-tools
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - britv3.0/src/hooks/useMortgageParams.ts
  - britv3.0/src/hooks/useMortgageParams.test.ts
  - britv3.0/src/components/calculators/MortgageCalculator.tsx
  - britv3.0/src/components/calculators/SdltCalculator.tsx
  - britv3.0/src/components/calculators/PersonalizedEstimate.tsx
  - britv3.0/src/components/calculators/PersonalizedEstimate.test.tsx
  - britv3.0/src/app/(main)/tools/mortgage-calculator/page.tsx
  - britv3.0/src/app/(main)/tools/stamp-duty-calculator/page.tsx
  - britv3.0/src/components/property/OfferLetterPdf.tsx
  - britv3.0/src/components/property/OfferLetterButton.tsx
  - britv3.0/src/components/property/OfferLetterPdf.test.tsx
autonomous: true
requirements:
  - FIN-03
  - FIN-04
  - FIN-06

must_haves:
  truths:
    - "User can access mortgage calculator page and see real-time monthly payment as they adjust inputs"
    - "User can access SDLT calculator page and see tax breakdown by buyer type"
    - "User's mortgage parameters are saved to localStorage and loaded on return visits"
    - "Property listing cards show personalized 'Est. X/mo' based on saved mortgage parameters"
    - "User can generate an offer letter PDF client-side from property details"
  artifacts:
    - path: "britv3.0/src/hooks/useMortgageParams.ts"
      provides: "localStorage + DB sync hook for mortgage parameters"
      exports: ["useMortgageParams"]
    - path: "britv3.0/src/components/calculators/MortgageCalculator.tsx"
      provides: "Interactive mortgage calculator component"
    - path: "britv3.0/src/components/calculators/SdltCalculator.tsx"
      provides: "Interactive SDLT calculator component"
    - path: "britv3.0/src/components/calculators/PersonalizedEstimate.tsx"
      provides: "Est. X/mo display for listing cards"
    - path: "britv3.0/src/app/(main)/tools/mortgage-calculator/page.tsx"
      provides: "Mortgage calculator page route"
    - path: "britv3.0/src/app/(main)/tools/stamp-duty-calculator/page.tsx"
      provides: "SDLT calculator page route"
    - path: "britv3.0/src/components/property/OfferLetterButton.tsx"
      provides: "Button to generate and download offer letter PDF"
  key_links:
    - from: "britv3.0/src/components/calculators/MortgageCalculator.tsx"
      to: "britv3.0/src/lib/calculators/mortgage.ts"
      via: "imports calculateMonthlyPayment"
      pattern: "import.*calculateMonthlyPayment.*from.*calculators/mortgage"
    - from: "britv3.0/src/components/calculators/SdltCalculator.tsx"
      to: "britv3.0/src/lib/calculators/sdlt.ts"
      via: "imports calculateSdlt"
      pattern: "import.*calculateSdlt.*from.*calculators/sdlt"
    - from: "britv3.0/src/components/calculators/PersonalizedEstimate.tsx"
      to: "britv3.0/src/hooks/useMortgageParams.ts"
      via: "reads saved mortgage params"
      pattern: "useMortgageParams"
    - from: "britv3.0/src/components/property/OfferLetterButton.tsx"
      to: "britv3.0/src/components/property/OfferLetterPdf.tsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*OfferLetterPdf.*ssr.*false"
---

<objective>
Build the calculator UI pages, mortgage parameters persistence hook, personalized affordability display for listing cards, and client-side offer letter PDF generation.

Purpose: These are the user-facing financial tools. The calculator pages give buyers tools to understand costs. The personalized estimate on listing cards is a high-value feature showing "Est. X/mo" based on saved mortgage parameters. The offer letter PDF rounds out the buying workflow.

Output: Two tool pages (mortgage calculator, SDLT calculator), PersonalizedEstimate component for listing cards, useMortgageParams hook, and OfferLetterButton with PDF generation.
</objective>

<execution_context>
@/Users/joanflerinbig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joanflerinbig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ai-financial-tools/05-RESEARCH.md

@britv3.0/src/types/calculators.ts
@britv3.0/src/lib/calculators/mortgage.ts
@britv3.0/src/lib/calculators/sdlt.ts
@britv3.0/src/lib/calculators/sdlt-rates.ts
</context>

<interfaces>
<!-- These are the calculator lib exports from Plan 05-02 that this plan consumes -->

From src/lib/calculators/mortgage.ts:
```typescript
export function calculateMonthlyPayment(principal: number, annualRate: number, termYears: number): number;
export function calculateTotalRepayable(principal: number, annualRate: number, termYears: number): { totalRepayable: number; totalInterest: number };
export function calculateAffordability(propertyPrice: number, deposit: number, annualRate: number, termYears: number): MortgageResult;
```

From src/lib/calculators/sdlt.ts:
```typescript
export function calculateSdlt(price: number, buyerType: BuyerType): SdltResult;
```

From src/types/calculators.ts:
```typescript
export type MortgageParams = { deposit: number; interestRate: number; termYears: number };
export type MortgageResult = { monthlyPayment: number; totalRepayable: number; totalInterest: number };
export type BuyerType = "standard" | "first_time" | "additional";
export type SdltResult = { totalTax: number; effectiveRate: number; bands: Array<{ from: number; to: number; rate: number; tax: number }> };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mortgage params hook, calculator UI components, and tool pages</name>
  <files>
    britv3.0/src/hooks/useMortgageParams.ts,
    britv3.0/src/hooks/useMortgageParams.test.ts,
    britv3.0/src/components/calculators/MortgageCalculator.tsx,
    britv3.0/src/components/calculators/SdltCalculator.tsx,
    britv3.0/src/app/(main)/tools/mortgage-calculator/page.tsx,
    britv3.0/src/app/(main)/tools/stamp-duty-calculator/page.tsx
  </files>
  <action>
    1. Create `src/hooks/useMortgageParams.ts` ("use client"):
       - STORAGE_KEY = "britestate_mortgage_params"
       - `useMortgageParams()` hook returning:
         - `params`: MortgageParams | null (from useState, initialized from localStorage)
         - `saveParams(params: MortgageParams)`: saves to localStorage + optional Supabase profiles.preferences JSONB update if user is authenticated
         - `clearParams()`: removes from localStorage + clears DB if authenticated
         - `hasParams`: boolean shorthand
       - Uses useEffect to hydrate from localStorage on mount (avoids SSR mismatch)
       - Handles localStorage unavailability (incognito mode) gracefully

    2. Create `src/hooks/useMortgageParams.test.ts`:
       - Mock localStorage (getItem, setItem, removeItem)
       - Test: returns null when no stored params
       - Test: returns parsed params when stored
       - Test: saveParams writes to localStorage
       - Test: clearParams removes from localStorage
       - Test: handles JSON parse errors gracefully (returns null)

    3. Create `src/components/calculators/MortgageCalculator.tsx` ("use client"):
       - Interactive form with Shadcn Input/Slider components:
         - Property price (number input, default 300,000)
         - Deposit amount (number input or slider, default 30,000)
         - Deposit percentage display (computed: deposit/price * 100)
         - Interest rate (number input, default 4.5%, step 0.1)
         - Term (slider 5-40 years, default 25)
       - Real-time results panel (recalculates on every input change via useMemo):
         - Monthly payment (formatted as currency)
         - Total repayable
         - Total interest
         - Loan-to-value ratio
       - "Save these parameters" button: calls useMortgageParams.saveParams
       - Uses calculateMonthlyPayment and calculateTotalRepayable from mortgage.ts
       - Format all currency values with Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" })

    4. Create `src/components/calculators/SdltCalculator.tsx` ("use client"):
       - Buyer type selector: 3 radio buttons (First-time buyer, Home mover, Additional property) using Shadcn RadioGroup
       - Property price input (number, default 300,000)
       - Real-time results:
         - Total SDLT payable (formatted currency)
         - Effective rate (percentage)
         - Band breakdown table: From, To, Rate, Tax columns
       - Uses calculateSdlt from sdlt.ts
       - Shows note: "Rates effective from 1 April 2025. Scotland (LBTT) and Wales (LTT) have different rates."

    5. Create `src/app/(main)/tools/mortgage-calculator/page.tsx`:
       - Server Component page with metadata (title: "Mortgage Calculator | Britestate")
       - Renders MortgageCalculator component
       - Brief explainer text about how the calculator works
       - Link to SDLT calculator page

    6. Create `src/app/(main)/tools/stamp-duty-calculator/page.tsx`:
       - Server Component page with metadata (title: "Stamp Duty Calculator | Britestate")
       - Renders SdltCalculator component
       - Brief explainer about SDLT
       - Link to mortgage calculator page
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm vitest run src/hooks/useMortgageParams.test.ts && pnpm build</automated>
  </verify>
  <done>Both calculator pages render with real-time results, mortgage params hook saves/loads from localStorage, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: PersonalizedEstimate component with tests</name>
  <files>
    britv3.0/src/components/calculators/PersonalizedEstimate.tsx,
    britv3.0/src/components/calculators/PersonalizedEstimate.test.tsx
  </files>
  <action>
    1. Create `src/components/calculators/PersonalizedEstimate.tsx` ("use client"):
       - Props: propertyPrice (number)
       - Uses useMortgageParams hook to get saved params
       - If no params saved: renders nothing (null) -- no empty state clutter on listing cards
       - If params exist: computes monthly payment using calculateAffordability(propertyPrice, params.deposit, params.interestRate, params.termYears)
       - Renders: "Est. {monthly}/mo" in a small, styled badge (text-sm, muted color)
       - Tooltip (Shadcn Tooltip) showing: "Based on {deposit}% deposit, {rate}% rate, {term} years"
       - Link to mortgage calculator page for adjustment: small "Adjust" text link

    2. Create `src/components/calculators/PersonalizedEstimate.test.tsx`:
       - Mock useMortgageParams hook
       - Test: renders nothing when no params saved (returns null)
       - Test: renders "Est. X/mo" when params exist (mock params: deposit 50K, rate 4.5%, term 25yr, price 300K -> calculates 250K principal)
       - Test: renders correct formatted amount using GBP currency
       - Test: includes tooltip with parameter details
       - Test: includes link to /tools/mortgage-calculator
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm vitest run src/components/calculators/PersonalizedEstimate.test.tsx</automated>
  </verify>
  <done>PersonalizedEstimate shows "Est. X/mo" on listing cards when mortgage params are saved, renders nothing when absent, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Offer letter PDF generation (client-side)</name>
  <files>
    britv3.0/src/components/property/OfferLetterPdf.tsx,
    britv3.0/src/components/property/OfferLetterButton.tsx,
    britv3.0/src/components/property/OfferLetterPdf.test.tsx
  </files>
  <action>
    1. Create `src/components/property/OfferLetterPdf.tsx` ("use client"):
       - Uses @react-pdf/renderer: Document, Page, Text, View, StyleSheet
       - Props (OfferLetterData): buyerName, buyerAddress, sellerName (optional), propertyAddress, offerAmount, conditions (string[]), mortgageInPrinciple (boolean), chainFree (boolean), completionDate (string, optional), date (string)
       - Renders a formal UK property offer letter with:
         - Britestate header/logo placeholder
         - Date
         - "Dear [Seller/Vendor]" opening
         - Offer amount (formatted GBP)
         - Property address
         - Key terms: mortgage status, chain status, proposed completion
         - Conditions list
         - "Subject to contract and survey" standard disclaimer
         - Buyer signature block
       - Styled with @react-pdf/renderer StyleSheet (professional, clean layout)

    2. Create `src/components/property/OfferLetterButton.tsx` ("use client"):
       - Props: propertyAddress (string), propertyPrice (number)
       - Uses dynamic import: `const OfferLetterPdf = dynamic(() => import("./OfferLetterPdf"), { ssr: false })`
       - Form dialog (Shadcn Dialog) with fields:
         - Buyer name (required)
         - Buyer address (required)
         - Offer amount (number, default: property price)
         - Mortgage in principle (checkbox)
         - Chain free (checkbox)
         - Proposed completion date (optional date picker)
         - Additional conditions (textarea, one per line)
       - "Generate PDF" button that:
         - Creates a Blob from the PDF using @react-pdf/renderer's pdf() function
         - Triggers download as "offer-letter-{property-address-slug}.pdf"
       - Loading state while PDF generates

    3. Create `src/components/property/OfferLetterPdf.test.tsx`:
       - Test: OfferLetterPdf component renders without errors (use @react-pdf/renderer test utilities or render to string)
       - Test: contains buyer name, property address, and offer amount in output
       - Test: includes "Subject to contract and survey" disclaimer
       - Test: conditions list renders correctly
       - Note: Since @react-pdf/renderer needs browser APIs, tests should mock the renderer or use a minimal check that the component tree is valid React
  </action>
  <verify>
    <automated>cd britv3.0 && pnpm vitest run src/components/property/OfferLetterPdf.test.tsx && pnpm build</automated>
  </verify>
  <done>Offer letter PDF generates client-side with property details and buyer info, downloads as PDF file, build succeeds with dynamic import (no SSR errors)</done>
</task>

</tasks>

<verification>
- All calculator component tests pass: `cd britv3.0 && pnpm vitest run src/components/calculators/ src/hooks/useMortgageParams.test.ts`
- Offer letter tests pass: `cd britv3.0 && pnpm vitest run src/components/property/OfferLetterPdf.test.tsx`
- Build succeeds (critical for @react-pdf/renderer SSR check): `cd britv3.0 && pnpm build`
- Lint passes: `cd britv3.0 && pnpm lint`
- Calculator pages accessible at /tools/mortgage-calculator and /tools/stamp-duty-calculator
</verification>

<success_criteria>
- Mortgage calculator shows real-time monthly payment, total repayable, total interest
- SDLT calculator shows tax breakdown by band for all 3 buyer types
- Mortgage params saved to localStorage and loaded on return visits
- PersonalizedEstimate renders "Est. X/mo" on listing cards when params exist, nothing when absent
- Offer letter PDF generates and downloads client-side without server calls
- No SSR errors from @react-pdf/renderer (dynamic import with ssr: false)
- All tests green, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-financial-tools/05-04-SUMMARY.md`
</output>

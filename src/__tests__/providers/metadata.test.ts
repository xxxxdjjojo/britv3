import { describe, it } from "vitest";
// import { generateProviderMetadata } from "@/app/(main)/providers/[category]/[location]/[slug]/metadata"; // uncomment when component exists

describe("generateProviderMetadata", () => {
  it.todo("renders with correct data");
});

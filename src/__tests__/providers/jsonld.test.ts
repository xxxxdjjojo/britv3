import { describe, it } from "vitest";
// import { generateProviderJsonLd } from "@/lib/providers/jsonld"; // uncomment when component exists

describe("generateProviderJsonLd", () => {
  it.todo("renders with correct data");
});
